diff --git a/client/src/app/app.tsx b/client/src/app/app.tsx
index 96b8a4f..0e3e38f 100644
--- a/client/src/app/app.tsx
+++ b/client/src/app/app.tsx
@@ -1,12 +1,10 @@
 import { type JSX, useEffect } from "react";
-import { App as AntdApp, Flex, Layout, theme } from "antd";
+import { App as AntdApp, Layout, theme } from "antd";
 import { Outlet } from "react-router-dom";
 
 import { ThemeToggle } from "@/widgets";
 
-import { Container } from "@/shared/ui";
-
-const { Header, Content } = Layout;
+const { Content } = Layout;
 
 export const App = (): JSX.Element => {
   const { token } = theme.useToken();
@@ -18,13 +16,9 @@ export const App = (): JSX.Element => {
   return (
     <AntdApp>
       <Layout style={{ minHeight: "100vh", background: "transparent" }}>
-        <Header style={{ background: "transparent", padding: "16px 0", height: "auto" }}>
-          <Container>
-            <Flex justify="flex-end" align="center">
-              <ThemeToggle />
-            </Flex>
-          </Container>
-        </Header>
+        <div style={{ position: "fixed", top: 20, right: 24, zIndex: 100 }}>
+          <ThemeToggle />
+        </div>
 
         <Content>
           <Outlet />
diff --git a/client/src/app/providers/query-provider.tsx b/client/src/app/providers/query-provider.tsx
index 61b879e..a655ce6 100644
--- a/client/src/app/providers/query-provider.tsx
+++ b/client/src/app/providers/query-provider.tsx
@@ -4,10 +4,12 @@ import type { JSX } from "react";
 import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
 import { ReactQueryDevtools } from "@tanstack/react-query-devtools";
 
+const STALE_TIME_MS = 5 * 60 * 1000;
+
 const queryClient = new QueryClient({
   defaultOptions: {
     queries: {
-      staleTime: 1000 * 60 * 5
+      staleTime: STALE_TIME_MS
     }
   }
 });
diff --git a/client/src/app/router/index.tsx b/client/src/app/router/index.tsx
index f22d1e1..a383d85 100644
--- a/client/src/app/router/index.tsx
+++ b/client/src/app/router/index.tsx
@@ -6,19 +6,22 @@ import { App } from "@/app/app";
 import { AdDetailsPage, AdEditPage, AdsListPage } from "@/features/ads/pages";
 
 import { NotFoundPage } from "@/shared/pages/not-found-page/not-found-page";
+import { ErrorBoundary } from "@/shared/ui";
 
 export const Router = (): JSX.Element => {
   return (
     <BrowserRouter>
-      <Routes>
-        <Route path="/" element={<App />}>
-          <Route index element={<Navigate to="/ads" replace />} />
-          <Route path="ads" element={<AdsListPage />} />
-          <Route path="ads/:id" element={<AdDetailsPage />} />
-          <Route path="ads/:id/edit" element={<AdEditPage />} />
-          <Route path="*" element={<NotFoundPage />} />
-        </Route>
-      </Routes>
+      <ErrorBoundary>
+        <Routes>
+          <Route path="/" element={<App />}>
+            <Route index element={<Navigate to="/ads" replace />} />
+            <Route path="ads" element={<AdsListPage />} />
+            <Route path="ads/:id" element={<AdDetailsPage />} />
+            <Route path="ads/:id/edit" element={<AdEditPage />} />
+            <Route path="*" element={<NotFoundPage />} />
+          </Route>
+        </Routes>
+      </ErrorBoundary>
     </BrowserRouter>
   );
 };
diff --git a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.module.css b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.module.css
index 7cdaecb..aaf4b2e 100644
--- a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.module.css
+++ b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.module.css
@@ -2,3 +2,15 @@
   flex-shrink: 0;
   width: 340px;
 }
+
+@media (max-width: 1024px) {
+  .gallery {
+    width: 280px;
+  }
+}
+
+@media (max-width: 768px) {
+  .gallery {
+    width: 100%;
+  }
+}
diff --git a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
index d592519..ae576f8 100644
--- a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
+++ b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
@@ -5,14 +5,10 @@ import { PlaceholderGallery } from "@/features/ads/components/details/placeholde
 
 import css from "./ad-details-gallery.module.css";
 
-type AdDetailsGalleryProps = {
-  photos?: string[];
-};
-
-export const AdDetailsGallery = ({ photos }: AdDetailsGalleryProps): JSX.Element => {
+export const AdDetailsGallery = (): JSX.Element => {
   return (
     <Flex vertical gap={12} className={css.gallery}>
-      <PlaceholderGallery photos={photos} />
+      <PlaceholderGallery />
     </Flex>
   );
 };
diff --git a/client/src/features/ads/components/details/ad-details-header/ad-details-header.module.css b/client/src/features/ads/components/details/ad-details-header/ad-details-header.module.css
index e186059..4480c91 100644
--- a/client/src/features/ads/components/details/ad-details-header/ad-details-header.module.css
+++ b/client/src/features/ads/components/details/ad-details-header/ad-details-header.module.css
@@ -3,14 +3,41 @@
   padding: 0;
 }
 
+/* Desktop: grid with 2 columns
+   Col 1: title (row 1) + button (row 2)
+   Col 2: price block (rows 1-2) */
 .header {
+  display: grid;
+  grid-template-columns: 1fr auto;
+  grid-template-rows: auto auto;
+  column-gap: 24px;
+  row-gap: 8px;
   width: 100%;
+  padding-right: 56px;
 }
 
 .title {
+  grid-column: 1;
+  grid-row: 1;
   margin: 0 !important;
 }
 
+.priceBlock {
+  grid-column: 2;
+  grid-row: 1 / 3;
+  display: flex;
+  flex-direction: column;
+  align-items: flex-end;
+  gap: 4px;
+}
+
+.editButton {
+  grid-column: 1;
+  grid-row: 2;
+  align-self: start;
+  justify-self: start;
+}
+
 .price {
   margin: 0 !important;
 }
@@ -18,3 +45,26 @@
 .date {
   font-size: 13px;
 }
+
+@media (max-width: 600px) {
+  .header {
+    display: flex;
+    flex-direction: column;
+    gap: 8px;
+    padding-right: 0;
+  }
+
+  .priceBlock {
+    align-items: flex-start;
+    order: 1;
+  }
+
+  .title {
+    order: 0;
+  }
+
+  .editButton {
+    order: 2;
+    width: 100%;
+  }
+}
diff --git a/client/src/features/ads/components/details/ad-details-header/ad-details-header.tsx b/client/src/features/ads/components/details/ad-details-header/ad-details-header.tsx
index 2785641..3074e56 100644
--- a/client/src/features/ads/components/details/ad-details-header/ad-details-header.tsx
+++ b/client/src/features/ads/components/details/ad-details-header/ad-details-header.tsx
@@ -33,17 +33,12 @@ export const AdDetailsHeader = ({
         </Link>
       </Flex>
 
-      <Flex justify="space-between" align="flex-start" className={css.header}>
-        <Flex vertical gap={8}>
-          <Typography.Title level={2} className={css.title}>
-            {title}
-          </Typography.Title>
-          <Button type="primary" icon={<EditOutlined />} onClick={() => onEdit(adId)}>
-            Редактировать
-          </Button>
-        </Flex>
+      <div className={css.header}>
+        <Typography.Title level={2} className={css.title}>
+          {title}
+        </Typography.Title>
 
-        <Flex vertical align="flex-end" gap={4}>
+        <div className={css.priceBlock}>
           {priceLabel && (
             <Typography.Title level={3} className={css.price}>
               {priceLabel}
@@ -55,8 +50,17 @@ export const AdDetailsHeader = ({
           <Typography.Text type="secondary" className={css.date}>
             Отредактировано: {updatedAtLabel}
           </Typography.Text>
-        </Flex>
-      </Flex>
+        </div>
+
+        <Button
+          type="primary"
+          icon={<EditOutlined />}
+          onClick={() => onEdit(adId)}
+          className={css.editButton}
+        >
+          Редактировать
+        </Button>
+      </div>
     </>
   );
 };
diff --git a/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx b/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
index ab0a4f7..a0403b8 100644
--- a/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
+++ b/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
@@ -1,5 +1,5 @@
 import { Descriptions, Typography } from "antd";
-import type { JSX } from "react";
+import type { JSX, ReactNode } from "react";
 
 import type { AdType } from "@/features/ads/types/ad.types";
 
@@ -9,29 +9,76 @@ type AdSpecificationsProps = {
   params: AdType["params"];
 };
 
-type SpecificationFieldConfig = {
-  key: string;
-  label: string;
-  format?: (value: unknown) => string;
+type SpecEntry = { label: string; value: ReactNode };
+
+const ELECTRONICS_TYPE: Record<string, string> = {
+  phone: "Телефон",
+  laptop: "Ноутбук",
+  misc: "Другое"
+};
+
+const REAL_ESTATE_TYPE: Record<string, string> = {
+  flat: "Квартира",
+  house: "Дом",
+  room: "Комната"
+};
+
+const TRANSMISSION: Record<string, string> = {
+  automatic: "Автомат",
+  manual: "Механика"
 };
 
-const SPECIFICATION_FIELDS: SpecificationFieldConfig[] = [
-  { key: "type", label: "Тип" },
-  { key: "brand", label: "Бренд" },
-  { key: "model", label: "Модель" },
-  { key: "color", label: "Цвет" },
-  { key: "condition", label: "Состояние" },
-  { key: "address", label: "Адрес" },
-  { key: "area", label: "Площадь", format: (value) => `${value} м²` },
-  { key: "floor", label: "Этаж" },
-  { key: "transmission", label: "Коробка" },
-  { key: "mileage", label: "Пробег", format: (value) => `${value} км` },
-  { key: "enginePower", label: "Мощность", format: (value) => `${value} л.с.` },
-  { key: "yearOfManufacture", label: "Год выпуска" }
-];
+const CONDITION: Record<string, string> = {
+  new: "Новое",
+  used: "Б/у"
+};
+
+const getSpecEntries = (params: AdType["params"]): SpecEntry[] => {
+  const entries: SpecEntry[] = [];
+
+  if ("type" in params && params.type) {
+    const label = ELECTRONICS_TYPE[params.type] ?? REAL_ESTATE_TYPE[params.type] ?? params.type;
+    entries.push({ label: "Тип", value: label });
+  }
+  if ("brand" in params && params.brand) {
+    entries.push({ label: "Бренд", value: params.brand });
+  }
+  if ("model" in params && params.model) {
+    entries.push({ label: "Модель", value: params.model });
+  }
+  if ("color" in params && params.color) {
+    entries.push({ label: "Цвет", value: params.color });
+  }
+  if ("condition" in params && params.condition) {
+    entries.push({ label: "Состояние", value: CONDITION[params.condition] ?? params.condition });
+  }
+  if ("address" in params && params.address) {
+    entries.push({ label: "Адрес", value: params.address });
+  }
+  if ("area" in params && params.area) {
+    entries.push({ label: "Площадь", value: `${params.area} м²` });
+  }
+  if ("floor" in params && params.floor) {
+    entries.push({ label: "Этаж", value: params.floor });
+  }
+  if ("transmission" in params && params.transmission) {
+    entries.push({ label: "Коробка", value: TRANSMISSION[params.transmission] ?? params.transmission });
+  }
+  if ("mileage" in params && params.mileage) {
+    entries.push({ label: "Пробег", value: `${params.mileage} км` });
+  }
+  if ("enginePower" in params && params.enginePower) {
+    entries.push({ label: "Мощность", value: `${params.enginePower} л.с.` });
+  }
+  if ("yearOfManufacture" in params && params.yearOfManufacture) {
+    entries.push({ label: "Год выпуска", value: params.yearOfManufacture });
+  }
+
+  return entries;
+};
 
 export const AdSpecifications = ({ params }: AdSpecificationsProps): JSX.Element => {
-  const paramsRecord = params as Record<string, unknown>;
+  const entries = getSpecEntries(params);
 
   return (
     <div>
@@ -39,22 +86,11 @@ export const AdSpecifications = ({ params }: AdSpecificationsProps): JSX.Element
         Характеристики
       </Typography.Title>
       <Descriptions column={1} size="small" className={css.descriptions}>
-        {SPECIFICATION_FIELDS.map((field) => {
-          if (!(field.key in paramsRecord)) {
-            return null;
-          }
-
-          const value = paramsRecord[field.key];
-          if (!value) {
-            return null;
-          }
-
-          return (
-            <Descriptions.Item key={field.key} label={field.label}>
-              {field.format ? field.format(value) : String(value)}
-            </Descriptions.Item>
-          );
-        })}
+        {entries.map((entry) => (
+          <Descriptions.Item key={entry.label} label={entry.label}>
+            {entry.value}
+          </Descriptions.Item>
+        ))}
       </Descriptions>
     </div>
   );
diff --git a/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx b/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx
new file mode 100644
index 0000000..0485c46
--- /dev/null
+++ b/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx
@@ -0,0 +1,50 @@
+import { type JSX } from "react";
+import { Flex, Form, Input, Typography } from "antd";
+
+import {
+  DESCRIPTION_GEN_LABELS,
+  DESCRIPTION_IMPROVE_LABELS,
+  MAX_DESCRIPTION_LENGTH
+} from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+
+import { AiGenerateButton } from "@/shared/ui";
+
+type AdEditDescriptionFieldProps = {
+  description: string;
+  isPending: boolean;
+  onGenerateDescription?: (currentText: string) => Promise<string>;
+  onApplyDescription: (text: string) => void;
+};
+
+export const AdEditDescriptionField = ({
+  description,
+  isPending,
+  onGenerateDescription,
+  onApplyDescription
+}: AdEditDescriptionFieldProps): JSX.Element => {
+  const isDescriptionEmpty = description.trim().length === 0;
+
+  return (
+    <Form.Item
+      name="description"
+      label={
+        <Flex align="center" gap={12}>
+          <span>Описание</span>
+          {onGenerateDescription && (
+            <AiGenerateButton
+              onGenerate={() => onGenerateDescription(description)}
+              onApply={onApplyDescription}
+              labels={isDescriptionEmpty ? DESCRIPTION_GEN_LABELS : DESCRIPTION_IMPROVE_LABELS}
+            />
+          )}
+        </Flex>
+      }
+      extra={
+        <Typography.Text type="secondary" style={{ fontSize: 12 }}>
+          {description.length} / {MAX_DESCRIPTION_LENGTH}
+        </Typography.Text>
+      }>
+      <Input.TextArea rows={6} maxLength={MAX_DESCRIPTION_LENGTH} disabled={isPending} />
+    </Form.Item>
+  );
+};
diff --git a/client/src/features/ads/components/edit/ad-edit-description-field/index.ts b/client/src/features/ads/components/edit/ad-edit-description-field/index.ts
new file mode 100644
index 0000000..0f33294
--- /dev/null
+++ b/client/src/features/ads/components/edit/ad-edit-description-field/index.ts
@@ -0,0 +1 @@
+export { AdEditDescriptionField } from "./ad-edit-description-field";
diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
similarity index 56%
rename from client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts
rename to client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
index f5f4d7a..037a16a 100644
--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts
+++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
@@ -29,3 +29,28 @@ export const REAL_ESTATE_TYPE_OPTIONS = [
   { value: "house", label: "Дом" },
   { value: "room", label: "Комната" }
 ];
+
+const DEFAULT_AI_LABELS = {
+  buttonLoading: "Выполняется запрос",
+  buttonRetry: "Повторить запрос",
+  popoverTitle: "Ответ AI:",
+  popoverErrorTitle: "Ошибка запроса",
+  errorMessage: "Не удалось получить оценку. Попробуйте позже.",
+  applyButton: "Применить",
+  closeButton: "Закрыть"
+};
+
+export const PRICE_AI_LABELS = {
+  buttonDefault: "Узнать рыночную цену",
+  ...DEFAULT_AI_LABELS
+};
+
+export const DESCRIPTION_GEN_LABELS = {
+  buttonDefault: "Придумать описание",
+  ...DEFAULT_AI_LABELS
+};
+
+export const DESCRIPTION_IMPROVE_LABELS = {
+  buttonDefault: "Улучшить описание",
+  ...DEFAULT_AI_LABELS
+};
diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.module.css b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.module.css
index 6c371a6..5920f95 100644
--- a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.module.css
+++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.module.css
@@ -17,6 +17,12 @@
   width: 100% !important; /* внутренний input/select тянется на весь контейнер */
 }
 
+@media (max-width: 768px) {
+  .fieldFixedWidth {
+    max-width: 100%;
+  }
+}
+
 /* Цена: Input растягивается внутри flex, кнопка фиксированная */
 .priceInput {
   flex: 1;
diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
index d34f000..5f3832b 100644
--- a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
+++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
@@ -1,45 +1,18 @@
 import { type JSX, useCallback, useEffect } from "react";
-import { Button, Divider, Flex, Form, Input, Select, Typography } from "antd";
+import { Button, Divider, Flex, Form, Input, Select } from "antd";
 import { useNavigate } from "react-router-dom";
 
 import { useFormDraft } from "@/features/ads/hooks";
-import { useUpdateAd } from "@/features/ads/hooks/use-replace-ad";
-import { CATEGORY_OPTIONS, MAX_DESCRIPTION_LENGTH } from "@/features/ads/pages/ad-edit-page";
+import { useUpdateAd } from "@/features/ads/hooks/use-update-ad";
 import { type AdCategory, type AdType, type AdUpdateType } from "@/features/ads/types";
 
-import { AiGenerateButton } from "@/shared/ui";
-
+import { AdEditDescriptionField } from "../ad-edit-description-field";
 import { AdEditParamsFields } from "../ad-edit-params-fields";
+import { AdEditPriceField } from "../ad-edit-price-field";
 
-import css from "./ad-edit-form.module.css";
-
-const { Text } = Typography;
-const { TextArea } = Input;
-
-const DEFAULT_AI_LABELS = {
-  buttonLoading: "Выполняется запрос",
-  buttonRetry: "Повторить запрос",
-  popoverTitle: "Ответ AI:",
-  popoverErrorTitle: "Ошибка запроса",
-  errorMessage: "Не удалось получить оценку. Попробуйте позже.",
-  applyButton: "Применить",
-  closeButton: "Закрыть"
-};
-
-const PRICE_AI_LABELS = {
-  buttonDefault: "Узнать рыночную цену",
-  ...DEFAULT_AI_LABELS
-};
-
-const DESCRIPTION_GEN_LABELS = {
-  buttonDefault: "Придумать описание",
-  ...DEFAULT_AI_LABELS
-};
+import { CATEGORY_OPTIONS } from "./ad-edit-form.constants";
 
-const DESCRIPTION_IMPROVE_LABELS = {
-  buttonDefault: "Улучшить описание",
-  ...DEFAULT_AI_LABELS
-};
+import css from "./ad-edit-form.module.css";
 
 type AdEditFormProps = {
   adId: number;
@@ -48,12 +21,6 @@ type AdEditFormProps = {
   onGenerateDescription?: (currentText: string) => Promise<string>;
 };
 
-const extractPrice = (value: string): string | null => {
-  const cleaned = value.replace(/\s/g, "");
-  const match = cleaned.match(/\d+([.,]\d+)?/);
-  return match ? match[0].replace(",", ".") : null;
-};
-
 export const AdEditForm = ({
   adId,
   initialValues,
@@ -68,7 +35,6 @@ export const AdEditForm = ({
 
   const category = Form.useWatch("category", form);
   const description = Form.useWatch("description", form) ?? "";
-  const isDescriptionEmpty = description.trim().length === 0;
 
   useEffect(() => {
     if (initialValues && !form.isFieldsTouched()) {
@@ -149,64 +115,30 @@ export const AdEditForm = ({
 
       <Divider className={css.divider} />
 
-      <div className={css.fieldFixedWidth}>
-        <Text style={{ display: "block", marginBottom: 8 }}>Цена</Text>
-        <Flex gap={8} align="flex-start">
-          <Form.Item name="price" noStyle rules={[{ required: true }]}>
-            <Input
-              type="number"
-              allowClear
-              disabled={isPending}
-              className={css.priceInput}
-              placeholder="0"
-            />
-          </Form.Item>
-          {onGeneratePrice && (
-            <AiGenerateButton
-              onGenerate={onGeneratePrice}
-              onTransform={(raw) => extractPrice(raw) ?? ""}
-              onApply={handleApplyPrice}
-              labels={PRICE_AI_LABELS}
-            />
-          )}
-        </Flex>
-      </div>
+      <AdEditPriceField
+        isPending={isPending}
+        onGeneratePrice={onGeneratePrice}
+        onApplyPrice={handleApplyPrice}
+      />
 
       <Divider className={css.divider} />
 
       {category && (
         <>
           <div className={css.section}>
-            <Text strong className={css.sectionTitle}>
-              Характеристики
-            </Text>
+            <span className={css.sectionTitle}>Характеристики</span>
             <AdEditParamsFields category={category as AdCategory} />
           </div>
           <Divider className={css.divider} />
         </>
       )}
 
-      <Form.Item
-        name="description"
-        label={
-          <Flex align="center" gap={12}>
-            <span>Описание</span>
-            {onGenerateDescription && (
-              <AiGenerateButton
-                onGenerate={() => onGenerateDescription(description)}
-                onApply={handleApplyDescription}
-                labels={isDescriptionEmpty ? DESCRIPTION_GEN_LABELS : DESCRIPTION_IMPROVE_LABELS}
-              />
-            )}
-          </Flex>
-        }
-        extra={
-          <Text type="secondary" style={{ fontSize: 12 }}>
-            {description.length} / {MAX_DESCRIPTION_LENGTH}
-          </Text>
-        }>
-        <TextArea rows={6} maxLength={MAX_DESCRIPTION_LENGTH} disabled={isPending} />
-      </Form.Item>
+      <AdEditDescriptionField
+        description={description}
+        isPending={isPending}
+        onGenerateDescription={onGenerateDescription}
+        onApplyDescription={handleApplyDescription}
+      />
 
       <Flex gap={8}>
         <Button type="primary" htmlType="submit" loading={isPending}>
diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts
new file mode 100644
index 0000000..3fd8ef6
--- /dev/null
+++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts
@@ -0,0 +1,5 @@
+export const extractPrice = (value: string): string | null => {
+  const cleaned = value.replace(/\s/g, "");
+  const match = cleaned.match(/\d+([.,]\d+)?/);
+  return match ? match[0].replace(",", ".") : null;
+};
diff --git a/client/src/features/ads/components/edit/ad-edit-form/index.ts b/client/src/features/ads/components/edit/ad-edit-form/index.ts
index 3b1a965..270f1aa 100644
--- a/client/src/features/ads/components/edit/ad-edit-form/index.ts
+++ b/client/src/features/ads/components/edit/ad-edit-form/index.ts
@@ -1 +1,3 @@
 export * from "./ad-edit-form";
+export * from "./ad-edit-form.constants";
+export * from "./ad-edit-form.utils";
diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
index fb07ee6..1c72c21 100644
--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
+++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
@@ -1,7 +1,7 @@
 import { type JSX } from "react";
 
 import { ParamsFieldsList } from "@/features/ads/components";
-import { AUTO_TRANSMISSION_OPTIONS } from "@/features/ads/pages/ad-edit-page";
+import { AUTO_TRANSMISSION_OPTIONS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
 import type { FieldConfig } from "@/features/ads/types";
 
 const FIELDS: FieldConfig[] = [
diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
index 5b09fb9..54c7e65 100644
--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
+++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
@@ -4,7 +4,7 @@ import { ParamsFieldsList } from "@/features/ads/components";
 import {
   ELECTRONICS_CONDITION_OPTIONS,
   ELECTRONICS_TYPE_OPTIONS
-} from "@/features/ads/pages/ad-edit-page";
+} from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
 import type { FieldConfig } from "@/features/ads/types";
 
 const FIELDS: FieldConfig[] = [
diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
index 0ebba6c..a2c300c 100644
--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
+++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
@@ -1,7 +1,7 @@
 import { type JSX } from "react";
 
 import { ParamsFieldsList } from "@/features/ads/components";
-import { REAL_ESTATE_TYPE_OPTIONS } from "@/features/ads/pages/ad-edit-page";
+import { REAL_ESTATE_TYPE_OPTIONS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
 import type { FieldConfig } from "@/features/ads/types";
 
 const FIELDS: FieldConfig[] = [
diff --git a/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx b/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx
new file mode 100644
index 0000000..05da6ff
--- /dev/null
+++ b/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx
@@ -0,0 +1,46 @@
+import { type JSX } from "react";
+import { Flex, Form, Input, Typography } from "antd";
+
+import { PRICE_AI_LABELS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+import { extractPrice } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.utils";
+
+import { AiGenerateButton } from "@/shared/ui";
+
+import css from "../ad-edit-form/ad-edit-form.module.css";
+
+type AdEditPriceFieldProps = {
+  isPending: boolean;
+  onGeneratePrice?: () => Promise<string>;
+  onApplyPrice: (value: string) => void;
+};
+
+export const AdEditPriceField = ({
+  isPending,
+  onGeneratePrice,
+  onApplyPrice
+}: AdEditPriceFieldProps): JSX.Element => {
+  return (
+    <div className={css.fieldFixedWidth}>
+      <Typography.Text style={{ display: "block", marginBottom: 8 }}>Цена</Typography.Text>
+      <Flex gap={8} align="flex-start">
+        <Form.Item name="price" noStyle rules={[{ required: true }]}>
+          <Input
+            type="number"
+            allowClear
+            disabled={isPending}
+            className={css.priceInput}
+            placeholder="0"
+          />
+        </Form.Item>
+        {onGeneratePrice && (
+          <AiGenerateButton
+            onGenerate={onGeneratePrice}
+            onTransform={(raw) => extractPrice(raw) ?? ""}
+            onApply={onApplyPrice}
+            labels={PRICE_AI_LABELS}
+          />
+        )}
+      </Flex>
+    </div>
+  );
+};
diff --git a/client/src/features/ads/components/edit/ad-edit-price-field/index.ts b/client/src/features/ads/components/edit/ad-edit-price-field/index.ts
new file mode 100644
index 0000000..4043f76
--- /dev/null
+++ b/client/src/features/ads/components/edit/ad-edit-price-field/index.ts
@@ -0,0 +1 @@
+export { AdEditPriceField } from "./ad-edit-price-field";
diff --git a/client/src/features/ads/components/edit/index.ts b/client/src/features/ads/components/edit/index.ts
index 4916e7c..85979ef 100644
--- a/client/src/features/ads/components/edit/index.ts
+++ b/client/src/features/ads/components/edit/index.ts
@@ -1,3 +1,5 @@
 export * from "./ad-edit-form";
+export * from "./ad-edit-description-field";
 export * from "./ad-edit-params-fields";
+export * from "./ad-edit-price-field";
 export * from "./params-field-list";
diff --git a/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css b/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css
new file mode 100644
index 0000000..a14060a
--- /dev/null
+++ b/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css
@@ -0,0 +1,15 @@
+.fieldFixedWidth {
+  width: 100%;
+  max-width: 456px;
+}
+
+.fieldFixedWidth :global(.ant-input):not(.ant-input-textarea),
+.fieldFixedWidth :global(.ant-select-selector) {
+  width: 100% !important;
+}
+
+@media (max-width: 768px) {
+  .fieldFixedWidth {
+    max-width: 100%;
+  }
+}
diff --git a/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx b/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
index a795cfd..ba42782 100644
--- a/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
+++ b/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
@@ -3,12 +3,12 @@ import { Form, Input, Select } from "antd";
 
 import type { FieldConfig } from "@/features/ads/types";
 
+import css from "./params-field-list.module.css";
+
 type ParamsFieldsListProps = {
   fields: FieldConfig[];
 };
 
-import css from "../ad-edit-form/ad-edit-form.module.css";
-
 export const ParamsFieldsList = ({ fields }: ParamsFieldsListProps): JSX.Element => (
   <>
     {fields.map((field) => (
diff --git a/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css b/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
index c53cc14..7e6415b 100644
--- a/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
+++ b/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
@@ -8,10 +8,50 @@
   list-style: none;
 }
 
+@media (max-width: 1200px) {
+  .list {
+    grid-template-columns: repeat(4, minmax(0, 1fr));
+  }
+}
+
+@media (max-width: 900px) {
+  .list {
+    grid-template-columns: repeat(3, minmax(0, 1fr));
+    gap: 12px;
+  }
+}
+
+@media (max-width: 600px) {
+  .list {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+    gap: 10px;
+  }
+}
+
+@media (max-width: 360px) {
+  .list {
+    grid-template-columns: 1fr;
+  }
+}
+
 .list > li {
   display: flex;
 }
 
+.listView {
+  display: grid;
+  grid-template-columns: 1fr;
+  gap: 12px;
+
+  padding-left: 0;
+
+  list-style: none;
+}
+
+.listView > li {
+  display: flex;
+}
+
 .card {
   width: 100%;
   border-radius: 12px;
diff --git a/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx b/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
index cc51c67..9ed21bc 100644
--- a/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
+++ b/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
@@ -2,6 +2,8 @@ import { Card, Skeleton } from "antd";
 import type { JSX } from "react";
 
 import { AdCard } from "@/features/ads/components";
+import type { ViewMode } from "@/features/ads/components/list/toolbar/toolbar";
+import { SKELETON_COUNT } from "@/features/ads/constants";
 import type { AdCardType } from "@/features/ads/types";
 
 import css from "./ad-card-list.module.css";
@@ -9,13 +11,16 @@ import css from "./ad-card-list.module.css";
 type AdCardListProps = {
   ads: AdCardType[];
   isLoading: boolean;
+  viewMode: ViewMode;
 };
 
-export const AdCardList = ({ ads, isLoading }: AdCardListProps): JSX.Element => {
+export const AdCardList = ({ ads, isLoading, viewMode }: AdCardListProps): JSX.Element => {
+  const listClass = viewMode === "list" ? css.listView : css.list;
+
   if (isLoading) {
     return (
-      <ul className={css.list}>
-        {Array.from({ length: 10 }).map((_, index) => (
+      <ul className={listClass}>
+        {Array.from({ length: SKELETON_COUNT }).map((_, index) => (
           <Card key={index} className={css.card}>
             <Skeleton active />
           </Card>
@@ -25,7 +30,7 @@ export const AdCardList = ({ ads, isLoading }: AdCardListProps): JSX.Element =>
   }
 
   return (
-    <ul className={css.list}>
+    <ul className={listClass}>
       {ads.map((ad) => (
         <AdCard key={ad.id} ad={ad} />
       ))}
diff --git a/client/src/features/ads/components/list/filter/filter.module.css b/client/src/features/ads/components/list/filter/filter.module.css
index 1ed3874..3ecaa99 100644
--- a/client/src/features/ads/components/list/filter/filter.module.css
+++ b/client/src/features/ads/components/list/filter/filter.module.css
@@ -8,6 +8,18 @@
   font-family: sans-serif;
 }
 
+@media (max-width: 1024px) {
+  .container {
+    width: 220px;
+  }
+}
+
+@media (max-width: 768px) {
+  .container {
+    width: 100%;
+  }
+}
+
 .card {
   padding: 16px;
   border: none;
@@ -48,6 +60,18 @@
   background: var(--ant-color-bg-container) !important;
 }
 
+@media (max-width: 1024px) {
+  .resetButton {
+    width: 220px !important;
+  }
+}
+
+@media (max-width: 768px) {
+  .resetButton {
+    width: 100% !important;
+  }
+}
+
 .optionsWrapper {
   display: flex;
   flex-direction: column;
diff --git a/client/src/features/ads/components/list/filter/filter.tsx b/client/src/features/ads/components/list/filter/filter.tsx
index f4fc911..6b94920 100644
--- a/client/src/features/ads/components/list/filter/filter.tsx
+++ b/client/src/features/ads/components/list/filter/filter.tsx
@@ -2,25 +2,20 @@ import { type JSX } from "react";
 import { Button, Collapse, Typography } from "antd";
 
 import { FilterItems } from "@/features/ads/components";
-import type { AdCategory } from "@/features/ads/types";
+import { useFilterContext } from "@/features/ads/contexts";
 
 import css from "./filter.module.css";
 
-type FilterProps = {
-  selectedCategories: AdCategory[];
-  needsRevision: boolean;
-  onCategoriesChange: (categories: AdCategory[]) => void;
-  onNeedsRevisionChange: () => void;
-  onReset: () => void;
-};
-export const Filter = ({
-  selectedCategories,
-  needsRevision,
-  onCategoriesChange,
-  onNeedsRevisionChange,
-  onReset
-}: FilterProps): JSX.Element => {
-  const hasActiveFilters = selectedCategories.length > 0 || needsRevision;
+export const Filter = (): JSX.Element => {
+  const {
+    categories,
+    needsRevision,
+    changeCategories,
+    changeNeedsRevision,
+    resetSearch
+  } = useFilterContext();
+
+  const hasActiveFilters = categories.length > 0 || needsRevision;
 
   return (
     <div className={css.container}>
@@ -39,10 +34,10 @@ export const Filter = ({
               label: <span style={{ fontSize: "16px" }}>Категория</span>,
               children: (
                 <FilterItems
-                  selectedCategories={selectedCategories}
+                  selectedCategories={categories}
                   needsRevision={needsRevision}
-                  onCategoriesChange={onCategoriesChange}
-                  onNeedsRevisionChange={onNeedsRevisionChange}
+                  onCategoriesChange={changeCategories}
+                  onNeedsRevisionChange={changeNeedsRevision}
                 />
               )
             }
@@ -50,7 +45,7 @@ export const Filter = ({
         />
       </div>
 
-      <Button onClick={onReset} disabled={!hasActiveFilters} className={css.resetButton}>
+      <Button onClick={resetSearch} disabled={!hasActiveFilters} className={css.resetButton}>
         Сбросить фильтры
       </Button>
     </div>
diff --git a/client/src/features/ads/components/list/toolbar/toolbar.module.css b/client/src/features/ads/components/list/toolbar/toolbar.module.css
new file mode 100644
index 0000000..719d93d
--- /dev/null
+++ b/client/src/features/ads/components/list/toolbar/toolbar.module.css
@@ -0,0 +1,24 @@
+.toolbar {
+  width: 100%;
+}
+
+.sortSelect {
+  width: 240px;
+}
+
+@media (max-width: 768px) {
+  .sortSelect {
+    width: 100%;
+  }
+}
+
+@media (max-width: 480px) {
+  .controlsRow {
+    width: 100%;
+    justify-content: space-between;
+  }
+
+  .sortSelect {
+    flex: 1;
+  }
+}
diff --git a/client/src/features/ads/components/list/toolbar/toolbar.tsx b/client/src/features/ads/components/list/toolbar/toolbar.tsx
index dbc9bc7..f28e0ab 100644
--- a/client/src/features/ads/components/list/toolbar/toolbar.tsx
+++ b/client/src/features/ads/components/list/toolbar/toolbar.tsx
@@ -4,43 +4,60 @@ import { Button, Card, Flex, Input, Select, Space } from "antd";
 import type { AdsSortValue } from "@/features/ads/types";
 
 import { ADS_SORT_OPTIONS, DEFAULT_ADS_SORT } from "./toolbar.constants";
+import css from "./toolbar.module.css";
 
 import { AppstoreOutlined, UnorderedListOutlined } from "@ant-design/icons";
 
+export type ViewMode = "grid" | "list";
+
 type ToolbarProps = {
   defaultSearchValue: string;
   sortValue: AdsSortValue;
+  viewMode: ViewMode;
   onSearch: (value: string) => void;
   onSortChange: (value: AdsSortValue) => void;
+  onViewModeChange: (mode: ViewMode) => void;
 };
 export const Toolbar = ({
   defaultSearchValue,
   onSearch,
   sortValue,
-  onSortChange
+  onSortChange,
+  viewMode,
+  onViewModeChange
 }: ToolbarProps): JSX.Element => {
   return (
-    <Card styles={{ body: { padding: "12px 16px" } }}>
-      <Flex gap={24} align="center">
+    <Card styles={{ body: { padding: "12px 16px" } }} className={css.toolbar}>
+      <Flex gap={12} align="center" wrap="wrap">
         <Input.Search
           defaultValue={defaultSearchValue}
           onSearch={onSearch}
           allowClear
           placeholder="Найти объявление..."
-          style={{ flex: 1 }}
+          style={{ flex: 1, minWidth: 160 }}
         />
 
-        <Space>
-          <Button type="primary" icon={<AppstoreOutlined />} />
-          <Button type="default" icon={<UnorderedListOutlined />} />
-        </Space>
-        <Select
-          options={ADS_SORT_OPTIONS}
-          value={sortValue}
-          defaultValue={DEFAULT_ADS_SORT}
-          onChange={onSortChange}
-          style={{ width: 240 }}
-        />
+        <Flex gap={8} align="center" flex="none" className={css.controlsRow}>
+          <Space>
+            <Button
+              type={viewMode === "grid" ? "primary" : "default"}
+              icon={<AppstoreOutlined />}
+              onClick={() => onViewModeChange("grid")}
+            />
+            <Button
+              type={viewMode === "list" ? "primary" : "default"}
+              icon={<UnorderedListOutlined />}
+              onClick={() => onViewModeChange("list")}
+            />
+          </Space>
+          <Select
+            options={ADS_SORT_OPTIONS}
+            value={sortValue}
+            defaultValue={DEFAULT_ADS_SORT}
+            onChange={onSortChange}
+            className={css.sortSelect}
+          />
+        </Flex>
       </Flex>
     </Card>
   );
diff --git a/client/src/features/ads/constants/index.ts b/client/src/features/ads/constants/index.ts
index c1dadd8..4b16749 100644
--- a/client/src/features/ads/constants/index.ts
+++ b/client/src/features/ads/constants/index.ts
@@ -8,3 +8,6 @@ export const ADS_PLURAL_VARIANTS: PluralizeVariants = {
   many: "объявлений",
   other: "объявления"
 };
+
+export const PAGE_SIZE = 10;
+export const SKELETON_COUNT = 10;
diff --git a/client/src/features/ads/contexts/filter-context.tsx b/client/src/features/ads/contexts/filter-context.tsx
new file mode 100644
index 0000000..061a3d3
--- /dev/null
+++ b/client/src/features/ads/contexts/filter-context.tsx
@@ -0,0 +1,48 @@
+import { createContext, type ReactNode, useContext, useState, type JSX } from "react";
+
+import { useAdsFilters } from "@/features/ads/hooks/use-ads-filters";
+import type { AdCategory, AdsSortValue } from "@/features/ads/types";
+import type { AdCardType } from "@/features/ads/types/ad.types";
+
+import type { ViewMode } from "@/features/ads/components/list/toolbar/toolbar";
+import { PAGE_SIZE } from "@/features/ads/constants";
+
+type FilterContextType = {
+  items: AdCardType[];
+  total: number;
+  isLoading: boolean;
+  page: number;
+  searchValue: string;
+  categories: AdCategory[];
+  needsRevision: boolean;
+  sortValue: AdsSortValue;
+  viewMode: ViewMode;
+  setPage: (page: number) => void;
+  search: (value: string) => void;
+  changeCategories: (values: AdCategory[]) => void;
+  changeNeedsRevision: () => void;
+  resetSearch: () => void;
+  sort: (value: AdsSortValue) => void;
+  setViewMode: (mode: ViewMode) => void;
+};
+
+const FilterContext = createContext<FilterContextType | null>(null);
+
+export const useFilterContext = (): FilterContextType => {
+  const context = useContext(FilterContext);
+  if (!context) {
+    throw new Error("useFilterContext must be used within FilterProvider");
+  }
+  return context;
+};
+
+export const FilterProvider = ({ children }: { children: ReactNode }): JSX.Element => {
+  const filters = useAdsFilters(PAGE_SIZE);
+  const [viewMode, setViewMode] = useState<ViewMode>("grid");
+
+  return (
+    <FilterContext.Provider value={{ ...filters, viewMode, setViewMode }}>
+      {children}
+    </FilterContext.Provider>
+  );
+};
diff --git a/client/src/features/ads/contexts/index.ts b/client/src/features/ads/contexts/index.ts
new file mode 100644
index 0000000..3a84555
--- /dev/null
+++ b/client/src/features/ads/contexts/index.ts
@@ -0,0 +1 @@
+export { FilterProvider, useFilterContext } from "./filter-context";
diff --git a/client/src/features/ads/hooks/index.ts b/client/src/features/ads/hooks/index.ts
index 5c68350..c522b85 100644
--- a/client/src/features/ads/hooks/index.ts
+++ b/client/src/features/ads/hooks/index.ts
@@ -1,5 +1,6 @@
+export * from "./use-ad-ai-generation";
 export * from "./use-get-ad";
 export * from "./use-get-ads";
 export * from "./use-ads-filters";
-export * from "./use-replace-ad";
+export * from "./use-update-ad";
 export * from "./use-form-draft";
diff --git a/client/src/features/ads/hooks/use-ad-ai-generation.ts b/client/src/features/ads/hooks/use-ad-ai-generation.ts
new file mode 100644
index 0000000..7bf4e0e
--- /dev/null
+++ b/client/src/features/ads/hooks/use-ad-ai-generation.ts
@@ -0,0 +1,44 @@
+import type { AdType } from "@/features/ads/types";
+import {
+  DESCRIPTION_GENERATION_INSTRUCTION,
+  DESCRIPTION_IMPROVEMENT_INSTRUCTION,
+  PRICE_INSTRUCTION
+} from "@/features/ai/constants";
+import { useGenerateResponse, useGetModels } from "@/features/ai/hooks";
+
+type UseAdAiGenerationReturnType = {
+  isAiReady: boolean;
+  generatePrice: () => Promise<string>;
+  generateDescription: (currentDescription: string) => Promise<string>;
+};
+
+export const useAdAiGeneration = (ad?: AdType): UseAdAiGenerationReturnType => {
+  const { weakestModel, middleModel, strongestModel, isLoading } = useGetModels();
+  const { mutateAsync } = useGenerateResponse();
+
+  const activeModel = middleModel || weakestModel || strongestModel;
+  const isAiReady = !isLoading && !!activeModel;
+
+  const generatePrice = async (): Promise<string> => {
+    if (!activeModel || !ad) throw new Error("Модель недоступна");
+
+    const prompt = `${PRICE_INSTRUCTION}\n\nТовар: ${ad.title}, Категория: ${ad.category}, Параметры: ${JSON.stringify(ad.params)}\n\nОТВЕТЬ НА РУССКОМ.`;
+    return mutateAsync({ model: activeModel, prompt });
+  };
+
+  const generateDescription = async (currentDescription: string): Promise<string> => {
+    if (!activeModel || !ad) throw new Error("Модель недоступна");
+
+    const hasText = currentDescription.trim().length > 0;
+    const instruction = hasText
+      ? DESCRIPTION_IMPROVEMENT_INSTRUCTION
+      : DESCRIPTION_GENERATION_INSTRUCTION;
+
+    const context = `Товар: ${ad.title}, Характеристики: ${JSON.stringify(ad.params)}${hasText ? `\nТекущий текст для улучшения: ${currentDescription}` : ""}`;
+    const prompt = `${instruction}\n\n${context}\n\nВАЖНО: Пиши на русском языке.`;
+
+    return mutateAsync({ model: activeModel, prompt });
+  };
+
+  return { isAiReady, generatePrice, generateDescription };
+};
diff --git a/client/src/features/ads/hooks/use-form-draft.ts b/client/src/features/ads/hooks/use-form-draft.ts
index bfacb5a..212cb27 100644
--- a/client/src/features/ads/hooks/use-form-draft.ts
+++ b/client/src/features/ads/hooks/use-form-draft.ts
@@ -1,13 +1,45 @@
 import { useEffect } from "react";
 import type { FormInstance } from "antd";
 
-import { type AdUpdateType } from "../types";
+import { type AdCategory, type AdUpdateType } from "../types";
 
 type UseFormDraftReturnType = {
   saveDraft: () => void;
   clearDraft: () => void;
 };
 
+const VALID_CATEGORIES: AdCategory[] = ["auto", "real_estate", "electronics"];
+
+const isValidDraft = (data: unknown): data is Partial<AdUpdateType> => {
+  if (typeof data !== "object" || data === null || Array.isArray(data)) {
+    return false;
+  }
+
+  const draft = data as Record<string, unknown>;
+
+  if (draft.category !== undefined && !VALID_CATEGORIES.includes(draft.category as AdCategory)) {
+    return false;
+  }
+
+  if (draft.title !== undefined && typeof draft.title !== "string") {
+    return false;
+  }
+
+  if (draft.price !== undefined && typeof draft.price !== "number") {
+    return false;
+  }
+
+  if (draft.description !== undefined && typeof draft.description !== "string") {
+    return false;
+  }
+
+  if (draft.params !== undefined && (typeof draft.params !== "object" || draft.params === null)) {
+    return false;
+  }
+
+  return true;
+};
+
 export const useFormDraft = (
   id: string | undefined,
   form: FormInstance<AdUpdateType>
@@ -19,10 +51,16 @@ export const useFormDraft = (
     const savedDraft = localStorage.getItem(storageKey);
     if (savedDraft) {
       try {
-        const parsed = JSON.parse(savedDraft);
-        setTimeout(() => form.setFieldsValue(parsed), 0);
+        const parsed: unknown = JSON.parse(savedDraft);
+        if (isValidDraft(parsed)) {
+          setTimeout(() => form.setFieldsValue(parsed), 0);
+        } else {
+          console.warn("Черновик повреждён, удаляем:", storageKey);
+          localStorage.removeItem(storageKey);
+        }
       } catch (e) {
         console.error("Ошибка парсинга черновика", e);
+        localStorage.removeItem(storageKey);
       }
     }
   }, [id, form, storageKey]);
diff --git a/client/src/features/ads/hooks/use-replace-ad.ts b/client/src/features/ads/hooks/use-update-ad.ts
similarity index 58%
rename from client/src/features/ads/hooks/use-replace-ad.ts
rename to client/src/features/ads/hooks/use-update-ad.ts
index d3b651a..547d8ee 100644
--- a/client/src/features/ads/hooks/use-replace-ad.ts
+++ b/client/src/features/ads/hooks/use-update-ad.ts
@@ -7,14 +7,16 @@ import { replaceAd, type UpdateAdResponseType } from "../api/replace-ad";
 
 import { useMutation, type UseMutationResult, useQueryClient } from "@tanstack/react-query";
 
+import { type ApiErrorResponse, getApiErrorMessage } from "@/shared/api";
+
 export const useUpdateAd = (
   id: number
-): UseMutationResult<UpdateAdResponseType, AxiosError<UpdateAdResponseType>, AdUpdateType> => {
+): UseMutationResult<UpdateAdResponseType, AxiosError<ApiErrorResponse>, AdUpdateType> => {
   const queryClient = useQueryClient();
 
   const { message } = App.useApp();
 
-  return useMutation<UpdateAdResponseType, AxiosError<UpdateAdResponseType>, AdUpdateType>({
+  return useMutation<UpdateAdResponseType, AxiosError<ApiErrorResponse>, AdUpdateType>({
     mutationFn: (data: AdUpdateType) => replaceAd(id, data),
 
     onSuccess: (data) => {
@@ -26,15 +28,7 @@ export const useUpdateAd = (
     },
 
     onError: (error) => {
-      const serverError = error.response?.data?.error;
-
-      if (typeof serverError === "string") {
-        void message.error(serverError);
-      } else if (serverError && typeof serverError === "object") {
-        void message.error("Ошибка валидации полей");
-      } else {
-        void message.error("Произошла сетевая ошибка");
-      }
+      void message.error(getApiErrorMessage(error));
     }
   });
 };
diff --git a/client/src/features/ads/index.ts b/client/src/features/ads/index.ts
new file mode 100644
index 0000000..2331d4f
--- /dev/null
+++ b/client/src/features/ads/index.ts
@@ -0,0 +1,8 @@
+export * from "./api";
+export * from "./components";
+export * from "./constants";
+export * from "./contexts";
+export * from "./hooks";
+export * from "./lib";
+export * from "./types";
+export * from "./utils";
diff --git a/client/src/features/ads/lib/get-missing-fields.ts b/client/src/features/ads/lib/get-missing-fields.ts
index d53cbdb..df45aa6 100644
--- a/client/src/features/ads/lib/get-missing-fields.ts
+++ b/client/src/features/ads/lib/get-missing-fields.ts
@@ -1,18 +1,26 @@
 import type { AdResponse } from "@/features/ads/api/get-ad";
 import { FIELD_LABELS, REQUIRED_FIELDS_BY_CATEGORY } from "@/features/ads/lib/labels";
 
+const getParamValue = (params: AdResponse["params"], key: string): unknown => {
+  if (key in params) {
+    return (params as Record<string, unknown>)[key];
+  }
+  return undefined;
+};
+
 export const getMissingFields = (ad: AdResponse): string[] => {
   const missing: string[] = [];
   const missingSet = new Set<string>();
-  const paramsRecord = ad.params as Record<string, unknown>;
 
   if (!ad.description) {
     missing.push("Описание");
     missingSet.add("Описание");
   }
 
-  REQUIRED_FIELDS_BY_CATEGORY[ad.category].forEach((key) => {
-    const value = paramsRecord[key];
+  const requiredFields = REQUIRED_FIELDS_BY_CATEGORY[ad.category];
+
+  requiredFields.forEach((key) => {
+    const value = getParamValue(ad.params, key);
 
     if (!value) {
       const fieldName = FIELD_LABELS[key] ?? key;
@@ -24,15 +32,18 @@ export const getMissingFields = (ad: AdResponse): string[] => {
     }
   });
 
-  Object.entries(paramsRecord).forEach(([key, value]) => {
-    const isKnownCategoryField = REQUIRED_FIELDS_BY_CATEGORY[ad.category].includes(key);
-
-    if (!isKnownCategoryField && !value) {
-      const fieldName = FIELD_LABELS[key] ?? key;
-
-      if (!missingSet.has(fieldName)) {
-        missing.push(fieldName);
-        missingSet.add(fieldName);
+  const paramKeys = Object.keys(ad.params);
+  paramKeys.forEach((key) => {
+    const isRequired = requiredFields.includes(key);
+    if (!isRequired) {
+      const value = getParamValue(ad.params, key);
+      if (!value) {
+        const fieldName = FIELD_LABELS[key] ?? key;
+
+        if (!missingSet.has(fieldName)) {
+          missing.push(fieldName);
+          missingSet.add(fieldName);
+        }
       }
     }
   });
diff --git a/client/src/features/ads/pages/ad-details-page/ad-details-page.module.css b/client/src/features/ads/pages/ad-details-page/ad-details-page.module.css
index 7ebdc8d..516fe7e 100644
--- a/client/src/features/ads/pages/ad-details-page/ad-details-page.module.css
+++ b/client/src/features/ads/pages/ad-details-page/ad-details-page.module.css
@@ -1,6 +1,6 @@
 .page {
   min-height: 100vh;
-  padding: 24px 0;
+  padding: 20px 0 32px;
 }
 
 .container {
@@ -22,3 +22,24 @@
   flex: 1;
   min-width: 0;
 }
+
+@media (max-width: 768px) {
+  .page {
+    padding: 16px 0;
+  }
+
+  .container {
+    padding: 0 16px;
+    gap: 16px;
+  }
+
+  .content {
+    flex-direction: column !important;
+  }
+}
+
+@media (max-width: 480px) {
+  .container {
+    padding: 0 16px;
+  }
+}
diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css
deleted file mode 100644
index 34f14c9..0000000
--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css
+++ /dev/null
@@ -1,33 +0,0 @@
-.aiPopover :global(.ant-popover-inner) {
-  min-width: 320px;
-  padding: 16px;
-  border-radius: 12px;
-  background: white;
-}
-
-.aiContent {
-  display: flex;
-  flex-direction: column;
-  gap: 12px;
-}
-
-.aiTitle {
-  font-size: 16px;
-  font-weight: 600;
-}
-
-.aiText {
-  font-size: 14px;
-  line-height: 1.5;
-  white-space: pre-line;
-}
-
-.aiActions {
-  display: flex;
-  gap: 8px;
-}
-
-.highlight {
-  border-color: #52c41a !important;
-  box-shadow: 0 0 0 2px rgb(82 196 26 / 20%);
-}
diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
index 43db414..7b5c3d5 100644
--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
+++ b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
@@ -4,54 +4,25 @@ import { useParams } from "react-router-dom";
 
 import { AdEditForm } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form";
 import { AdEditFormSkeleton } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form-skeleton";
+import { useAdAiGeneration } from "@/features/ads/hooks/use-ad-ai-generation";
 import { useGetAd } from "@/features/ads/hooks";
-import {
-  DESCRIPTION_GENERATION_INSTRUCTION,
-  DESCRIPTION_IMPROVEMENT_INSTRUCTION,
-  PRICE_INSTRUCTION
-} from "@/features/ai/constants";
-import { useGenerateResponse, useGetModels } from "@/features/ai/hooks";
 
 import { Container } from "@/shared/ui";
 
-const { Title } = Typography;
-
 export const AdEditPage = (): JSX.Element => {
   const { id } = useParams<{ id: string }>();
   const adId = Number(id);
 
-  const { ad, isLoading: isAdLoading, isError, errorMessage } = useGetAd(adId);
-  const { weakestModel, middleModel, strongestModel, isLoading: isModelsLoading } = useGetModels();
-  const { mutateAsync } = useGenerateResponse();
-
-  const activeModel = middleModel || weakestModel || strongestModel;
-
-  const handleGeneratePrice = async (): Promise<string> => {
-    if (!activeModel || !ad) throw new Error("Модель недоступна");
-    const prompt = `${PRICE_INSTRUCTION}\n\nТовар: ${ad.title}, Категория: ${ad.category}, Параметры: ${JSON.stringify(ad.params)}\n\nОТВЕТЬ НА РУССКОМ.`;
-    return mutateAsync({ model: activeModel, prompt });
-  };
-
-  const handleGenerateDescription = async (currentDescription: string): Promise<string> => {
-    if (!activeModel || !ad) throw new Error("Модель недоступна");
-    const hasText = currentDescription.trim().length > 0;
-    const instruction = hasText
-      ? DESCRIPTION_IMPROVEMENT_INSTRUCTION
-      : DESCRIPTION_GENERATION_INSTRUCTION;
-
-    const context = `Товар: ${ad.title}, Характеристики: ${JSON.stringify(ad.params)}${hasText ? `\nТекущий текст для улучшения: ${currentDescription}` : ""}`;
-    const prompt = `${instruction}\n\n${context}\n\nВАЖНО: Пиши на русском языке.`;
-
-    return mutateAsync({ model: activeModel, prompt });
-  };
+  const { ad, isLoading, isError, errorMessage } = useGetAd(adId);
+  const { isAiReady, generatePrice, generateDescription } = useAdAiGeneration(ad);
 
   return (
     <Container maxWidth={942}>
-      <Title level={3} style={{ marginBottom: 24 }}>
+      <Typography.Title level={3} style={{ marginBottom: 24 }}>
         Редактирование объявления
-      </Title>
+      </Typography.Title>
 
-      {isAdLoading ? (
+      {isLoading ? (
         <AdEditFormSkeleton />
       ) : isError || !ad || Number.isNaN(adId) ? (
         <Alert
@@ -64,8 +35,8 @@ export const AdEditPage = (): JSX.Element => {
         <AdEditForm
           adId={adId}
           initialValues={ad}
-          onGeneratePrice={isModelsLoading ? undefined : handleGeneratePrice}
-          onGenerateDescription={isModelsLoading ? undefined : handleGenerateDescription}
+          onGeneratePrice={isAiReady ? generatePrice : undefined}
+          onGenerateDescription={isAiReady ? generateDescription : undefined}
         />
       )}
     </Container>
diff --git a/client/src/features/ads/pages/ad-edit-page/index.ts b/client/src/features/ads/pages/ad-edit-page/index.ts
index c98140b..22d2266 100644
--- a/client/src/features/ads/pages/ad-edit-page/index.ts
+++ b/client/src/features/ads/pages/ad-edit-page/index.ts
@@ -1,2 +1 @@
 export * from "./ad-edit-page";
-export * from "./ad-edit-page.constants";
diff --git a/client/src/features/ads/pages/ads-list-page/ads-list-page.module.css b/client/src/features/ads/pages/ads-list-page/ads-list-page.module.css
index fcce374..0610aad 100644
--- a/client/src/features/ads/pages/ads-list-page/ads-list-page.module.css
+++ b/client/src/features/ads/pages/ads-list-page/ads-list-page.module.css
@@ -1,3 +1,11 @@
+.page {
+  padding: 20px 0 32px;
+}
+
+.header {
+  padding-right: 56px;
+}
+
 .filterWrapper {
   flex: 0 0 256px;
 }
@@ -20,3 +28,16 @@
 .pagination {
   margin-top: auto;
 }
+
+@media (max-width: 1024px) {
+  .filterWrapper {
+    flex: 0 0 220px;
+  }
+}
+
+@media (max-width: 768px) {
+  .filterWrapper {
+    flex: 0 0 100%;
+    width: 100%;
+  }
+}
diff --git a/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx b/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
index a1272b1..563ec21 100644
--- a/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
+++ b/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
@@ -3,38 +3,33 @@ import { Flex, Space, Typography } from "antd";
 
 import { Filter, PaginationComponent, Toolbar } from "@/features/ads/components";
 import { AdCardList } from "@/features/ads/components/list/ad-card-list";
-import { ADS_PLURAL_VARIANTS } from "@/features/ads/constants";
-import { useAdsFilters } from "@/features/ads/hooks";
+import { ADS_PLURAL_VARIANTS, PAGE_SIZE } from "@/features/ads/constants";
+import { FilterProvider, useFilterContext } from "@/features/ads/contexts";
 import { formatAdsCount } from "@/features/ads/utils";
 
 import { Container } from "@/shared/ui";
 
 import css from "./ads-list-page.module.css";
 
-const pageSize = 10;
-
-export const AdsListPage = (): JSX.Element => {
+const AdsListContent = (): JSX.Element => {
   const {
     items,
     total,
     isLoading,
     page,
     searchValue,
-    categories,
-    needsRevision,
     sortValue,
+    viewMode,
     setPage,
     search,
-    changeCategories,
-    changeNeedsRevision,
-    resetSearch,
-    sort
-  } = useAdsFilters(pageSize);
+    sort,
+    setViewMode
+  } = useFilterContext();
 
   return (
-    <Container>
+    <Container className={css.page}>
       <Flex vertical gap={16}>
-        <Space orientation="vertical" size={4}>
+        <Space orientation="vertical" size={4} className={css.header}>
           <Typography.Title level={3} className={css.title}>
             Мои объявления
           </Typography.Title>
@@ -45,15 +40,9 @@ export const AdsListPage = (): JSX.Element => {
           )}
         </Space>
 
-        <Flex align="stretch" gap={48} className={css.contentWrapper}>
+        <Flex align="stretch" gap={32} wrap="wrap" className={css.contentWrapper}>
           <aside className={css.filterWrapper}>
-            <Filter
-              selectedCategories={categories}
-              needsRevision={needsRevision}
-              onCategoriesChange={changeCategories}
-              onNeedsRevisionChange={changeNeedsRevision}
-              onReset={resetSearch}
-            />
+            <Filter />
           </aside>
 
           <Flex vertical gap={16} className={css.sectionWrapper}>
@@ -62,14 +51,16 @@ export const AdsListPage = (): JSX.Element => {
               onSearch={search}
               sortValue={sortValue}
               onSortChange={sort}
+              viewMode={viewMode}
+              onViewModeChange={setViewMode}
             />
 
-            <AdCardList ads={items} isLoading={isLoading} />
+            <AdCardList ads={items} isLoading={isLoading} viewMode={viewMode} />
 
             <div className={css.pagination}>
               <PaginationComponent
                 current={page}
-                pageSize={pageSize}
+                pageSize={PAGE_SIZE}
                 total={total}
                 onChange={setPage}
               />
@@ -80,3 +71,11 @@ export const AdsListPage = (): JSX.Element => {
     </Container>
   );
 };
+
+export const AdsListPage = (): JSX.Element => {
+  return (
+    <FilterProvider>
+      <AdsListContent />
+    </FilterProvider>
+  );
+};
diff --git a/client/src/features/ai/api/generate-response.ts b/client/src/features/ai/api/generate-response.ts
index 7e86e4c..21ffdb9 100644
--- a/client/src/features/ai/api/generate-response.ts
+++ b/client/src/features/ai/api/generate-response.ts
@@ -6,6 +6,9 @@ import type {
 
 import { apiOllama } from "@/shared/api";
 
+const AI_TEMPERATURE = 0.1;
+const AI_MAX_TOKENS = 300;
+
 export const generateResponse = async (
   payload: GenerateRequestType
 ): Promise<SimplifiedGenerateResponseType> => {
@@ -14,8 +17,8 @@ export const generateResponse = async (
     prompt: payload.prompt,
     stream: false,
     options: {
-      temperature: 0.1,
-      num_predict: 300
+      temperature: AI_TEMPERATURE,
+      num_predict: AI_MAX_TOKENS
     }
   });
 
diff --git a/client/src/features/ai/index.ts b/client/src/features/ai/index.ts
new file mode 100644
index 0000000..48aff0b
--- /dev/null
+++ b/client/src/features/ai/index.ts
@@ -0,0 +1,4 @@
+export * from "./api";
+export * from "./constants";
+export * from "./hooks";
+export * from "./types";
diff --git a/client/src/features/ai/types/gerenate.types.ts b/client/src/features/ai/types/generate.types.ts
similarity index 100%
rename from client/src/features/ai/types/gerenate.types.ts
rename to client/src/features/ai/types/generate.types.ts
diff --git a/client/src/features/ai/types/index.ts b/client/src/features/ai/types/index.ts
index 9678987..c51eb1d 100644
--- a/client/src/features/ai/types/index.ts
+++ b/client/src/features/ai/types/index.ts
@@ -1,2 +1,2 @@
 export * from "./models.types";
-export * from "./gerenate.types";
+export * from "./generate.types";
diff --git a/client/src/shared/api/api-error.ts b/client/src/shared/api/api-error.ts
new file mode 100644
index 0000000..bd1fbef
--- /dev/null
+++ b/client/src/shared/api/api-error.ts
@@ -0,0 +1,28 @@
+import type { AxiosError } from "axios";
+
+export type ApiErrorResponse = {
+  error?: string | Record<string, string[]>;
+  message?: string;
+};
+
+export const getApiErrorMessage = (
+  error: AxiosError<ApiErrorResponse>,
+  fallback = "Произошла сетевая ошибка"
+): string => {
+  const serverError = error.response?.data?.error;
+
+  if (typeof serverError === "string") {
+    return serverError;
+  }
+
+  if (serverError && typeof serverError === "object") {
+    return "Ошибка валидации полей";
+  }
+
+  const message = error.response?.data?.message;
+  if (typeof message === "string") {
+    return message;
+  }
+
+  return fallback;
+};
diff --git a/client/src/shared/api/index.ts b/client/src/shared/api/index.ts
index b1120b9..073cfe6 100644
--- a/client/src/shared/api/index.ts
+++ b/client/src/shared/api/index.ts
@@ -1 +1,2 @@
 export * from "./instance";
+export * from "./api-error";
diff --git a/client/src/shared/api/instance.ts b/client/src/shared/api/instance.ts
index 744070a..3d5d14e 100644
--- a/client/src/shared/api/instance.ts
+++ b/client/src/shared/api/instance.ts
@@ -1,21 +1,48 @@
-import axios from "axios";
+import axios, { type AxiosError } from "axios";
 
 import { API_URL, OLLAMA_API_URL } from "@/shared/config";
 
+const API_TIMEOUT = 15_000;
+const OLLAMA_TIMEOUT = 60_000;
+const MAX_RETRIES = 2;
+const RETRY_DELAY = 1_000;
+
+const isRetryable = (error: AxiosError): boolean => {
+  if (!error.response) return true;
+  return error.response.status >= 500;
+};
+
+const delay = (ms: number): Promise<void> => new Promise((resolve) => setTimeout(resolve, ms));
+
+const createRetryInterceptor = (maxRetries: number) => {
+  return async (error: AxiosError): Promise<unknown> => {
+    const config = error.config;
+    if (!config) return Promise.reject(error);
+
+    const retryCount = (config as Record<string, unknown>).__retryCount as number ?? 0;
+
+    if (retryCount >= maxRetries || !isRetryable(error)) {
+      return Promise.reject(error);
+    }
+
+    (config as Record<string, unknown>).__retryCount = retryCount + 1;
+    await delay(RETRY_DELAY * (retryCount + 1));
+
+    return axios(config);
+  };
+};
+
 export const api = axios.create({
   baseURL: API_URL,
-  headers: { "Content-Type": "application/json" }
+  headers: { "Content-Type": "application/json" },
+  timeout: API_TIMEOUT
 });
 
 export const apiOllama = axios.create({
   baseURL: OLLAMA_API_URL,
-  headers: { "Content-Type": "application/json" }
+  headers: { "Content-Type": "application/json" },
+  timeout: OLLAMA_TIMEOUT
 });
 
-const errorHandler = (error: unknown): Promise<unknown> => {
-  console.error(`API Error: ${error || "Cannot get error message."}`);
-  return Promise.reject(error);
-};
-
-api.interceptors.response.use((r) => r, errorHandler);
-apiOllama.interceptors.response.use((r) => r, errorHandler);
+api.interceptors.response.use((r) => r, createRetryInterceptor(MAX_RETRIES));
+apiOllama.interceptors.response.use((r) => r, createRetryInterceptor(0));
diff --git a/client/src/shared/index.ts b/client/src/shared/index.ts
new file mode 100644
index 0000000..b857841
--- /dev/null
+++ b/client/src/shared/index.ts
@@ -0,0 +1,6 @@
+export * from "./api";
+export * from "./config";
+export * from "./contexts";
+export * from "./hooks";
+export * from "./lib";
+export * from "./ui";
diff --git a/client/src/shared/pages/not-found-page/not-found-page.module.css b/client/src/shared/pages/not-found-page/not-found-page.module.css
index d5797dd..0303c5b 100644
--- a/client/src/shared/pages/not-found-page/not-found-page.module.css
+++ b/client/src/shared/pages/not-found-page/not-found-page.module.css
@@ -6,3 +6,10 @@
 .title {
   margin: 0;
 }
+
+@media (max-width: 768px) {
+  .wrapper {
+    gap: 24px;
+    padding: 24px 16px;
+  }
+}
diff --git a/client/src/shared/ui/container/container.module.css b/client/src/shared/ui/container/container.module.css
index 0d1928d..800edf6 100644
--- a/client/src/shared/ui/container/container.module.css
+++ b/client/src/shared/ui/container/container.module.css
@@ -2,4 +2,17 @@
   width: 100%;
   max-width: 1280px;
   margin: 0 auto;
+  padding: 0 24px;
+}
+
+@media (max-width: 768px) {
+  .container {
+    padding: 0 16px;
+  }
+}
+
+@media (max-width: 480px) {
+  .container {
+    padding: 0 16px;
+  }
 }
diff --git a/client/src/shared/ui/container/container.tsx b/client/src/shared/ui/container/container.tsx
index 4ffa631..e6426fb 100644
--- a/client/src/shared/ui/container/container.tsx
+++ b/client/src/shared/ui/container/container.tsx
@@ -5,11 +5,15 @@ import css from "./container.module.css";
 type ContainerProps = {
   children: ReactNode;
   maxWidth?: number | string;
+  className?: string;
 };
 
-export const Container = ({ children, maxWidth }: ContainerProps): JSX.Element => {
+export const Container = ({ children, maxWidth, className }: ContainerProps): JSX.Element => {
   return (
-    <div className={css.container} style={maxWidth ? { maxWidth: maxWidth } : undefined}>
+    <div
+      className={[css.container, className].filter(Boolean).join(" ")}
+      style={maxWidth ? { maxWidth: maxWidth } : undefined}
+    >
       {children}
     </div>
   );
diff --git a/client/src/shared/ui/error-boundary/error-boundary.tsx b/client/src/shared/ui/error-boundary/error-boundary.tsx
new file mode 100644
index 0000000..b40ad94
--- /dev/null
+++ b/client/src/shared/ui/error-boundary/error-boundary.tsx
@@ -0,0 +1,60 @@
+import { Component, type ErrorInfo, type ReactNode } from "react";
+import { Alert, Button, Flex } from "antd";
+
+type ErrorBoundaryProps = {
+  children: ReactNode;
+  fallback?: ReactNode;
+};
+
+type ErrorBoundaryState = {
+  hasError: boolean;
+  error: Error | null;
+};
+
+export class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
+  constructor(props: ErrorBoundaryProps) {
+    super(props);
+    this.state = { hasError: false, error: null };
+  }
+
+  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
+    return { hasError: true, error };
+  }
+
+  componentDidCatch(error: Error, errorInfo: ErrorInfo): void {
+    console.error("ErrorBoundary caught:", error, errorInfo);
+  }
+
+  handleReset = (): void => {
+    this.setState({ hasError: false, error: null });
+  };
+
+  render(): ReactNode {
+    if (this.state.hasError) {
+      if (this.props.fallback) {
+        return this.props.fallback;
+      }
+
+      return (
+        <Flex
+          vertical
+          align="center"
+          justify="center"
+          style={{ padding: 48, minHeight: "50vh" }}>
+          <Alert
+            type="error"
+            showIcon
+            message="Что-то пошло не так"
+            description={this.state.error?.message ?? "Произошла непредвиденная ошибка"}
+            style={{ maxWidth: 600, width: "100%" }}
+          />
+          <Button type="primary" onClick={this.handleReset} style={{ marginTop: 16 }}>
+            Попробовать снова
+          </Button>
+        </Flex>
+      );
+    }
+
+    return this.props.children;
+  }
+}
diff --git a/client/src/shared/ui/error-boundary/index.ts b/client/src/shared/ui/error-boundary/index.ts
new file mode 100644
index 0000000..899c04f
--- /dev/null
+++ b/client/src/shared/ui/error-boundary/index.ts
@@ -0,0 +1 @@
+export { ErrorBoundary } from "./error-boundary";
diff --git a/client/src/shared/ui/index.ts b/client/src/shared/ui/index.ts
index d28c14e..31259c8 100644
--- a/client/src/shared/ui/index.ts
+++ b/client/src/shared/ui/index.ts
@@ -1,2 +1,3 @@
 export * from "./container";
 export * from "./ai-generate-button";
+export * from "./error-boundary";
diff --git a/file_name.patch b/file_name.patch
new file mode 100644
index 0000000..5e94756
--- /dev/null
+++ b/file_name.patch
@@ -0,0 +1,1561 @@
+diff --git a/client/src/app/providers/query-provider.tsx b/client/src/app/providers/query-provider.tsx
+index 61b879e..a655ce6 100644
+--- a/client/src/app/providers/query-provider.tsx
++++ b/client/src/app/providers/query-provider.tsx
+@@ -4,10 +4,12 @@ import type { JSX } from "react";
+ import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+ import { ReactQueryDevtools } from "@tanstack/react-query-devtools";
+ 
++const STALE_TIME_MS = 5 * 60 * 1000;
++
+ const queryClient = new QueryClient({
+   defaultOptions: {
+     queries: {
+-      staleTime: 1000 * 60 * 5
++      staleTime: STALE_TIME_MS
+     }
+   }
+ });
+diff --git a/client/src/app/router/index.tsx b/client/src/app/router/index.tsx
+index f22d1e1..a383d85 100644
+--- a/client/src/app/router/index.tsx
++++ b/client/src/app/router/index.tsx
+@@ -6,19 +6,22 @@ import { App } from "@/app/app";
+ import { AdDetailsPage, AdEditPage, AdsListPage } from "@/features/ads/pages";
+ 
+ import { NotFoundPage } from "@/shared/pages/not-found-page/not-found-page";
++import { ErrorBoundary } from "@/shared/ui";
+ 
+ export const Router = (): JSX.Element => {
+   return (
+     <BrowserRouter>
+-      <Routes>
+-        <Route path="/" element={<App />}>
+-          <Route index element={<Navigate to="/ads" replace />} />
+-          <Route path="ads" element={<AdsListPage />} />
+-          <Route path="ads/:id" element={<AdDetailsPage />} />
+-          <Route path="ads/:id/edit" element={<AdEditPage />} />
+-          <Route path="*" element={<NotFoundPage />} />
+-        </Route>
+-      </Routes>
++      <ErrorBoundary>
++        <Routes>
++          <Route path="/" element={<App />}>
++            <Route index element={<Navigate to="/ads" replace />} />
++            <Route path="ads" element={<AdsListPage />} />
++            <Route path="ads/:id" element={<AdDetailsPage />} />
++            <Route path="ads/:id/edit" element={<AdEditPage />} />
++            <Route path="*" element={<NotFoundPage />} />
++          </Route>
++        </Routes>
++      </ErrorBoundary>
+     </BrowserRouter>
+   );
+ };
+diff --git a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
+index d592519..ae576f8 100644
+--- a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
++++ b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
+@@ -5,14 +5,10 @@ import { PlaceholderGallery } from "@/features/ads/components/details/placeholde
+ 
+ import css from "./ad-details-gallery.module.css";
+ 
+-type AdDetailsGalleryProps = {
+-  photos?: string[];
+-};
+-
+-export const AdDetailsGallery = ({ photos }: AdDetailsGalleryProps): JSX.Element => {
++export const AdDetailsGallery = (): JSX.Element => {
+   return (
+     <Flex vertical gap={12} className={css.gallery}>
+-      <PlaceholderGallery photos={photos} />
++      <PlaceholderGallery />
+     </Flex>
+   );
+ };
+diff --git a/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx b/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
+index ab0a4f7..cea8162 100644
+--- a/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
++++ b/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
+@@ -1,5 +1,5 @@
+ import { Descriptions, Typography } from "antd";
+-import type { JSX } from "react";
++import type { JSX, ReactNode } from "react";
+ 
+ import type { AdType } from "@/features/ads/types/ad.types";
+ 
+@@ -9,29 +9,53 @@ type AdSpecificationsProps = {
+   params: AdType["params"];
+ };
+ 
+-type SpecificationFieldConfig = {
+-  key: string;
+-  label: string;
+-  format?: (value: unknown) => string;
+-};
++type SpecEntry = { label: string; value: ReactNode };
++
++const getSpecEntries = (params: AdType["params"]): SpecEntry[] => {
++  const entries: SpecEntry[] = [];
+ 
+-const SPECIFICATION_FIELDS: SpecificationFieldConfig[] = [
+-  { key: "type", label: "Тип" },
+-  { key: "brand", label: "Бренд" },
+-  { key: "model", label: "Модель" },
+-  { key: "color", label: "Цвет" },
+-  { key: "condition", label: "Состояние" },
+-  { key: "address", label: "Адрес" },
+-  { key: "area", label: "Площадь", format: (value) => `${value} м²` },
+-  { key: "floor", label: "Этаж" },
+-  { key: "transmission", label: "Коробка" },
+-  { key: "mileage", label: "Пробег", format: (value) => `${value} км` },
+-  { key: "enginePower", label: "Мощность", format: (value) => `${value} л.с.` },
+-  { key: "yearOfManufacture", label: "Год выпуска" }
+-];
++  if ("type" in params && params.type) {
++    entries.push({ label: "Тип", value: params.type });
++  }
++  if ("brand" in params && params.brand) {
++    entries.push({ label: "Бренд", value: params.brand });
++  }
++  if ("model" in params && params.model) {
++    entries.push({ label: "Модель", value: params.model });
++  }
++  if ("color" in params && params.color) {
++    entries.push({ label: "Цвет", value: params.color });
++  }
++  if ("condition" in params && params.condition) {
++    entries.push({ label: "Состояние", value: params.condition });
++  }
++  if ("address" in params && params.address) {
++    entries.push({ label: "Адрес", value: params.address });
++  }
++  if ("area" in params && params.area) {
++    entries.push({ label: "Площадь", value: `${params.area} м²` });
++  }
++  if ("floor" in params && params.floor) {
++    entries.push({ label: "Этаж", value: params.floor });
++  }
++  if ("transmission" in params && params.transmission) {
++    entries.push({ label: "Коробка", value: params.transmission });
++  }
++  if ("mileage" in params && params.mileage) {
++    entries.push({ label: "Пробег", value: `${params.mileage} км` });
++  }
++  if ("enginePower" in params && params.enginePower) {
++    entries.push({ label: "Мощность", value: `${params.enginePower} л.с.` });
++  }
++  if ("yearOfManufacture" in params && params.yearOfManufacture) {
++    entries.push({ label: "Год выпуска", value: params.yearOfManufacture });
++  }
++
++  return entries;
++};
+ 
+ export const AdSpecifications = ({ params }: AdSpecificationsProps): JSX.Element => {
+-  const paramsRecord = params as Record<string, unknown>;
++  const entries = getSpecEntries(params);
+ 
+   return (
+     <div>
+@@ -39,22 +63,11 @@ export const AdSpecifications = ({ params }: AdSpecificationsProps): JSX.Element
+         Характеристики
+       </Typography.Title>
+       <Descriptions column={1} size="small" className={css.descriptions}>
+-        {SPECIFICATION_FIELDS.map((field) => {
+-          if (!(field.key in paramsRecord)) {
+-            return null;
+-          }
+-
+-          const value = paramsRecord[field.key];
+-          if (!value) {
+-            return null;
+-          }
+-
+-          return (
+-            <Descriptions.Item key={field.key} label={field.label}>
+-              {field.format ? field.format(value) : String(value)}
+-            </Descriptions.Item>
+-          );
+-        })}
++        {entries.map((entry) => (
++          <Descriptions.Item key={entry.label} label={entry.label}>
++            {entry.value}
++          </Descriptions.Item>
++        ))}
+       </Descriptions>
+     </div>
+   );
+diff --git a/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx b/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx
+new file mode 100644
+index 0000000..0485c46
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx
+@@ -0,0 +1,50 @@
++import { type JSX } from "react";
++import { Flex, Form, Input, Typography } from "antd";
++
++import {
++  DESCRIPTION_GEN_LABELS,
++  DESCRIPTION_IMPROVE_LABELS,
++  MAX_DESCRIPTION_LENGTH
++} from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
++
++import { AiGenerateButton } from "@/shared/ui";
++
++type AdEditDescriptionFieldProps = {
++  description: string;
++  isPending: boolean;
++  onGenerateDescription?: (currentText: string) => Promise<string>;
++  onApplyDescription: (text: string) => void;
++};
++
++export const AdEditDescriptionField = ({
++  description,
++  isPending,
++  onGenerateDescription,
++  onApplyDescription
++}: AdEditDescriptionFieldProps): JSX.Element => {
++  const isDescriptionEmpty = description.trim().length === 0;
++
++  return (
++    <Form.Item
++      name="description"
++      label={
++        <Flex align="center" gap={12}>
++          <span>Описание</span>
++          {onGenerateDescription && (
++            <AiGenerateButton
++              onGenerate={() => onGenerateDescription(description)}
++              onApply={onApplyDescription}
++              labels={isDescriptionEmpty ? DESCRIPTION_GEN_LABELS : DESCRIPTION_IMPROVE_LABELS}
++            />
++          )}
++        </Flex>
++      }
++      extra={
++        <Typography.Text type="secondary" style={{ fontSize: 12 }}>
++          {description.length} / {MAX_DESCRIPTION_LENGTH}
++        </Typography.Text>
++      }>
++      <Input.TextArea rows={6} maxLength={MAX_DESCRIPTION_LENGTH} disabled={isPending} />
++    </Form.Item>
++  );
++};
+diff --git a/client/src/features/ads/components/edit/ad-edit-description-field/index.ts b/client/src/features/ads/components/edit/ad-edit-description-field/index.ts
+new file mode 100644
+index 0000000..0f33294
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-description-field/index.ts
+@@ -0,0 +1 @@
++export { AdEditDescriptionField } from "./ad-edit-description-field";
+diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
+similarity index 56%
+rename from client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts
+rename to client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
+index f5f4d7a..037a16a 100644
+--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts
++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
+@@ -29,3 +29,28 @@ export const REAL_ESTATE_TYPE_OPTIONS = [
+   { value: "house", label: "Дом" },
+   { value: "room", label: "Комната" }
+ ];
++
++const DEFAULT_AI_LABELS = {
++  buttonLoading: "Выполняется запрос",
++  buttonRetry: "Повторить запрос",
++  popoverTitle: "Ответ AI:",
++  popoverErrorTitle: "Ошибка запроса",
++  errorMessage: "Не удалось получить оценку. Попробуйте позже.",
++  applyButton: "Применить",
++  closeButton: "Закрыть"
++};
++
++export const PRICE_AI_LABELS = {
++  buttonDefault: "Узнать рыночную цену",
++  ...DEFAULT_AI_LABELS
++};
++
++export const DESCRIPTION_GEN_LABELS = {
++  buttonDefault: "Придумать описание",
++  ...DEFAULT_AI_LABELS
++};
++
++export const DESCRIPTION_IMPROVE_LABELS = {
++  buttonDefault: "Улучшить описание",
++  ...DEFAULT_AI_LABELS
++};
+diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
+index d34f000..5f3832b 100644
+--- a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
+@@ -1,45 +1,18 @@
+ import { type JSX, useCallback, useEffect } from "react";
+-import { Button, Divider, Flex, Form, Input, Select, Typography } from "antd";
++import { Button, Divider, Flex, Form, Input, Select } from "antd";
+ import { useNavigate } from "react-router-dom";
+ 
+ import { useFormDraft } from "@/features/ads/hooks";
+-import { useUpdateAd } from "@/features/ads/hooks/use-replace-ad";
+-import { CATEGORY_OPTIONS, MAX_DESCRIPTION_LENGTH } from "@/features/ads/pages/ad-edit-page";
++import { useUpdateAd } from "@/features/ads/hooks/use-update-ad";
+ import { type AdCategory, type AdType, type AdUpdateType } from "@/features/ads/types";
+ 
+-import { AiGenerateButton } from "@/shared/ui";
+-
++import { AdEditDescriptionField } from "../ad-edit-description-field";
+ import { AdEditParamsFields } from "../ad-edit-params-fields";
++import { AdEditPriceField } from "../ad-edit-price-field";
+ 
+-import css from "./ad-edit-form.module.css";
+-
+-const { Text } = Typography;
+-const { TextArea } = Input;
+-
+-const DEFAULT_AI_LABELS = {
+-  buttonLoading: "Выполняется запрос",
+-  buttonRetry: "Повторить запрос",
+-  popoverTitle: "Ответ AI:",
+-  popoverErrorTitle: "Ошибка запроса",
+-  errorMessage: "Не удалось получить оценку. Попробуйте позже.",
+-  applyButton: "Применить",
+-  closeButton: "Закрыть"
+-};
+-
+-const PRICE_AI_LABELS = {
+-  buttonDefault: "Узнать рыночную цену",
+-  ...DEFAULT_AI_LABELS
+-};
+-
+-const DESCRIPTION_GEN_LABELS = {
+-  buttonDefault: "Придумать описание",
+-  ...DEFAULT_AI_LABELS
+-};
++import { CATEGORY_OPTIONS } from "./ad-edit-form.constants";
+ 
+-const DESCRIPTION_IMPROVE_LABELS = {
+-  buttonDefault: "Улучшить описание",
+-  ...DEFAULT_AI_LABELS
+-};
++import css from "./ad-edit-form.module.css";
+ 
+ type AdEditFormProps = {
+   adId: number;
+@@ -48,12 +21,6 @@ type AdEditFormProps = {
+   onGenerateDescription?: (currentText: string) => Promise<string>;
+ };
+ 
+-const extractPrice = (value: string): string | null => {
+-  const cleaned = value.replace(/\s/g, "");
+-  const match = cleaned.match(/\d+([.,]\d+)?/);
+-  return match ? match[0].replace(",", ".") : null;
+-};
+-
+ export const AdEditForm = ({
+   adId,
+   initialValues,
+@@ -68,7 +35,6 @@ export const AdEditForm = ({
+ 
+   const category = Form.useWatch("category", form);
+   const description = Form.useWatch("description", form) ?? "";
+-  const isDescriptionEmpty = description.trim().length === 0;
+ 
+   useEffect(() => {
+     if (initialValues && !form.isFieldsTouched()) {
+@@ -149,64 +115,30 @@ export const AdEditForm = ({
+ 
+       <Divider className={css.divider} />
+ 
+-      <div className={css.fieldFixedWidth}>
+-        <Text style={{ display: "block", marginBottom: 8 }}>Цена</Text>
+-        <Flex gap={8} align="flex-start">
+-          <Form.Item name="price" noStyle rules={[{ required: true }]}>
+-            <Input
+-              type="number"
+-              allowClear
+-              disabled={isPending}
+-              className={css.priceInput}
+-              placeholder="0"
+-            />
+-          </Form.Item>
+-          {onGeneratePrice && (
+-            <AiGenerateButton
+-              onGenerate={onGeneratePrice}
+-              onTransform={(raw) => extractPrice(raw) ?? ""}
+-              onApply={handleApplyPrice}
+-              labels={PRICE_AI_LABELS}
+-            />
+-          )}
+-        </Flex>
+-      </div>
++      <AdEditPriceField
++        isPending={isPending}
++        onGeneratePrice={onGeneratePrice}
++        onApplyPrice={handleApplyPrice}
++      />
+ 
+       <Divider className={css.divider} />
+ 
+       {category && (
+         <>
+           <div className={css.section}>
+-            <Text strong className={css.sectionTitle}>
+-              Характеристики
+-            </Text>
++            <span className={css.sectionTitle}>Характеристики</span>
+             <AdEditParamsFields category={category as AdCategory} />
+           </div>
+           <Divider className={css.divider} />
+         </>
+       )}
+ 
+-      <Form.Item
+-        name="description"
+-        label={
+-          <Flex align="center" gap={12}>
+-            <span>Описание</span>
+-            {onGenerateDescription && (
+-              <AiGenerateButton
+-                onGenerate={() => onGenerateDescription(description)}
+-                onApply={handleApplyDescription}
+-                labels={isDescriptionEmpty ? DESCRIPTION_GEN_LABELS : DESCRIPTION_IMPROVE_LABELS}
+-              />
+-            )}
+-          </Flex>
+-        }
+-        extra={
+-          <Text type="secondary" style={{ fontSize: 12 }}>
+-            {description.length} / {MAX_DESCRIPTION_LENGTH}
+-          </Text>
+-        }>
+-        <TextArea rows={6} maxLength={MAX_DESCRIPTION_LENGTH} disabled={isPending} />
+-      </Form.Item>
++      <AdEditDescriptionField
++        description={description}
++        isPending={isPending}
++        onGenerateDescription={onGenerateDescription}
++        onApplyDescription={handleApplyDescription}
++      />
+ 
+       <Flex gap={8}>
+         <Button type="primary" htmlType="submit" loading={isPending}>
+diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts
+new file mode 100644
+index 0000000..3fd8ef6
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts
+@@ -0,0 +1,5 @@
++export const extractPrice = (value: string): string | null => {
++  const cleaned = value.replace(/\s/g, "");
++  const match = cleaned.match(/\d+([.,]\d+)?/);
++  return match ? match[0].replace(",", ".") : null;
++};
+diff --git a/client/src/features/ads/components/edit/ad-edit-form/index.ts b/client/src/features/ads/components/edit/ad-edit-form/index.ts
+index 3b1a965..270f1aa 100644
+--- a/client/src/features/ads/components/edit/ad-edit-form/index.ts
++++ b/client/src/features/ads/components/edit/ad-edit-form/index.ts
+@@ -1 +1,3 @@
+ export * from "./ad-edit-form";
++export * from "./ad-edit-form.constants";
++export * from "./ad-edit-form.utils";
+diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
+index fb07ee6..1c72c21 100644
+--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
++++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
+@@ -1,7 +1,7 @@
+ import { type JSX } from "react";
+ 
+ import { ParamsFieldsList } from "@/features/ads/components";
+-import { AUTO_TRANSMISSION_OPTIONS } from "@/features/ads/pages/ad-edit-page";
++import { AUTO_TRANSMISSION_OPTIONS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+ import type { FieldConfig } from "@/features/ads/types";
+ 
+ const FIELDS: FieldConfig[] = [
+diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
+index 5b09fb9..54c7e65 100644
+--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
++++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
+@@ -4,7 +4,7 @@ import { ParamsFieldsList } from "@/features/ads/components";
+ import {
+   ELECTRONICS_CONDITION_OPTIONS,
+   ELECTRONICS_TYPE_OPTIONS
+-} from "@/features/ads/pages/ad-edit-page";
++} from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+ import type { FieldConfig } from "@/features/ads/types";
+ 
+ const FIELDS: FieldConfig[] = [
+diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
+index 0ebba6c..a2c300c 100644
+--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
++++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
+@@ -1,7 +1,7 @@
+ import { type JSX } from "react";
+ 
+ import { ParamsFieldsList } from "@/features/ads/components";
+-import { REAL_ESTATE_TYPE_OPTIONS } from "@/features/ads/pages/ad-edit-page";
++import { REAL_ESTATE_TYPE_OPTIONS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+ import type { FieldConfig } from "@/features/ads/types";
+ 
+ const FIELDS: FieldConfig[] = [
+diff --git a/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx b/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx
+new file mode 100644
+index 0000000..05da6ff
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx
+@@ -0,0 +1,46 @@
++import { type JSX } from "react";
++import { Flex, Form, Input, Typography } from "antd";
++
++import { PRICE_AI_LABELS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
++import { extractPrice } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.utils";
++
++import { AiGenerateButton } from "@/shared/ui";
++
++import css from "../ad-edit-form/ad-edit-form.module.css";
++
++type AdEditPriceFieldProps = {
++  isPending: boolean;
++  onGeneratePrice?: () => Promise<string>;
++  onApplyPrice: (value: string) => void;
++};
++
++export const AdEditPriceField = ({
++  isPending,
++  onGeneratePrice,
++  onApplyPrice
++}: AdEditPriceFieldProps): JSX.Element => {
++  return (
++    <div className={css.fieldFixedWidth}>
++      <Typography.Text style={{ display: "block", marginBottom: 8 }}>Цена</Typography.Text>
++      <Flex gap={8} align="flex-start">
++        <Form.Item name="price" noStyle rules={[{ required: true }]}>
++          <Input
++            type="number"
++            allowClear
++            disabled={isPending}
++            className={css.priceInput}
++            placeholder="0"
++          />
++        </Form.Item>
++        {onGeneratePrice && (
++          <AiGenerateButton
++            onGenerate={onGeneratePrice}
++            onTransform={(raw) => extractPrice(raw) ?? ""}
++            onApply={onApplyPrice}
++            labels={PRICE_AI_LABELS}
++          />
++        )}
++      </Flex>
++    </div>
++  );
++};
+diff --git a/client/src/features/ads/components/edit/ad-edit-price-field/index.ts b/client/src/features/ads/components/edit/ad-edit-price-field/index.ts
+new file mode 100644
+index 0000000..4043f76
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-price-field/index.ts
+@@ -0,0 +1 @@
++export { AdEditPriceField } from "./ad-edit-price-field";
+diff --git a/client/src/features/ads/components/edit/index.ts b/client/src/features/ads/components/edit/index.ts
+index 4916e7c..85979ef 100644
+--- a/client/src/features/ads/components/edit/index.ts
++++ b/client/src/features/ads/components/edit/index.ts
+@@ -1,3 +1,5 @@
+ export * from "./ad-edit-form";
++export * from "./ad-edit-description-field";
+ export * from "./ad-edit-params-fields";
++export * from "./ad-edit-price-field";
+ export * from "./params-field-list";
+diff --git a/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css b/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css
+new file mode 100644
+index 0000000..c3df19f
+--- /dev/null
++++ b/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css
+@@ -0,0 +1,9 @@
++.fieldFixedWidth {
++  width: 100%;
++  max-width: 456px;
++}
++
++.fieldFixedWidth :global(.ant-input):not(.ant-input-textarea),
++.fieldFixedWidth :global(.ant-select-selector) {
++  width: 100% !important;
++}
+diff --git a/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx b/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
+index a795cfd..ba42782 100644
+--- a/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
++++ b/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
+@@ -3,12 +3,12 @@ import { Form, Input, Select } from "antd";
+ 
+ import type { FieldConfig } from "@/features/ads/types";
+ 
++import css from "./params-field-list.module.css";
++
+ type ParamsFieldsListProps = {
+   fields: FieldConfig[];
+ };
+ 
+-import css from "../ad-edit-form/ad-edit-form.module.css";
+-
+ export const ParamsFieldsList = ({ fields }: ParamsFieldsListProps): JSX.Element => (
+   <>
+     {fields.map((field) => (
+diff --git a/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css b/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
+index c53cc14..6d1c589 100644
+--- a/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
++++ b/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
+@@ -12,6 +12,20 @@
+   display: flex;
+ }
+ 
++.listView {
++  display: grid;
++  grid-template-columns: 1fr;
++  gap: 12px;
++
++  padding-left: 0;
++
++  list-style: none;
++}
++
++.listView > li {
++  display: flex;
++}
++
+ .card {
+   width: 100%;
+   border-radius: 12px;
+diff --git a/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx b/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
+index cc51c67..9ed21bc 100644
+--- a/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
++++ b/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
+@@ -2,6 +2,8 @@ import { Card, Skeleton } from "antd";
+ import type { JSX } from "react";
+ 
+ import { AdCard } from "@/features/ads/components";
++import type { ViewMode } from "@/features/ads/components/list/toolbar/toolbar";
++import { SKELETON_COUNT } from "@/features/ads/constants";
+ import type { AdCardType } from "@/features/ads/types";
+ 
+ import css from "./ad-card-list.module.css";
+@@ -9,13 +11,16 @@ import css from "./ad-card-list.module.css";
+ type AdCardListProps = {
+   ads: AdCardType[];
+   isLoading: boolean;
++  viewMode: ViewMode;
+ };
+ 
+-export const AdCardList = ({ ads, isLoading }: AdCardListProps): JSX.Element => {
++export const AdCardList = ({ ads, isLoading, viewMode }: AdCardListProps): JSX.Element => {
++  const listClass = viewMode === "list" ? css.listView : css.list;
++
+   if (isLoading) {
+     return (
+-      <ul className={css.list}>
+-        {Array.from({ length: 10 }).map((_, index) => (
++      <ul className={listClass}>
++        {Array.from({ length: SKELETON_COUNT }).map((_, index) => (
+           <Card key={index} className={css.card}>
+             <Skeleton active />
+           </Card>
+@@ -25,7 +30,7 @@ export const AdCardList = ({ ads, isLoading }: AdCardListProps): JSX.Element =>
+   }
+ 
+   return (
+-    <ul className={css.list}>
++    <ul className={listClass}>
+       {ads.map((ad) => (
+         <AdCard key={ad.id} ad={ad} />
+       ))}
+diff --git a/client/src/features/ads/components/list/filter/filter.tsx b/client/src/features/ads/components/list/filter/filter.tsx
+index f4fc911..6b94920 100644
+--- a/client/src/features/ads/components/list/filter/filter.tsx
++++ b/client/src/features/ads/components/list/filter/filter.tsx
+@@ -2,25 +2,20 @@ import { type JSX } from "react";
+ import { Button, Collapse, Typography } from "antd";
+ 
+ import { FilterItems } from "@/features/ads/components";
+-import type { AdCategory } from "@/features/ads/types";
++import { useFilterContext } from "@/features/ads/contexts";
+ 
+ import css from "./filter.module.css";
+ 
+-type FilterProps = {
+-  selectedCategories: AdCategory[];
+-  needsRevision: boolean;
+-  onCategoriesChange: (categories: AdCategory[]) => void;
+-  onNeedsRevisionChange: () => void;
+-  onReset: () => void;
+-};
+-export const Filter = ({
+-  selectedCategories,
+-  needsRevision,
+-  onCategoriesChange,
+-  onNeedsRevisionChange,
+-  onReset
+-}: FilterProps): JSX.Element => {
+-  const hasActiveFilters = selectedCategories.length > 0 || needsRevision;
++export const Filter = (): JSX.Element => {
++  const {
++    categories,
++    needsRevision,
++    changeCategories,
++    changeNeedsRevision,
++    resetSearch
++  } = useFilterContext();
++
++  const hasActiveFilters = categories.length > 0 || needsRevision;
+ 
+   return (
+     <div className={css.container}>
+@@ -39,10 +34,10 @@ export const Filter = ({
+               label: <span style={{ fontSize: "16px" }}>Категория</span>,
+               children: (
+                 <FilterItems
+-                  selectedCategories={selectedCategories}
++                  selectedCategories={categories}
+                   needsRevision={needsRevision}
+-                  onCategoriesChange={onCategoriesChange}
+-                  onNeedsRevisionChange={onNeedsRevisionChange}
++                  onCategoriesChange={changeCategories}
++                  onNeedsRevisionChange={changeNeedsRevision}
+                 />
+               )
+             }
+@@ -50,7 +45,7 @@ export const Filter = ({
+         />
+       </div>
+ 
+-      <Button onClick={onReset} disabled={!hasActiveFilters} className={css.resetButton}>
++      <Button onClick={resetSearch} disabled={!hasActiveFilters} className={css.resetButton}>
+         Сбросить фильтры
+       </Button>
+     </div>
+diff --git a/client/src/features/ads/components/list/toolbar/toolbar.tsx b/client/src/features/ads/components/list/toolbar/toolbar.tsx
+index dbc9bc7..2641b6e 100644
+--- a/client/src/features/ads/components/list/toolbar/toolbar.tsx
++++ b/client/src/features/ads/components/list/toolbar/toolbar.tsx
+@@ -7,17 +7,23 @@ import { ADS_SORT_OPTIONS, DEFAULT_ADS_SORT } from "./toolbar.constants";
+ 
+ import { AppstoreOutlined, UnorderedListOutlined } from "@ant-design/icons";
+ 
++export type ViewMode = "grid" | "list";
++
+ type ToolbarProps = {
+   defaultSearchValue: string;
+   sortValue: AdsSortValue;
++  viewMode: ViewMode;
+   onSearch: (value: string) => void;
+   onSortChange: (value: AdsSortValue) => void;
++  onViewModeChange: (mode: ViewMode) => void;
+ };
+ export const Toolbar = ({
+   defaultSearchValue,
+   onSearch,
+   sortValue,
+-  onSortChange
++  onSortChange,
++  viewMode,
++  onViewModeChange
+ }: ToolbarProps): JSX.Element => {
+   return (
+     <Card styles={{ body: { padding: "12px 16px" } }}>
+@@ -31,8 +37,16 @@ export const Toolbar = ({
+         />
+ 
+         <Space>
+-          <Button type="primary" icon={<AppstoreOutlined />} />
+-          <Button type="default" icon={<UnorderedListOutlined />} />
++          <Button
++            type={viewMode === "grid" ? "primary" : "default"}
++            icon={<AppstoreOutlined />}
++            onClick={() => onViewModeChange("grid")}
++          />
++          <Button
++            type={viewMode === "list" ? "primary" : "default"}
++            icon={<UnorderedListOutlined />}
++            onClick={() => onViewModeChange("list")}
++          />
+         </Space>
+         <Select
+           options={ADS_SORT_OPTIONS}
+diff --git a/client/src/features/ads/constants/index.ts b/client/src/features/ads/constants/index.ts
+index c1dadd8..4b16749 100644
+--- a/client/src/features/ads/constants/index.ts
++++ b/client/src/features/ads/constants/index.ts
+@@ -8,3 +8,6 @@ export const ADS_PLURAL_VARIANTS: PluralizeVariants = {
+   many: "объявлений",
+   other: "объявления"
+ };
++
++export const PAGE_SIZE = 10;
++export const SKELETON_COUNT = 10;
+diff --git a/client/src/features/ads/contexts/filter-context.tsx b/client/src/features/ads/contexts/filter-context.tsx
+new file mode 100644
+index 0000000..061a3d3
+--- /dev/null
++++ b/client/src/features/ads/contexts/filter-context.tsx
+@@ -0,0 +1,48 @@
++import { createContext, type ReactNode, useContext, useState, type JSX } from "react";
++
++import { useAdsFilters } from "@/features/ads/hooks/use-ads-filters";
++import type { AdCategory, AdsSortValue } from "@/features/ads/types";
++import type { AdCardType } from "@/features/ads/types/ad.types";
++
++import type { ViewMode } from "@/features/ads/components/list/toolbar/toolbar";
++import { PAGE_SIZE } from "@/features/ads/constants";
++
++type FilterContextType = {
++  items: AdCardType[];
++  total: number;
++  isLoading: boolean;
++  page: number;
++  searchValue: string;
++  categories: AdCategory[];
++  needsRevision: boolean;
++  sortValue: AdsSortValue;
++  viewMode: ViewMode;
++  setPage: (page: number) => void;
++  search: (value: string) => void;
++  changeCategories: (values: AdCategory[]) => void;
++  changeNeedsRevision: () => void;
++  resetSearch: () => void;
++  sort: (value: AdsSortValue) => void;
++  setViewMode: (mode: ViewMode) => void;
++};
++
++const FilterContext = createContext<FilterContextType | null>(null);
++
++export const useFilterContext = (): FilterContextType => {
++  const context = useContext(FilterContext);
++  if (!context) {
++    throw new Error("useFilterContext must be used within FilterProvider");
++  }
++  return context;
++};
++
++export const FilterProvider = ({ children }: { children: ReactNode }): JSX.Element => {
++  const filters = useAdsFilters(PAGE_SIZE);
++  const [viewMode, setViewMode] = useState<ViewMode>("grid");
++
++  return (
++    <FilterContext.Provider value={{ ...filters, viewMode, setViewMode }}>
++      {children}
++    </FilterContext.Provider>
++  );
++};
+diff --git a/client/src/features/ads/contexts/index.ts b/client/src/features/ads/contexts/index.ts
+new file mode 100644
+index 0000000..3a84555
+--- /dev/null
++++ b/client/src/features/ads/contexts/index.ts
+@@ -0,0 +1 @@
++export { FilterProvider, useFilterContext } from "./filter-context";
+diff --git a/client/src/features/ads/hooks/index.ts b/client/src/features/ads/hooks/index.ts
+index 5c68350..c522b85 100644
+--- a/client/src/features/ads/hooks/index.ts
++++ b/client/src/features/ads/hooks/index.ts
+@@ -1,5 +1,6 @@
++export * from "./use-ad-ai-generation";
+ export * from "./use-get-ad";
+ export * from "./use-get-ads";
+ export * from "./use-ads-filters";
+-export * from "./use-replace-ad";
++export * from "./use-update-ad";
+ export * from "./use-form-draft";
+diff --git a/client/src/features/ads/hooks/use-ad-ai-generation.ts b/client/src/features/ads/hooks/use-ad-ai-generation.ts
+new file mode 100644
+index 0000000..7bf4e0e
+--- /dev/null
++++ b/client/src/features/ads/hooks/use-ad-ai-generation.ts
+@@ -0,0 +1,44 @@
++import type { AdType } from "@/features/ads/types";
++import {
++  DESCRIPTION_GENERATION_INSTRUCTION,
++  DESCRIPTION_IMPROVEMENT_INSTRUCTION,
++  PRICE_INSTRUCTION
++} from "@/features/ai/constants";
++import { useGenerateResponse, useGetModels } from "@/features/ai/hooks";
++
++type UseAdAiGenerationReturnType = {
++  isAiReady: boolean;
++  generatePrice: () => Promise<string>;
++  generateDescription: (currentDescription: string) => Promise<string>;
++};
++
++export const useAdAiGeneration = (ad?: AdType): UseAdAiGenerationReturnType => {
++  const { weakestModel, middleModel, strongestModel, isLoading } = useGetModels();
++  const { mutateAsync } = useGenerateResponse();
++
++  const activeModel = middleModel || weakestModel || strongestModel;
++  const isAiReady = !isLoading && !!activeModel;
++
++  const generatePrice = async (): Promise<string> => {
++    if (!activeModel || !ad) throw new Error("Модель недоступна");
++
++    const prompt = `${PRICE_INSTRUCTION}\n\nТовар: ${ad.title}, Категория: ${ad.category}, Параметры: ${JSON.stringify(ad.params)}\n\nОТВЕТЬ НА РУССКОМ.`;
++    return mutateAsync({ model: activeModel, prompt });
++  };
++
++  const generateDescription = async (currentDescription: string): Promise<string> => {
++    if (!activeModel || !ad) throw new Error("Модель недоступна");
++
++    const hasText = currentDescription.trim().length > 0;
++    const instruction = hasText
++      ? DESCRIPTION_IMPROVEMENT_INSTRUCTION
++      : DESCRIPTION_GENERATION_INSTRUCTION;
++
++    const context = `Товар: ${ad.title}, Характеристики: ${JSON.stringify(ad.params)}${hasText ? `\nТекущий текст для улучшения: ${currentDescription}` : ""}`;
++    const prompt = `${instruction}\n\n${context}\n\nВАЖНО: Пиши на русском языке.`;
++
++    return mutateAsync({ model: activeModel, prompt });
++  };
++
++  return { isAiReady, generatePrice, generateDescription };
++};
+diff --git a/client/src/features/ads/hooks/use-form-draft.ts b/client/src/features/ads/hooks/use-form-draft.ts
+index bfacb5a..212cb27 100644
+--- a/client/src/features/ads/hooks/use-form-draft.ts
++++ b/client/src/features/ads/hooks/use-form-draft.ts
+@@ -1,13 +1,45 @@
+ import { useEffect } from "react";
+ import type { FormInstance } from "antd";
+ 
+-import { type AdUpdateType } from "../types";
++import { type AdCategory, type AdUpdateType } from "../types";
+ 
+ type UseFormDraftReturnType = {
+   saveDraft: () => void;
+   clearDraft: () => void;
+ };
+ 
++const VALID_CATEGORIES: AdCategory[] = ["auto", "real_estate", "electronics"];
++
++const isValidDraft = (data: unknown): data is Partial<AdUpdateType> => {
++  if (typeof data !== "object" || data === null || Array.isArray(data)) {
++    return false;
++  }
++
++  const draft = data as Record<string, unknown>;
++
++  if (draft.category !== undefined && !VALID_CATEGORIES.includes(draft.category as AdCategory)) {
++    return false;
++  }
++
++  if (draft.title !== undefined && typeof draft.title !== "string") {
++    return false;
++  }
++
++  if (draft.price !== undefined && typeof draft.price !== "number") {
++    return false;
++  }
++
++  if (draft.description !== undefined && typeof draft.description !== "string") {
++    return false;
++  }
++
++  if (draft.params !== undefined && (typeof draft.params !== "object" || draft.params === null)) {
++    return false;
++  }
++
++  return true;
++};
++
+ export const useFormDraft = (
+   id: string | undefined,
+   form: FormInstance<AdUpdateType>
+@@ -19,10 +51,16 @@ export const useFormDraft = (
+     const savedDraft = localStorage.getItem(storageKey);
+     if (savedDraft) {
+       try {
+-        const parsed = JSON.parse(savedDraft);
+-        setTimeout(() => form.setFieldsValue(parsed), 0);
++        const parsed: unknown = JSON.parse(savedDraft);
++        if (isValidDraft(parsed)) {
++          setTimeout(() => form.setFieldsValue(parsed), 0);
++        } else {
++          console.warn("Черновик повреждён, удаляем:", storageKey);
++          localStorage.removeItem(storageKey);
++        }
+       } catch (e) {
+         console.error("Ошибка парсинга черновика", e);
++        localStorage.removeItem(storageKey);
+       }
+     }
+   }, [id, form, storageKey]);
+diff --git a/client/src/features/ads/hooks/use-replace-ad.ts b/client/src/features/ads/hooks/use-update-ad.ts
+similarity index 58%
+rename from client/src/features/ads/hooks/use-replace-ad.ts
+rename to client/src/features/ads/hooks/use-update-ad.ts
+index d3b651a..547d8ee 100644
+--- a/client/src/features/ads/hooks/use-replace-ad.ts
++++ b/client/src/features/ads/hooks/use-update-ad.ts
+@@ -7,14 +7,16 @@ import { replaceAd, type UpdateAdResponseType } from "../api/replace-ad";
+ 
+ import { useMutation, type UseMutationResult, useQueryClient } from "@tanstack/react-query";
+ 
++import { type ApiErrorResponse, getApiErrorMessage } from "@/shared/api";
++
+ export const useUpdateAd = (
+   id: number
+-): UseMutationResult<UpdateAdResponseType, AxiosError<UpdateAdResponseType>, AdUpdateType> => {
++): UseMutationResult<UpdateAdResponseType, AxiosError<ApiErrorResponse>, AdUpdateType> => {
+   const queryClient = useQueryClient();
+ 
+   const { message } = App.useApp();
+ 
+-  return useMutation<UpdateAdResponseType, AxiosError<UpdateAdResponseType>, AdUpdateType>({
++  return useMutation<UpdateAdResponseType, AxiosError<ApiErrorResponse>, AdUpdateType>({
+     mutationFn: (data: AdUpdateType) => replaceAd(id, data),
+ 
+     onSuccess: (data) => {
+@@ -26,15 +28,7 @@ export const useUpdateAd = (
+     },
+ 
+     onError: (error) => {
+-      const serverError = error.response?.data?.error;
+-
+-      if (typeof serverError === "string") {
+-        void message.error(serverError);
+-      } else if (serverError && typeof serverError === "object") {
+-        void message.error("Ошибка валидации полей");
+-      } else {
+-        void message.error("Произошла сетевая ошибка");
+-      }
++      void message.error(getApiErrorMessage(error));
+     }
+   });
+ };
+diff --git a/client/src/features/ads/index.ts b/client/src/features/ads/index.ts
+new file mode 100644
+index 0000000..2331d4f
+--- /dev/null
++++ b/client/src/features/ads/index.ts
+@@ -0,0 +1,8 @@
++export * from "./api";
++export * from "./components";
++export * from "./constants";
++export * from "./contexts";
++export * from "./hooks";
++export * from "./lib";
++export * from "./types";
++export * from "./utils";
+diff --git a/client/src/features/ads/lib/get-missing-fields.ts b/client/src/features/ads/lib/get-missing-fields.ts
+index d53cbdb..df45aa6 100644
+--- a/client/src/features/ads/lib/get-missing-fields.ts
++++ b/client/src/features/ads/lib/get-missing-fields.ts
+@@ -1,18 +1,26 @@
+ import type { AdResponse } from "@/features/ads/api/get-ad";
+ import { FIELD_LABELS, REQUIRED_FIELDS_BY_CATEGORY } from "@/features/ads/lib/labels";
+ 
++const getParamValue = (params: AdResponse["params"], key: string): unknown => {
++  if (key in params) {
++    return (params as Record<string, unknown>)[key];
++  }
++  return undefined;
++};
++
+ export const getMissingFields = (ad: AdResponse): string[] => {
+   const missing: string[] = [];
+   const missingSet = new Set<string>();
+-  const paramsRecord = ad.params as Record<string, unknown>;
+ 
+   if (!ad.description) {
+     missing.push("Описание");
+     missingSet.add("Описание");
+   }
+ 
+-  REQUIRED_FIELDS_BY_CATEGORY[ad.category].forEach((key) => {
+-    const value = paramsRecord[key];
++  const requiredFields = REQUIRED_FIELDS_BY_CATEGORY[ad.category];
++
++  requiredFields.forEach((key) => {
++    const value = getParamValue(ad.params, key);
+ 
+     if (!value) {
+       const fieldName = FIELD_LABELS[key] ?? key;
+@@ -24,15 +32,18 @@ export const getMissingFields = (ad: AdResponse): string[] => {
+     }
+   });
+ 
+-  Object.entries(paramsRecord).forEach(([key, value]) => {
+-    const isKnownCategoryField = REQUIRED_FIELDS_BY_CATEGORY[ad.category].includes(key);
+-
+-    if (!isKnownCategoryField && !value) {
+-      const fieldName = FIELD_LABELS[key] ?? key;
+-
+-      if (!missingSet.has(fieldName)) {
+-        missing.push(fieldName);
+-        missingSet.add(fieldName);
++  const paramKeys = Object.keys(ad.params);
++  paramKeys.forEach((key) => {
++    const isRequired = requiredFields.includes(key);
++    if (!isRequired) {
++      const value = getParamValue(ad.params, key);
++      if (!value) {
++        const fieldName = FIELD_LABELS[key] ?? key;
++
++        if (!missingSet.has(fieldName)) {
++          missing.push(fieldName);
++          missingSet.add(fieldName);
++        }
+       }
+     }
+   });
+diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css
+deleted file mode 100644
+index 34f14c9..0000000
+--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css
++++ /dev/null
+@@ -1,33 +0,0 @@
+-.aiPopover :global(.ant-popover-inner) {
+-  min-width: 320px;
+-  padding: 16px;
+-  border-radius: 12px;
+-  background: white;
+-}
+-
+-.aiContent {
+-  display: flex;
+-  flex-direction: column;
+-  gap: 12px;
+-}
+-
+-.aiTitle {
+-  font-size: 16px;
+-  font-weight: 600;
+-}
+-
+-.aiText {
+-  font-size: 14px;
+-  line-height: 1.5;
+-  white-space: pre-line;
+-}
+-
+-.aiActions {
+-  display: flex;
+-  gap: 8px;
+-}
+-
+-.highlight {
+-  border-color: #52c41a !important;
+-  box-shadow: 0 0 0 2px rgb(82 196 26 / 20%);
+-}
+diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
+index 43db414..7b5c3d5 100644
+--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
++++ b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
+@@ -4,54 +4,25 @@ import { useParams } from "react-router-dom";
+ 
+ import { AdEditForm } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form";
+ import { AdEditFormSkeleton } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form-skeleton";
++import { useAdAiGeneration } from "@/features/ads/hooks/use-ad-ai-generation";
+ import { useGetAd } from "@/features/ads/hooks";
+-import {
+-  DESCRIPTION_GENERATION_INSTRUCTION,
+-  DESCRIPTION_IMPROVEMENT_INSTRUCTION,
+-  PRICE_INSTRUCTION
+-} from "@/features/ai/constants";
+-import { useGenerateResponse, useGetModels } from "@/features/ai/hooks";
+ 
+ import { Container } from "@/shared/ui";
+ 
+-const { Title } = Typography;
+-
+ export const AdEditPage = (): JSX.Element => {
+   const { id } = useParams<{ id: string }>();
+   const adId = Number(id);
+ 
+-  const { ad, isLoading: isAdLoading, isError, errorMessage } = useGetAd(adId);
+-  const { weakestModel, middleModel, strongestModel, isLoading: isModelsLoading } = useGetModels();
+-  const { mutateAsync } = useGenerateResponse();
+-
+-  const activeModel = middleModel || weakestModel || strongestModel;
+-
+-  const handleGeneratePrice = async (): Promise<string> => {
+-    if (!activeModel || !ad) throw new Error("Модель недоступна");
+-    const prompt = `${PRICE_INSTRUCTION}\n\nТовар: ${ad.title}, Категория: ${ad.category}, Параметры: ${JSON.stringify(ad.params)}\n\nОТВЕТЬ НА РУССКОМ.`;
+-    return mutateAsync({ model: activeModel, prompt });
+-  };
+-
+-  const handleGenerateDescription = async (currentDescription: string): Promise<string> => {
+-    if (!activeModel || !ad) throw new Error("Модель недоступна");
+-    const hasText = currentDescription.trim().length > 0;
+-    const instruction = hasText
+-      ? DESCRIPTION_IMPROVEMENT_INSTRUCTION
+-      : DESCRIPTION_GENERATION_INSTRUCTION;
+-
+-    const context = `Товар: ${ad.title}, Характеристики: ${JSON.stringify(ad.params)}${hasText ? `\nТекущий текст для улучшения: ${currentDescription}` : ""}`;
+-    const prompt = `${instruction}\n\n${context}\n\nВАЖНО: Пиши на русском языке.`;
+-
+-    return mutateAsync({ model: activeModel, prompt });
+-  };
++  const { ad, isLoading, isError, errorMessage } = useGetAd(adId);
++  const { isAiReady, generatePrice, generateDescription } = useAdAiGeneration(ad);
+ 
+   return (
+     <Container maxWidth={942}>
+-      <Title level={3} style={{ marginBottom: 24 }}>
++      <Typography.Title level={3} style={{ marginBottom: 24 }}>
+         Редактирование объявления
+-      </Title>
++      </Typography.Title>
+ 
+-      {isAdLoading ? (
++      {isLoading ? (
+         <AdEditFormSkeleton />
+       ) : isError || !ad || Number.isNaN(adId) ? (
+         <Alert
+@@ -64,8 +35,8 @@ export const AdEditPage = (): JSX.Element => {
+         <AdEditForm
+           adId={adId}
+           initialValues={ad}
+-          onGeneratePrice={isModelsLoading ? undefined : handleGeneratePrice}
+-          onGenerateDescription={isModelsLoading ? undefined : handleGenerateDescription}
++          onGeneratePrice={isAiReady ? generatePrice : undefined}
++          onGenerateDescription={isAiReady ? generateDescription : undefined}
+         />
+       )}
+     </Container>
+diff --git a/client/src/features/ads/pages/ad-edit-page/index.ts b/client/src/features/ads/pages/ad-edit-page/index.ts
+index c98140b..22d2266 100644
+--- a/client/src/features/ads/pages/ad-edit-page/index.ts
++++ b/client/src/features/ads/pages/ad-edit-page/index.ts
+@@ -1,2 +1 @@
+ export * from "./ad-edit-page";
+-export * from "./ad-edit-page.constants";
+diff --git a/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx b/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
+index a1272b1..b749908 100644
+--- a/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
++++ b/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
+@@ -3,33 +3,28 @@ import { Flex, Space, Typography } from "antd";
+ 
+ import { Filter, PaginationComponent, Toolbar } from "@/features/ads/components";
+ import { AdCardList } from "@/features/ads/components/list/ad-card-list";
+-import { ADS_PLURAL_VARIANTS } from "@/features/ads/constants";
+-import { useAdsFilters } from "@/features/ads/hooks";
++import { ADS_PLURAL_VARIANTS, PAGE_SIZE } from "@/features/ads/constants";
++import { FilterProvider, useFilterContext } from "@/features/ads/contexts";
+ import { formatAdsCount } from "@/features/ads/utils";
+ 
+ import { Container } from "@/shared/ui";
+ 
+ import css from "./ads-list-page.module.css";
+ 
+-const pageSize = 10;
+-
+-export const AdsListPage = (): JSX.Element => {
++const AdsListContent = (): JSX.Element => {
+   const {
+     items,
+     total,
+     isLoading,
+     page,
+     searchValue,
+-    categories,
+-    needsRevision,
+     sortValue,
++    viewMode,
+     setPage,
+     search,
+-    changeCategories,
+-    changeNeedsRevision,
+-    resetSearch,
+-    sort
+-  } = useAdsFilters(pageSize);
++    sort,
++    setViewMode
++  } = useFilterContext();
+ 
+   return (
+     <Container>
+@@ -47,13 +42,7 @@ export const AdsListPage = (): JSX.Element => {
+ 
+         <Flex align="stretch" gap={48} className={css.contentWrapper}>
+           <aside className={css.filterWrapper}>
+-            <Filter
+-              selectedCategories={categories}
+-              needsRevision={needsRevision}
+-              onCategoriesChange={changeCategories}
+-              onNeedsRevisionChange={changeNeedsRevision}
+-              onReset={resetSearch}
+-            />
++            <Filter />
+           </aside>
+ 
+           <Flex vertical gap={16} className={css.sectionWrapper}>
+@@ -62,14 +51,16 @@ export const AdsListPage = (): JSX.Element => {
+               onSearch={search}
+               sortValue={sortValue}
+               onSortChange={sort}
++              viewMode={viewMode}
++              onViewModeChange={setViewMode}
+             />
+ 
+-            <AdCardList ads={items} isLoading={isLoading} />
++            <AdCardList ads={items} isLoading={isLoading} viewMode={viewMode} />
+ 
+             <div className={css.pagination}>
+               <PaginationComponent
+                 current={page}
+-                pageSize={pageSize}
++                pageSize={PAGE_SIZE}
+                 total={total}
+                 onChange={setPage}
+               />
+@@ -80,3 +71,11 @@ export const AdsListPage = (): JSX.Element => {
+     </Container>
+   );
+ };
++
++export const AdsListPage = (): JSX.Element => {
++  return (
++    <FilterProvider>
++      <AdsListContent />
++    </FilterProvider>
++  );
++};
+diff --git a/client/src/features/ai/api/generate-response.ts b/client/src/features/ai/api/generate-response.ts
+index 7e86e4c..21ffdb9 100644
+--- a/client/src/features/ai/api/generate-response.ts
++++ b/client/src/features/ai/api/generate-response.ts
+@@ -6,6 +6,9 @@ import type {
+ 
+ import { apiOllama } from "@/shared/api";
+ 
++const AI_TEMPERATURE = 0.1;
++const AI_MAX_TOKENS = 300;
++
+ export const generateResponse = async (
+   payload: GenerateRequestType
+ ): Promise<SimplifiedGenerateResponseType> => {
+@@ -14,8 +17,8 @@ export const generateResponse = async (
+     prompt: payload.prompt,
+     stream: false,
+     options: {
+-      temperature: 0.1,
+-      num_predict: 300
++      temperature: AI_TEMPERATURE,
++      num_predict: AI_MAX_TOKENS
+     }
+   });
+ 
+diff --git a/client/src/features/ai/index.ts b/client/src/features/ai/index.ts
+new file mode 100644
+index 0000000..48aff0b
+--- /dev/null
++++ b/client/src/features/ai/index.ts
+@@ -0,0 +1,4 @@
++export * from "./api";
++export * from "./constants";
++export * from "./hooks";
++export * from "./types";
+diff --git a/client/src/features/ai/types/gerenate.types.ts b/client/src/features/ai/types/generate.types.ts
+similarity index 100%
+rename from client/src/features/ai/types/gerenate.types.ts
+rename to client/src/features/ai/types/generate.types.ts
+diff --git a/client/src/features/ai/types/index.ts b/client/src/features/ai/types/index.ts
+index 9678987..c51eb1d 100644
+--- a/client/src/features/ai/types/index.ts
++++ b/client/src/features/ai/types/index.ts
+@@ -1,2 +1,2 @@
+ export * from "./models.types";
+-export * from "./gerenate.types";
++export * from "./generate.types";
+diff --git a/client/src/shared/api/api-error.ts b/client/src/shared/api/api-error.ts
+new file mode 100644
+index 0000000..bd1fbef
+--- /dev/null
++++ b/client/src/shared/api/api-error.ts
+@@ -0,0 +1,28 @@
++import type { AxiosError } from "axios";
++
++export type ApiErrorResponse = {
++  error?: string | Record<string, string[]>;
++  message?: string;
++};
++
++export const getApiErrorMessage = (
++  error: AxiosError<ApiErrorResponse>,
++  fallback = "Произошла сетевая ошибка"
++): string => {
++  const serverError = error.response?.data?.error;
++
++  if (typeof serverError === "string") {
++    return serverError;
++  }
++
++  if (serverError && typeof serverError === "object") {
++    return "Ошибка валидации полей";
++  }
++
++  const message = error.response?.data?.message;
++  if (typeof message === "string") {
++    return message;
++  }
++
++  return fallback;
++};
+diff --git a/client/src/shared/api/index.ts b/client/src/shared/api/index.ts
+index b1120b9..073cfe6 100644
+--- a/client/src/shared/api/index.ts
++++ b/client/src/shared/api/index.ts
+@@ -1 +1,2 @@
+ export * from "./instance";
++export * from "./api-error";
+diff --git a/client/src/shared/api/instance.ts b/client/src/shared/api/instance.ts
+index 744070a..3d5d14e 100644
+--- a/client/src/shared/api/instance.ts
++++ b/client/src/shared/api/instance.ts
+@@ -1,21 +1,48 @@
+-import axios from "axios";
++import axios, { type AxiosError } from "axios";
+ 
+ import { API_URL, OLLAMA_API_URL } from "@/shared/config";
+ 
++const API_TIMEOUT = 15_000;
++const OLLAMA_TIMEOUT = 60_000;
++const MAX_RETRIES = 2;
++const RETRY_DELAY = 1_000;
++
++const isRetryable = (error: AxiosError): boolean => {
++  if (!error.response) return true;
++  return error.response.status >= 500;
++};
++
++const delay = (ms: number): Promise<void> => new Promise((resolve) => setTimeout(resolve, ms));
++
++const createRetryInterceptor = (maxRetries: number) => {
++  return async (error: AxiosError): Promise<unknown> => {
++    const config = error.config;
++    if (!config) return Promise.reject(error);
++
++    const retryCount = (config as Record<string, unknown>).__retryCount as number ?? 0;
++
++    if (retryCount >= maxRetries || !isRetryable(error)) {
++      return Promise.reject(error);
++    }
++
++    (config as Record<string, unknown>).__retryCount = retryCount + 1;
++    await delay(RETRY_DELAY * (retryCount + 1));
++
++    return axios(config);
++  };
++};
++
+ export const api = axios.create({
+   baseURL: API_URL,
+-  headers: { "Content-Type": "application/json" }
++  headers: { "Content-Type": "application/json" },
++  timeout: API_TIMEOUT
+ });
+ 
+ export const apiOllama = axios.create({
+   baseURL: OLLAMA_API_URL,
+-  headers: { "Content-Type": "application/json" }
++  headers: { "Content-Type": "application/json" },
++  timeout: OLLAMA_TIMEOUT
+ });
+ 
+-const errorHandler = (error: unknown): Promise<unknown> => {
+-  console.error(`API Error: ${error || "Cannot get error message."}`);
+-  return Promise.reject(error);
+-};
+-
+-api.interceptors.response.use((r) => r, errorHandler);
+-apiOllama.interceptors.response.use((r) => r, errorHandler);
++api.interceptors.response.use((r) => r, createRetryInterceptor(MAX_RETRIES));
++apiOllama.interceptors.response.use((r) => r, createRetryInterceptor(0));
+diff --git a/client/src/shared/index.ts b/client/src/shared/index.ts
+new file mode 100644
+index 0000000..b857841
+--- /dev/null
++++ b/client/src/shared/index.ts
+@@ -0,0 +1,6 @@
++export * from "./api";
++export * from "./config";
++export * from "./contexts";
++export * from "./hooks";
++export * from "./lib";
++export * from "./ui";
+diff --git a/client/src/shared/ui/error-boundary/error-boundary.tsx b/client/src/shared/ui/error-boundary/error-boundary.tsx
+new file mode 100644
+index 0000000..b40ad94
+--- /dev/null
++++ b/client/src/shared/ui/error-boundary/error-boundary.tsx
+@@ -0,0 +1,60 @@
++import { Component, type ErrorInfo, type ReactNode } from "react";
++import { Alert, Button, Flex } from "antd";
++
++type ErrorBoundaryProps = {
++  children: ReactNode;
++  fallback?: ReactNode;
++};
++
++type ErrorBoundaryState = {
++  hasError: boolean;
++  error: Error | null;
++};
++
++export class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
++  constructor(props: ErrorBoundaryProps) {
++    super(props);
++    this.state = { hasError: false, error: null };
++  }
++
++  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
++    return { hasError: true, error };
++  }
++
++  componentDidCatch(error: Error, errorInfo: ErrorInfo): void {
++    console.error("ErrorBoundary caught:", error, errorInfo);
++  }
++
++  handleReset = (): void => {
++    this.setState({ hasError: false, error: null });
++  };
++
++  render(): ReactNode {
++    if (this.state.hasError) {
++      if (this.props.fallback) {
++        return this.props.fallback;
++      }
++
++      return (
++        <Flex
++          vertical
++          align="center"
++          justify="center"
++          style={{ padding: 48, minHeight: "50vh" }}>
++          <Alert
++            type="error"
++            showIcon
++            message="Что-то пошло не так"
++            description={this.state.error?.message ?? "Произошла непредвиденная ошибка"}
++            style={{ maxWidth: 600, width: "100%" }}
++          />
++          <Button type="primary" onClick={this.handleReset} style={{ marginTop: 16 }}>
++            Попробовать снова
++          </Button>
++        </Flex>
++      );
++    }
++
++    return this.props.children;
++  }
++}
+diff --git a/client/src/shared/ui/error-boundary/index.ts b/client/src/shared/ui/error-boundary/index.ts
+new file mode 100644
+index 0000000..899c04f
+--- /dev/null
++++ b/client/src/shared/ui/error-boundary/index.ts
+@@ -0,0 +1 @@
++export { ErrorBoundary } from "./error-boundary";
+diff --git a/client/src/shared/ui/index.ts b/client/src/shared/ui/index.ts
+index d28c14e..31259c8 100644
+--- a/client/src/shared/ui/index.ts
++++ b/client/src/shared/ui/index.ts
+@@ -1,2 +1,3 @@
+ export * from "./container";
+ export * from "./ai-generate-button";
++export * from "./error-boundary";
diff --git a/file_name_1.patch b/file_name_1.patch
new file mode 100644
index 0000000..47708dc
--- /dev/null
+++ b/file_name_1.patch
@@ -0,0 +1,3388 @@
+diff --git a/client/src/app/providers/query-provider.tsx b/client/src/app/providers/query-provider.tsx
+index 61b879e..a655ce6 100644
+--- a/client/src/app/providers/query-provider.tsx
++++ b/client/src/app/providers/query-provider.tsx
+@@ -4,10 +4,12 @@ import type { JSX } from "react";
+ import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+ import { ReactQueryDevtools } from "@tanstack/react-query-devtools";
+ 
++const STALE_TIME_MS = 5 * 60 * 1000;
++
+ const queryClient = new QueryClient({
+   defaultOptions: {
+     queries: {
+-      staleTime: 1000 * 60 * 5
++      staleTime: STALE_TIME_MS
+     }
+   }
+ });
+diff --git a/client/src/app/router/index.tsx b/client/src/app/router/index.tsx
+index f22d1e1..a383d85 100644
+--- a/client/src/app/router/index.tsx
++++ b/client/src/app/router/index.tsx
+@@ -6,19 +6,22 @@ import { App } from "@/app/app";
+ import { AdDetailsPage, AdEditPage, AdsListPage } from "@/features/ads/pages";
+ 
+ import { NotFoundPage } from "@/shared/pages/not-found-page/not-found-page";
++import { ErrorBoundary } from "@/shared/ui";
+ 
+ export const Router = (): JSX.Element => {
+   return (
+     <BrowserRouter>
+-      <Routes>
+-        <Route path="/" element={<App />}>
+-          <Route index element={<Navigate to="/ads" replace />} />
+-          <Route path="ads" element={<AdsListPage />} />
+-          <Route path="ads/:id" element={<AdDetailsPage />} />
+-          <Route path="ads/:id/edit" element={<AdEditPage />} />
+-          <Route path="*" element={<NotFoundPage />} />
+-        </Route>
+-      </Routes>
++      <ErrorBoundary>
++        <Routes>
++          <Route path="/" element={<App />}>
++            <Route index element={<Navigate to="/ads" replace />} />
++            <Route path="ads" element={<AdsListPage />} />
++            <Route path="ads/:id" element={<AdDetailsPage />} />
++            <Route path="ads/:id/edit" element={<AdEditPage />} />
++            <Route path="*" element={<NotFoundPage />} />
++          </Route>
++        </Routes>
++      </ErrorBoundary>
+     </BrowserRouter>
+   );
+ };
+diff --git a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.module.css b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.module.css
+index 7cdaecb..aaf4b2e 100644
+--- a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.module.css
++++ b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.module.css
+@@ -2,3 +2,15 @@
+   flex-shrink: 0;
+   width: 340px;
+ }
++
++@media (max-width: 1024px) {
++  .gallery {
++    width: 280px;
++  }
++}
++
++@media (max-width: 768px) {
++  .gallery {
++    width: 100%;
++  }
++}
+diff --git a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
+index d592519..ae576f8 100644
+--- a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
++++ b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
+@@ -5,14 +5,10 @@ import { PlaceholderGallery } from "@/features/ads/components/details/placeholde
+ 
+ import css from "./ad-details-gallery.module.css";
+ 
+-type AdDetailsGalleryProps = {
+-  photos?: string[];
+-};
+-
+-export const AdDetailsGallery = ({ photos }: AdDetailsGalleryProps): JSX.Element => {
++export const AdDetailsGallery = (): JSX.Element => {
+   return (
+     <Flex vertical gap={12} className={css.gallery}>
+-      <PlaceholderGallery photos={photos} />
++      <PlaceholderGallery />
+     </Flex>
+   );
+ };
+diff --git a/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx b/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
+index ab0a4f7..cea8162 100644
+--- a/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
++++ b/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
+@@ -1,5 +1,5 @@
+ import { Descriptions, Typography } from "antd";
+-import type { JSX } from "react";
++import type { JSX, ReactNode } from "react";
+ 
+ import type { AdType } from "@/features/ads/types/ad.types";
+ 
+@@ -9,29 +9,53 @@ type AdSpecificationsProps = {
+   params: AdType["params"];
+ };
+ 
+-type SpecificationFieldConfig = {
+-  key: string;
+-  label: string;
+-  format?: (value: unknown) => string;
+-};
++type SpecEntry = { label: string; value: ReactNode };
++
++const getSpecEntries = (params: AdType["params"]): SpecEntry[] => {
++  const entries: SpecEntry[] = [];
+ 
+-const SPECIFICATION_FIELDS: SpecificationFieldConfig[] = [
+-  { key: "type", label: "Тип" },
+-  { key: "brand", label: "Бренд" },
+-  { key: "model", label: "Модель" },
+-  { key: "color", label: "Цвет" },
+-  { key: "condition", label: "Состояние" },
+-  { key: "address", label: "Адрес" },
+-  { key: "area", label: "Площадь", format: (value) => `${value} м²` },
+-  { key: "floor", label: "Этаж" },
+-  { key: "transmission", label: "Коробка" },
+-  { key: "mileage", label: "Пробег", format: (value) => `${value} км` },
+-  { key: "enginePower", label: "Мощность", format: (value) => `${value} л.с.` },
+-  { key: "yearOfManufacture", label: "Год выпуска" }
+-];
++  if ("type" in params && params.type) {
++    entries.push({ label: "Тип", value: params.type });
++  }
++  if ("brand" in params && params.brand) {
++    entries.push({ label: "Бренд", value: params.brand });
++  }
++  if ("model" in params && params.model) {
++    entries.push({ label: "Модель", value: params.model });
++  }
++  if ("color" in params && params.color) {
++    entries.push({ label: "Цвет", value: params.color });
++  }
++  if ("condition" in params && params.condition) {
++    entries.push({ label: "Состояние", value: params.condition });
++  }
++  if ("address" in params && params.address) {
++    entries.push({ label: "Адрес", value: params.address });
++  }
++  if ("area" in params && params.area) {
++    entries.push({ label: "Площадь", value: `${params.area} м²` });
++  }
++  if ("floor" in params && params.floor) {
++    entries.push({ label: "Этаж", value: params.floor });
++  }
++  if ("transmission" in params && params.transmission) {
++    entries.push({ label: "Коробка", value: params.transmission });
++  }
++  if ("mileage" in params && params.mileage) {
++    entries.push({ label: "Пробег", value: `${params.mileage} км` });
++  }
++  if ("enginePower" in params && params.enginePower) {
++    entries.push({ label: "Мощность", value: `${params.enginePower} л.с.` });
++  }
++  if ("yearOfManufacture" in params && params.yearOfManufacture) {
++    entries.push({ label: "Год выпуска", value: params.yearOfManufacture });
++  }
++
++  return entries;
++};
+ 
+ export const AdSpecifications = ({ params }: AdSpecificationsProps): JSX.Element => {
+-  const paramsRecord = params as Record<string, unknown>;
++  const entries = getSpecEntries(params);
+ 
+   return (
+     <div>
+@@ -39,22 +63,11 @@ export const AdSpecifications = ({ params }: AdSpecificationsProps): JSX.Element
+         Характеристики
+       </Typography.Title>
+       <Descriptions column={1} size="small" className={css.descriptions}>
+-        {SPECIFICATION_FIELDS.map((field) => {
+-          if (!(field.key in paramsRecord)) {
+-            return null;
+-          }
+-
+-          const value = paramsRecord[field.key];
+-          if (!value) {
+-            return null;
+-          }
+-
+-          return (
+-            <Descriptions.Item key={field.key} label={field.label}>
+-              {field.format ? field.format(value) : String(value)}
+-            </Descriptions.Item>
+-          );
+-        })}
++        {entries.map((entry) => (
++          <Descriptions.Item key={entry.label} label={entry.label}>
++            {entry.value}
++          </Descriptions.Item>
++        ))}
+       </Descriptions>
+     </div>
+   );
+diff --git a/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx b/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx
+new file mode 100644
+index 0000000..0485c46
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx
+@@ -0,0 +1,50 @@
++import { type JSX } from "react";
++import { Flex, Form, Input, Typography } from "antd";
++
++import {
++  DESCRIPTION_GEN_LABELS,
++  DESCRIPTION_IMPROVE_LABELS,
++  MAX_DESCRIPTION_LENGTH
++} from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
++
++import { AiGenerateButton } from "@/shared/ui";
++
++type AdEditDescriptionFieldProps = {
++  description: string;
++  isPending: boolean;
++  onGenerateDescription?: (currentText: string) => Promise<string>;
++  onApplyDescription: (text: string) => void;
++};
++
++export const AdEditDescriptionField = ({
++  description,
++  isPending,
++  onGenerateDescription,
++  onApplyDescription
++}: AdEditDescriptionFieldProps): JSX.Element => {
++  const isDescriptionEmpty = description.trim().length === 0;
++
++  return (
++    <Form.Item
++      name="description"
++      label={
++        <Flex align="center" gap={12}>
++          <span>Описание</span>
++          {onGenerateDescription && (
++            <AiGenerateButton
++              onGenerate={() => onGenerateDescription(description)}
++              onApply={onApplyDescription}
++              labels={isDescriptionEmpty ? DESCRIPTION_GEN_LABELS : DESCRIPTION_IMPROVE_LABELS}
++            />
++          )}
++        </Flex>
++      }
++      extra={
++        <Typography.Text type="secondary" style={{ fontSize: 12 }}>
++          {description.length} / {MAX_DESCRIPTION_LENGTH}
++        </Typography.Text>
++      }>
++      <Input.TextArea rows={6} maxLength={MAX_DESCRIPTION_LENGTH} disabled={isPending} />
++    </Form.Item>
++  );
++};
+diff --git a/client/src/features/ads/components/edit/ad-edit-description-field/index.ts b/client/src/features/ads/components/edit/ad-edit-description-field/index.ts
+new file mode 100644
+index 0000000..0f33294
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-description-field/index.ts
+@@ -0,0 +1 @@
++export { AdEditDescriptionField } from "./ad-edit-description-field";
+diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
+similarity index 56%
+rename from client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts
+rename to client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
+index f5f4d7a..037a16a 100644
+--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts
++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
+@@ -29,3 +29,28 @@ export const REAL_ESTATE_TYPE_OPTIONS = [
+   { value: "house", label: "Дом" },
+   { value: "room", label: "Комната" }
+ ];
++
++const DEFAULT_AI_LABELS = {
++  buttonLoading: "Выполняется запрос",
++  buttonRetry: "Повторить запрос",
++  popoverTitle: "Ответ AI:",
++  popoverErrorTitle: "Ошибка запроса",
++  errorMessage: "Не удалось получить оценку. Попробуйте позже.",
++  applyButton: "Применить",
++  closeButton: "Закрыть"
++};
++
++export const PRICE_AI_LABELS = {
++  buttonDefault: "Узнать рыночную цену",
++  ...DEFAULT_AI_LABELS
++};
++
++export const DESCRIPTION_GEN_LABELS = {
++  buttonDefault: "Придумать описание",
++  ...DEFAULT_AI_LABELS
++};
++
++export const DESCRIPTION_IMPROVE_LABELS = {
++  buttonDefault: "Улучшить описание",
++  ...DEFAULT_AI_LABELS
++};
+diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.module.css b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.module.css
+index 6c371a6..5920f95 100644
+--- a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.module.css
++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.module.css
+@@ -17,6 +17,12 @@
+   width: 100% !important; /* внутренний input/select тянется на весь контейнер */
+ }
+ 
++@media (max-width: 768px) {
++  .fieldFixedWidth {
++    max-width: 100%;
++  }
++}
++
+ /* Цена: Input растягивается внутри flex, кнопка фиксированная */
+ .priceInput {
+   flex: 1;
+diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
+index d34f000..5f3832b 100644
+--- a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
+@@ -1,45 +1,18 @@
+ import { type JSX, useCallback, useEffect } from "react";
+-import { Button, Divider, Flex, Form, Input, Select, Typography } from "antd";
++import { Button, Divider, Flex, Form, Input, Select } from "antd";
+ import { useNavigate } from "react-router-dom";
+ 
+ import { useFormDraft } from "@/features/ads/hooks";
+-import { useUpdateAd } from "@/features/ads/hooks/use-replace-ad";
+-import { CATEGORY_OPTIONS, MAX_DESCRIPTION_LENGTH } from "@/features/ads/pages/ad-edit-page";
++import { useUpdateAd } from "@/features/ads/hooks/use-update-ad";
+ import { type AdCategory, type AdType, type AdUpdateType } from "@/features/ads/types";
+ 
+-import { AiGenerateButton } from "@/shared/ui";
+-
++import { AdEditDescriptionField } from "../ad-edit-description-field";
+ import { AdEditParamsFields } from "../ad-edit-params-fields";
++import { AdEditPriceField } from "../ad-edit-price-field";
+ 
+-import css from "./ad-edit-form.module.css";
+-
+-const { Text } = Typography;
+-const { TextArea } = Input;
+-
+-const DEFAULT_AI_LABELS = {
+-  buttonLoading: "Выполняется запрос",
+-  buttonRetry: "Повторить запрос",
+-  popoverTitle: "Ответ AI:",
+-  popoverErrorTitle: "Ошибка запроса",
+-  errorMessage: "Не удалось получить оценку. Попробуйте позже.",
+-  applyButton: "Применить",
+-  closeButton: "Закрыть"
+-};
+-
+-const PRICE_AI_LABELS = {
+-  buttonDefault: "Узнать рыночную цену",
+-  ...DEFAULT_AI_LABELS
+-};
+-
+-const DESCRIPTION_GEN_LABELS = {
+-  buttonDefault: "Придумать описание",
+-  ...DEFAULT_AI_LABELS
+-};
++import { CATEGORY_OPTIONS } from "./ad-edit-form.constants";
+ 
+-const DESCRIPTION_IMPROVE_LABELS = {
+-  buttonDefault: "Улучшить описание",
+-  ...DEFAULT_AI_LABELS
+-};
++import css from "./ad-edit-form.module.css";
+ 
+ type AdEditFormProps = {
+   adId: number;
+@@ -48,12 +21,6 @@ type AdEditFormProps = {
+   onGenerateDescription?: (currentText: string) => Promise<string>;
+ };
+ 
+-const extractPrice = (value: string): string | null => {
+-  const cleaned = value.replace(/\s/g, "");
+-  const match = cleaned.match(/\d+([.,]\d+)?/);
+-  return match ? match[0].replace(",", ".") : null;
+-};
+-
+ export const AdEditForm = ({
+   adId,
+   initialValues,
+@@ -68,7 +35,6 @@ export const AdEditForm = ({
+ 
+   const category = Form.useWatch("category", form);
+   const description = Form.useWatch("description", form) ?? "";
+-  const isDescriptionEmpty = description.trim().length === 0;
+ 
+   useEffect(() => {
+     if (initialValues && !form.isFieldsTouched()) {
+@@ -149,64 +115,30 @@ export const AdEditForm = ({
+ 
+       <Divider className={css.divider} />
+ 
+-      <div className={css.fieldFixedWidth}>
+-        <Text style={{ display: "block", marginBottom: 8 }}>Цена</Text>
+-        <Flex gap={8} align="flex-start">
+-          <Form.Item name="price" noStyle rules={[{ required: true }]}>
+-            <Input
+-              type="number"
+-              allowClear
+-              disabled={isPending}
+-              className={css.priceInput}
+-              placeholder="0"
+-            />
+-          </Form.Item>
+-          {onGeneratePrice && (
+-            <AiGenerateButton
+-              onGenerate={onGeneratePrice}
+-              onTransform={(raw) => extractPrice(raw) ?? ""}
+-              onApply={handleApplyPrice}
+-              labels={PRICE_AI_LABELS}
+-            />
+-          )}
+-        </Flex>
+-      </div>
++      <AdEditPriceField
++        isPending={isPending}
++        onGeneratePrice={onGeneratePrice}
++        onApplyPrice={handleApplyPrice}
++      />
+ 
+       <Divider className={css.divider} />
+ 
+       {category && (
+         <>
+           <div className={css.section}>
+-            <Text strong className={css.sectionTitle}>
+-              Характеристики
+-            </Text>
++            <span className={css.sectionTitle}>Характеристики</span>
+             <AdEditParamsFields category={category as AdCategory} />
+           </div>
+           <Divider className={css.divider} />
+         </>
+       )}
+ 
+-      <Form.Item
+-        name="description"
+-        label={
+-          <Flex align="center" gap={12}>
+-            <span>Описание</span>
+-            {onGenerateDescription && (
+-              <AiGenerateButton
+-                onGenerate={() => onGenerateDescription(description)}
+-                onApply={handleApplyDescription}
+-                labels={isDescriptionEmpty ? DESCRIPTION_GEN_LABELS : DESCRIPTION_IMPROVE_LABELS}
+-              />
+-            )}
+-          </Flex>
+-        }
+-        extra={
+-          <Text type="secondary" style={{ fontSize: 12 }}>
+-            {description.length} / {MAX_DESCRIPTION_LENGTH}
+-          </Text>
+-        }>
+-        <TextArea rows={6} maxLength={MAX_DESCRIPTION_LENGTH} disabled={isPending} />
+-      </Form.Item>
++      <AdEditDescriptionField
++        description={description}
++        isPending={isPending}
++        onGenerateDescription={onGenerateDescription}
++        onApplyDescription={handleApplyDescription}
++      />
+ 
+       <Flex gap={8}>
+         <Button type="primary" htmlType="submit" loading={isPending}>
+diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts
+new file mode 100644
+index 0000000..3fd8ef6
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts
+@@ -0,0 +1,5 @@
++export const extractPrice = (value: string): string | null => {
++  const cleaned = value.replace(/\s/g, "");
++  const match = cleaned.match(/\d+([.,]\d+)?/);
++  return match ? match[0].replace(",", ".") : null;
++};
+diff --git a/client/src/features/ads/components/edit/ad-edit-form/index.ts b/client/src/features/ads/components/edit/ad-edit-form/index.ts
+index 3b1a965..270f1aa 100644
+--- a/client/src/features/ads/components/edit/ad-edit-form/index.ts
++++ b/client/src/features/ads/components/edit/ad-edit-form/index.ts
+@@ -1 +1,3 @@
+ export * from "./ad-edit-form";
++export * from "./ad-edit-form.constants";
++export * from "./ad-edit-form.utils";
+diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
+index fb07ee6..1c72c21 100644
+--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
++++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
+@@ -1,7 +1,7 @@
+ import { type JSX } from "react";
+ 
+ import { ParamsFieldsList } from "@/features/ads/components";
+-import { AUTO_TRANSMISSION_OPTIONS } from "@/features/ads/pages/ad-edit-page";
++import { AUTO_TRANSMISSION_OPTIONS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+ import type { FieldConfig } from "@/features/ads/types";
+ 
+ const FIELDS: FieldConfig[] = [
+diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
+index 5b09fb9..54c7e65 100644
+--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
++++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
+@@ -4,7 +4,7 @@ import { ParamsFieldsList } from "@/features/ads/components";
+ import {
+   ELECTRONICS_CONDITION_OPTIONS,
+   ELECTRONICS_TYPE_OPTIONS
+-} from "@/features/ads/pages/ad-edit-page";
++} from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+ import type { FieldConfig } from "@/features/ads/types";
+ 
+ const FIELDS: FieldConfig[] = [
+diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
+index 0ebba6c..a2c300c 100644
+--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
++++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
+@@ -1,7 +1,7 @@
+ import { type JSX } from "react";
+ 
+ import { ParamsFieldsList } from "@/features/ads/components";
+-import { REAL_ESTATE_TYPE_OPTIONS } from "@/features/ads/pages/ad-edit-page";
++import { REAL_ESTATE_TYPE_OPTIONS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+ import type { FieldConfig } from "@/features/ads/types";
+ 
+ const FIELDS: FieldConfig[] = [
+diff --git a/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx b/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx
+new file mode 100644
+index 0000000..05da6ff
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx
+@@ -0,0 +1,46 @@
++import { type JSX } from "react";
++import { Flex, Form, Input, Typography } from "antd";
++
++import { PRICE_AI_LABELS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
++import { extractPrice } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.utils";
++
++import { AiGenerateButton } from "@/shared/ui";
++
++import css from "../ad-edit-form/ad-edit-form.module.css";
++
++type AdEditPriceFieldProps = {
++  isPending: boolean;
++  onGeneratePrice?: () => Promise<string>;
++  onApplyPrice: (value: string) => void;
++};
++
++export const AdEditPriceField = ({
++  isPending,
++  onGeneratePrice,
++  onApplyPrice
++}: AdEditPriceFieldProps): JSX.Element => {
++  return (
++    <div className={css.fieldFixedWidth}>
++      <Typography.Text style={{ display: "block", marginBottom: 8 }}>Цена</Typography.Text>
++      <Flex gap={8} align="flex-start">
++        <Form.Item name="price" noStyle rules={[{ required: true }]}>
++          <Input
++            type="number"
++            allowClear
++            disabled={isPending}
++            className={css.priceInput}
++            placeholder="0"
++          />
++        </Form.Item>
++        {onGeneratePrice && (
++          <AiGenerateButton
++            onGenerate={onGeneratePrice}
++            onTransform={(raw) => extractPrice(raw) ?? ""}
++            onApply={onApplyPrice}
++            labels={PRICE_AI_LABELS}
++          />
++        )}
++      </Flex>
++    </div>
++  );
++};
+diff --git a/client/src/features/ads/components/edit/ad-edit-price-field/index.ts b/client/src/features/ads/components/edit/ad-edit-price-field/index.ts
+new file mode 100644
+index 0000000..4043f76
+--- /dev/null
++++ b/client/src/features/ads/components/edit/ad-edit-price-field/index.ts
+@@ -0,0 +1 @@
++export { AdEditPriceField } from "./ad-edit-price-field";
+diff --git a/client/src/features/ads/components/edit/index.ts b/client/src/features/ads/components/edit/index.ts
+index 4916e7c..85979ef 100644
+--- a/client/src/features/ads/components/edit/index.ts
++++ b/client/src/features/ads/components/edit/index.ts
+@@ -1,3 +1,5 @@
+ export * from "./ad-edit-form";
++export * from "./ad-edit-description-field";
+ export * from "./ad-edit-params-fields";
++export * from "./ad-edit-price-field";
+ export * from "./params-field-list";
+diff --git a/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css b/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css
+new file mode 100644
+index 0000000..a14060a
+--- /dev/null
++++ b/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css
+@@ -0,0 +1,15 @@
++.fieldFixedWidth {
++  width: 100%;
++  max-width: 456px;
++}
++
++.fieldFixedWidth :global(.ant-input):not(.ant-input-textarea),
++.fieldFixedWidth :global(.ant-select-selector) {
++  width: 100% !important;
++}
++
++@media (max-width: 768px) {
++  .fieldFixedWidth {
++    max-width: 100%;
++  }
++}
+diff --git a/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx b/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
+index a795cfd..ba42782 100644
+--- a/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
++++ b/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
+@@ -3,12 +3,12 @@ import { Form, Input, Select } from "antd";
+ 
+ import type { FieldConfig } from "@/features/ads/types";
+ 
++import css from "./params-field-list.module.css";
++
+ type ParamsFieldsListProps = {
+   fields: FieldConfig[];
+ };
+ 
+-import css from "../ad-edit-form/ad-edit-form.module.css";
+-
+ export const ParamsFieldsList = ({ fields }: ParamsFieldsListProps): JSX.Element => (
+   <>
+     {fields.map((field) => (
+diff --git a/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css b/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
+index c53cc14..7e6415b 100644
+--- a/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
++++ b/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
+@@ -8,10 +8,50 @@
+   list-style: none;
+ }
+ 
++@media (max-width: 1200px) {
++  .list {
++    grid-template-columns: repeat(4, minmax(0, 1fr));
++  }
++}
++
++@media (max-width: 900px) {
++  .list {
++    grid-template-columns: repeat(3, minmax(0, 1fr));
++    gap: 12px;
++  }
++}
++
++@media (max-width: 600px) {
++  .list {
++    grid-template-columns: repeat(2, minmax(0, 1fr));
++    gap: 10px;
++  }
++}
++
++@media (max-width: 360px) {
++  .list {
++    grid-template-columns: 1fr;
++  }
++}
++
+ .list > li {
+   display: flex;
+ }
+ 
++.listView {
++  display: grid;
++  grid-template-columns: 1fr;
++  gap: 12px;
++
++  padding-left: 0;
++
++  list-style: none;
++}
++
++.listView > li {
++  display: flex;
++}
++
+ .card {
+   width: 100%;
+   border-radius: 12px;
+diff --git a/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx b/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
+index cc51c67..9ed21bc 100644
+--- a/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
++++ b/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
+@@ -2,6 +2,8 @@ import { Card, Skeleton } from "antd";
+ import type { JSX } from "react";
+ 
+ import { AdCard } from "@/features/ads/components";
++import type { ViewMode } from "@/features/ads/components/list/toolbar/toolbar";
++import { SKELETON_COUNT } from "@/features/ads/constants";
+ import type { AdCardType } from "@/features/ads/types";
+ 
+ import css from "./ad-card-list.module.css";
+@@ -9,13 +11,16 @@ import css from "./ad-card-list.module.css";
+ type AdCardListProps = {
+   ads: AdCardType[];
+   isLoading: boolean;
++  viewMode: ViewMode;
+ };
+ 
+-export const AdCardList = ({ ads, isLoading }: AdCardListProps): JSX.Element => {
++export const AdCardList = ({ ads, isLoading, viewMode }: AdCardListProps): JSX.Element => {
++  const listClass = viewMode === "list" ? css.listView : css.list;
++
+   if (isLoading) {
+     return (
+-      <ul className={css.list}>
+-        {Array.from({ length: 10 }).map((_, index) => (
++      <ul className={listClass}>
++        {Array.from({ length: SKELETON_COUNT }).map((_, index) => (
+           <Card key={index} className={css.card}>
+             <Skeleton active />
+           </Card>
+@@ -25,7 +30,7 @@ export const AdCardList = ({ ads, isLoading }: AdCardListProps): JSX.Element =>
+   }
+ 
+   return (
+-    <ul className={css.list}>
++    <ul className={listClass}>
+       {ads.map((ad) => (
+         <AdCard key={ad.id} ad={ad} />
+       ))}
+diff --git a/client/src/features/ads/components/list/filter/filter.module.css b/client/src/features/ads/components/list/filter/filter.module.css
+index 1ed3874..3ecaa99 100644
+--- a/client/src/features/ads/components/list/filter/filter.module.css
++++ b/client/src/features/ads/components/list/filter/filter.module.css
+@@ -8,6 +8,18 @@
+   font-family: sans-serif;
+ }
+ 
++@media (max-width: 1024px) {
++  .container {
++    width: 220px;
++  }
++}
++
++@media (max-width: 768px) {
++  .container {
++    width: 100%;
++  }
++}
++
+ .card {
+   padding: 16px;
+   border: none;
+@@ -48,6 +60,18 @@
+   background: var(--ant-color-bg-container) !important;
+ }
+ 
++@media (max-width: 1024px) {
++  .resetButton {
++    width: 220px !important;
++  }
++}
++
++@media (max-width: 768px) {
++  .resetButton {
++    width: 100% !important;
++  }
++}
++
+ .optionsWrapper {
+   display: flex;
+   flex-direction: column;
+diff --git a/client/src/features/ads/components/list/filter/filter.tsx b/client/src/features/ads/components/list/filter/filter.tsx
+index f4fc911..6b94920 100644
+--- a/client/src/features/ads/components/list/filter/filter.tsx
++++ b/client/src/features/ads/components/list/filter/filter.tsx
+@@ -2,25 +2,20 @@ import { type JSX } from "react";
+ import { Button, Collapse, Typography } from "antd";
+ 
+ import { FilterItems } from "@/features/ads/components";
+-import type { AdCategory } from "@/features/ads/types";
++import { useFilterContext } from "@/features/ads/contexts";
+ 
+ import css from "./filter.module.css";
+ 
+-type FilterProps = {
+-  selectedCategories: AdCategory[];
+-  needsRevision: boolean;
+-  onCategoriesChange: (categories: AdCategory[]) => void;
+-  onNeedsRevisionChange: () => void;
+-  onReset: () => void;
+-};
+-export const Filter = ({
+-  selectedCategories,
+-  needsRevision,
+-  onCategoriesChange,
+-  onNeedsRevisionChange,
+-  onReset
+-}: FilterProps): JSX.Element => {
+-  const hasActiveFilters = selectedCategories.length > 0 || needsRevision;
++export const Filter = (): JSX.Element => {
++  const {
++    categories,
++    needsRevision,
++    changeCategories,
++    changeNeedsRevision,
++    resetSearch
++  } = useFilterContext();
++
++  const hasActiveFilters = categories.length > 0 || needsRevision;
+ 
+   return (
+     <div className={css.container}>
+@@ -39,10 +34,10 @@ export const Filter = ({
+               label: <span style={{ fontSize: "16px" }}>Категория</span>,
+               children: (
+                 <FilterItems
+-                  selectedCategories={selectedCategories}
++                  selectedCategories={categories}
+                   needsRevision={needsRevision}
+-                  onCategoriesChange={onCategoriesChange}
+-                  onNeedsRevisionChange={onNeedsRevisionChange}
++                  onCategoriesChange={changeCategories}
++                  onNeedsRevisionChange={changeNeedsRevision}
+                 />
+               )
+             }
+@@ -50,7 +45,7 @@ export const Filter = ({
+         />
+       </div>
+ 
+-      <Button onClick={onReset} disabled={!hasActiveFilters} className={css.resetButton}>
++      <Button onClick={resetSearch} disabled={!hasActiveFilters} className={css.resetButton}>
+         Сбросить фильтры
+       </Button>
+     </div>
+diff --git a/client/src/features/ads/components/list/toolbar/toolbar.module.css b/client/src/features/ads/components/list/toolbar/toolbar.module.css
+new file mode 100644
+index 0000000..7bf2093
+--- /dev/null
++++ b/client/src/features/ads/components/list/toolbar/toolbar.module.css
+@@ -0,0 +1,17 @@
++.toolbar {
++  width: 100%;
++}
++
++.sortSelect {
++  width: 240px;
++}
++
++@media (max-width: 768px) {
++  .toolbar :global(.ant-flex) {
++    flex-wrap: wrap;
++  }
++
++  .sortSelect {
++    width: 100%;
++  }
++}
+diff --git a/client/src/features/ads/components/list/toolbar/toolbar.tsx b/client/src/features/ads/components/list/toolbar/toolbar.tsx
+index dbc9bc7..320be58 100644
+--- a/client/src/features/ads/components/list/toolbar/toolbar.tsx
++++ b/client/src/features/ads/components/list/toolbar/toolbar.tsx
+@@ -4,43 +4,60 @@ import { Button, Card, Flex, Input, Select, Space } from "antd";
+ import type { AdsSortValue } from "@/features/ads/types";
+ 
+ import { ADS_SORT_OPTIONS, DEFAULT_ADS_SORT } from "./toolbar.constants";
++import css from "./toolbar.module.css";
+ 
+ import { AppstoreOutlined, UnorderedListOutlined } from "@ant-design/icons";
+ 
++export type ViewMode = "grid" | "list";
++
+ type ToolbarProps = {
+   defaultSearchValue: string;
+   sortValue: AdsSortValue;
++  viewMode: ViewMode;
+   onSearch: (value: string) => void;
+   onSortChange: (value: AdsSortValue) => void;
++  onViewModeChange: (mode: ViewMode) => void;
+ };
+ export const Toolbar = ({
+   defaultSearchValue,
+   onSearch,
+   sortValue,
+-  onSortChange
++  onSortChange,
++  viewMode,
++  onViewModeChange
+ }: ToolbarProps): JSX.Element => {
+   return (
+-    <Card styles={{ body: { padding: "12px 16px" } }}>
+-      <Flex gap={24} align="center">
++    <Card styles={{ body: { padding: "12px 16px" } }} className={css.toolbar}>
++      <Flex gap={12} align="center" wrap="wrap">
+         <Input.Search
+           defaultValue={defaultSearchValue}
+           onSearch={onSearch}
+           allowClear
+           placeholder="Найти объявление..."
+-          style={{ flex: 1 }}
++          style={{ flex: 1, minWidth: 160 }}
+         />
+ 
+-        <Space>
+-          <Button type="primary" icon={<AppstoreOutlined />} />
+-          <Button type="default" icon={<UnorderedListOutlined />} />
+-        </Space>
+-        <Select
+-          options={ADS_SORT_OPTIONS}
+-          value={sortValue}
+-          defaultValue={DEFAULT_ADS_SORT}
+-          onChange={onSortChange}
+-          style={{ width: 240 }}
+-        />
++        <Flex gap={8} align="center" flex="none">
++          <Space>
++            <Button
++              type={viewMode === "grid" ? "primary" : "default"}
++              icon={<AppstoreOutlined />}
++              onClick={() => onViewModeChange("grid")}
++            />
++            <Button
++              type={viewMode === "list" ? "primary" : "default"}
++              icon={<UnorderedListOutlined />}
++              onClick={() => onViewModeChange("list")}
++            />
++          </Space>
++          <Select
++            options={ADS_SORT_OPTIONS}
++            value={sortValue}
++            defaultValue={DEFAULT_ADS_SORT}
++            onChange={onSortChange}
++            className={css.sortSelect}
++          />
++        </Flex>
+       </Flex>
+     </Card>
+   );
+diff --git a/client/src/features/ads/constants/index.ts b/client/src/features/ads/constants/index.ts
+index c1dadd8..4b16749 100644
+--- a/client/src/features/ads/constants/index.ts
++++ b/client/src/features/ads/constants/index.ts
+@@ -8,3 +8,6 @@ export const ADS_PLURAL_VARIANTS: PluralizeVariants = {
+   many: "объявлений",
+   other: "объявления"
+ };
++
++export const PAGE_SIZE = 10;
++export const SKELETON_COUNT = 10;
+diff --git a/client/src/features/ads/contexts/filter-context.tsx b/client/src/features/ads/contexts/filter-context.tsx
+new file mode 100644
+index 0000000..061a3d3
+--- /dev/null
++++ b/client/src/features/ads/contexts/filter-context.tsx
+@@ -0,0 +1,48 @@
++import { createContext, type ReactNode, useContext, useState, type JSX } from "react";
++
++import { useAdsFilters } from "@/features/ads/hooks/use-ads-filters";
++import type { AdCategory, AdsSortValue } from "@/features/ads/types";
++import type { AdCardType } from "@/features/ads/types/ad.types";
++
++import type { ViewMode } from "@/features/ads/components/list/toolbar/toolbar";
++import { PAGE_SIZE } from "@/features/ads/constants";
++
++type FilterContextType = {
++  items: AdCardType[];
++  total: number;
++  isLoading: boolean;
++  page: number;
++  searchValue: string;
++  categories: AdCategory[];
++  needsRevision: boolean;
++  sortValue: AdsSortValue;
++  viewMode: ViewMode;
++  setPage: (page: number) => void;
++  search: (value: string) => void;
++  changeCategories: (values: AdCategory[]) => void;
++  changeNeedsRevision: () => void;
++  resetSearch: () => void;
++  sort: (value: AdsSortValue) => void;
++  setViewMode: (mode: ViewMode) => void;
++};
++
++const FilterContext = createContext<FilterContextType | null>(null);
++
++export const useFilterContext = (): FilterContextType => {
++  const context = useContext(FilterContext);
++  if (!context) {
++    throw new Error("useFilterContext must be used within FilterProvider");
++  }
++  return context;
++};
++
++export const FilterProvider = ({ children }: { children: ReactNode }): JSX.Element => {
++  const filters = useAdsFilters(PAGE_SIZE);
++  const [viewMode, setViewMode] = useState<ViewMode>("grid");
++
++  return (
++    <FilterContext.Provider value={{ ...filters, viewMode, setViewMode }}>
++      {children}
++    </FilterContext.Provider>
++  );
++};
+diff --git a/client/src/features/ads/contexts/index.ts b/client/src/features/ads/contexts/index.ts
+new file mode 100644
+index 0000000..3a84555
+--- /dev/null
++++ b/client/src/features/ads/contexts/index.ts
+@@ -0,0 +1 @@
++export { FilterProvider, useFilterContext } from "./filter-context";
+diff --git a/client/src/features/ads/hooks/index.ts b/client/src/features/ads/hooks/index.ts
+index 5c68350..c522b85 100644
+--- a/client/src/features/ads/hooks/index.ts
++++ b/client/src/features/ads/hooks/index.ts
+@@ -1,5 +1,6 @@
++export * from "./use-ad-ai-generation";
+ export * from "./use-get-ad";
+ export * from "./use-get-ads";
+ export * from "./use-ads-filters";
+-export * from "./use-replace-ad";
++export * from "./use-update-ad";
+ export * from "./use-form-draft";
+diff --git a/client/src/features/ads/hooks/use-ad-ai-generation.ts b/client/src/features/ads/hooks/use-ad-ai-generation.ts
+new file mode 100644
+index 0000000..7bf4e0e
+--- /dev/null
++++ b/client/src/features/ads/hooks/use-ad-ai-generation.ts
+@@ -0,0 +1,44 @@
++import type { AdType } from "@/features/ads/types";
++import {
++  DESCRIPTION_GENERATION_INSTRUCTION,
++  DESCRIPTION_IMPROVEMENT_INSTRUCTION,
++  PRICE_INSTRUCTION
++} from "@/features/ai/constants";
++import { useGenerateResponse, useGetModels } from "@/features/ai/hooks";
++
++type UseAdAiGenerationReturnType = {
++  isAiReady: boolean;
++  generatePrice: () => Promise<string>;
++  generateDescription: (currentDescription: string) => Promise<string>;
++};
++
++export const useAdAiGeneration = (ad?: AdType): UseAdAiGenerationReturnType => {
++  const { weakestModel, middleModel, strongestModel, isLoading } = useGetModels();
++  const { mutateAsync } = useGenerateResponse();
++
++  const activeModel = middleModel || weakestModel || strongestModel;
++  const isAiReady = !isLoading && !!activeModel;
++
++  const generatePrice = async (): Promise<string> => {
++    if (!activeModel || !ad) throw new Error("Модель недоступна");
++
++    const prompt = `${PRICE_INSTRUCTION}\n\nТовар: ${ad.title}, Категория: ${ad.category}, Параметры: ${JSON.stringify(ad.params)}\n\nОТВЕТЬ НА РУССКОМ.`;
++    return mutateAsync({ model: activeModel, prompt });
++  };
++
++  const generateDescription = async (currentDescription: string): Promise<string> => {
++    if (!activeModel || !ad) throw new Error("Модель недоступна");
++
++    const hasText = currentDescription.trim().length > 0;
++    const instruction = hasText
++      ? DESCRIPTION_IMPROVEMENT_INSTRUCTION
++      : DESCRIPTION_GENERATION_INSTRUCTION;
++
++    const context = `Товар: ${ad.title}, Характеристики: ${JSON.stringify(ad.params)}${hasText ? `\nТекущий текст для улучшения: ${currentDescription}` : ""}`;
++    const prompt = `${instruction}\n\n${context}\n\nВАЖНО: Пиши на русском языке.`;
++
++    return mutateAsync({ model: activeModel, prompt });
++  };
++
++  return { isAiReady, generatePrice, generateDescription };
++};
+diff --git a/client/src/features/ads/hooks/use-form-draft.ts b/client/src/features/ads/hooks/use-form-draft.ts
+index bfacb5a..212cb27 100644
+--- a/client/src/features/ads/hooks/use-form-draft.ts
++++ b/client/src/features/ads/hooks/use-form-draft.ts
+@@ -1,13 +1,45 @@
+ import { useEffect } from "react";
+ import type { FormInstance } from "antd";
+ 
+-import { type AdUpdateType } from "../types";
++import { type AdCategory, type AdUpdateType } from "../types";
+ 
+ type UseFormDraftReturnType = {
+   saveDraft: () => void;
+   clearDraft: () => void;
+ };
+ 
++const VALID_CATEGORIES: AdCategory[] = ["auto", "real_estate", "electronics"];
++
++const isValidDraft = (data: unknown): data is Partial<AdUpdateType> => {
++  if (typeof data !== "object" || data === null || Array.isArray(data)) {
++    return false;
++  }
++
++  const draft = data as Record<string, unknown>;
++
++  if (draft.category !== undefined && !VALID_CATEGORIES.includes(draft.category as AdCategory)) {
++    return false;
++  }
++
++  if (draft.title !== undefined && typeof draft.title !== "string") {
++    return false;
++  }
++
++  if (draft.price !== undefined && typeof draft.price !== "number") {
++    return false;
++  }
++
++  if (draft.description !== undefined && typeof draft.description !== "string") {
++    return false;
++  }
++
++  if (draft.params !== undefined && (typeof draft.params !== "object" || draft.params === null)) {
++    return false;
++  }
++
++  return true;
++};
++
+ export const useFormDraft = (
+   id: string | undefined,
+   form: FormInstance<AdUpdateType>
+@@ -19,10 +51,16 @@ export const useFormDraft = (
+     const savedDraft = localStorage.getItem(storageKey);
+     if (savedDraft) {
+       try {
+-        const parsed = JSON.parse(savedDraft);
+-        setTimeout(() => form.setFieldsValue(parsed), 0);
++        const parsed: unknown = JSON.parse(savedDraft);
++        if (isValidDraft(parsed)) {
++          setTimeout(() => form.setFieldsValue(parsed), 0);
++        } else {
++          console.warn("Черновик повреждён, удаляем:", storageKey);
++          localStorage.removeItem(storageKey);
++        }
+       } catch (e) {
+         console.error("Ошибка парсинга черновика", e);
++        localStorage.removeItem(storageKey);
+       }
+     }
+   }, [id, form, storageKey]);
+diff --git a/client/src/features/ads/hooks/use-replace-ad.ts b/client/src/features/ads/hooks/use-update-ad.ts
+similarity index 58%
+rename from client/src/features/ads/hooks/use-replace-ad.ts
+rename to client/src/features/ads/hooks/use-update-ad.ts
+index d3b651a..547d8ee 100644
+--- a/client/src/features/ads/hooks/use-replace-ad.ts
++++ b/client/src/features/ads/hooks/use-update-ad.ts
+@@ -7,14 +7,16 @@ import { replaceAd, type UpdateAdResponseType } from "../api/replace-ad";
+ 
+ import { useMutation, type UseMutationResult, useQueryClient } from "@tanstack/react-query";
+ 
++import { type ApiErrorResponse, getApiErrorMessage } from "@/shared/api";
++
+ export const useUpdateAd = (
+   id: number
+-): UseMutationResult<UpdateAdResponseType, AxiosError<UpdateAdResponseType>, AdUpdateType> => {
++): UseMutationResult<UpdateAdResponseType, AxiosError<ApiErrorResponse>, AdUpdateType> => {
+   const queryClient = useQueryClient();
+ 
+   const { message } = App.useApp();
+ 
+-  return useMutation<UpdateAdResponseType, AxiosError<UpdateAdResponseType>, AdUpdateType>({
++  return useMutation<UpdateAdResponseType, AxiosError<ApiErrorResponse>, AdUpdateType>({
+     mutationFn: (data: AdUpdateType) => replaceAd(id, data),
+ 
+     onSuccess: (data) => {
+@@ -26,15 +28,7 @@ export const useUpdateAd = (
+     },
+ 
+     onError: (error) => {
+-      const serverError = error.response?.data?.error;
+-
+-      if (typeof serverError === "string") {
+-        void message.error(serverError);
+-      } else if (serverError && typeof serverError === "object") {
+-        void message.error("Ошибка валидации полей");
+-      } else {
+-        void message.error("Произошла сетевая ошибка");
+-      }
++      void message.error(getApiErrorMessage(error));
+     }
+   });
+ };
+diff --git a/client/src/features/ads/index.ts b/client/src/features/ads/index.ts
+new file mode 100644
+index 0000000..2331d4f
+--- /dev/null
++++ b/client/src/features/ads/index.ts
+@@ -0,0 +1,8 @@
++export * from "./api";
++export * from "./components";
++export * from "./constants";
++export * from "./contexts";
++export * from "./hooks";
++export * from "./lib";
++export * from "./types";
++export * from "./utils";
+diff --git a/client/src/features/ads/lib/get-missing-fields.ts b/client/src/features/ads/lib/get-missing-fields.ts
+index d53cbdb..df45aa6 100644
+--- a/client/src/features/ads/lib/get-missing-fields.ts
++++ b/client/src/features/ads/lib/get-missing-fields.ts
+@@ -1,18 +1,26 @@
+ import type { AdResponse } from "@/features/ads/api/get-ad";
+ import { FIELD_LABELS, REQUIRED_FIELDS_BY_CATEGORY } from "@/features/ads/lib/labels";
+ 
++const getParamValue = (params: AdResponse["params"], key: string): unknown => {
++  if (key in params) {
++    return (params as Record<string, unknown>)[key];
++  }
++  return undefined;
++};
++
+ export const getMissingFields = (ad: AdResponse): string[] => {
+   const missing: string[] = [];
+   const missingSet = new Set<string>();
+-  const paramsRecord = ad.params as Record<string, unknown>;
+ 
+   if (!ad.description) {
+     missing.push("Описание");
+     missingSet.add("Описание");
+   }
+ 
+-  REQUIRED_FIELDS_BY_CATEGORY[ad.category].forEach((key) => {
+-    const value = paramsRecord[key];
++  const requiredFields = REQUIRED_FIELDS_BY_CATEGORY[ad.category];
++
++  requiredFields.forEach((key) => {
++    const value = getParamValue(ad.params, key);
+ 
+     if (!value) {
+       const fieldName = FIELD_LABELS[key] ?? key;
+@@ -24,15 +32,18 @@ export const getMissingFields = (ad: AdResponse): string[] => {
+     }
+   });
+ 
+-  Object.entries(paramsRecord).forEach(([key, value]) => {
+-    const isKnownCategoryField = REQUIRED_FIELDS_BY_CATEGORY[ad.category].includes(key);
+-
+-    if (!isKnownCategoryField && !value) {
+-      const fieldName = FIELD_LABELS[key] ?? key;
+-
+-      if (!missingSet.has(fieldName)) {
+-        missing.push(fieldName);
+-        missingSet.add(fieldName);
++  const paramKeys = Object.keys(ad.params);
++  paramKeys.forEach((key) => {
++    const isRequired = requiredFields.includes(key);
++    if (!isRequired) {
++      const value = getParamValue(ad.params, key);
++      if (!value) {
++        const fieldName = FIELD_LABELS[key] ?? key;
++
++        if (!missingSet.has(fieldName)) {
++          missing.push(fieldName);
++          missingSet.add(fieldName);
++        }
+       }
+     }
+   });
+diff --git a/client/src/features/ads/pages/ad-details-page/ad-details-page.module.css b/client/src/features/ads/pages/ad-details-page/ad-details-page.module.css
+index 7ebdc8d..21ee27a 100644
+--- a/client/src/features/ads/pages/ad-details-page/ad-details-page.module.css
++++ b/client/src/features/ads/pages/ad-details-page/ad-details-page.module.css
+@@ -22,3 +22,24 @@
+   flex: 1;
+   min-width: 0;
+ }
++
++@media (max-width: 768px) {
++  .page {
++    padding: 16px 0;
++  }
++
++  .container {
++    padding: 0 16px;
++    gap: 16px;
++  }
++
++  .content {
++    flex-direction: column !important;
++  }
++}
++
++@media (max-width: 480px) {
++  .container {
++    padding: 0 12px;
++  }
++}
+diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css
+deleted file mode 100644
+index 34f14c9..0000000
+--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css
++++ /dev/null
+@@ -1,33 +0,0 @@
+-.aiPopover :global(.ant-popover-inner) {
+-  min-width: 320px;
+-  padding: 16px;
+-  border-radius: 12px;
+-  background: white;
+-}
+-
+-.aiContent {
+-  display: flex;
+-  flex-direction: column;
+-  gap: 12px;
+-}
+-
+-.aiTitle {
+-  font-size: 16px;
+-  font-weight: 600;
+-}
+-
+-.aiText {
+-  font-size: 14px;
+-  line-height: 1.5;
+-  white-space: pre-line;
+-}
+-
+-.aiActions {
+-  display: flex;
+-  gap: 8px;
+-}
+-
+-.highlight {
+-  border-color: #52c41a !important;
+-  box-shadow: 0 0 0 2px rgb(82 196 26 / 20%);
+-}
+diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
+index 43db414..7b5c3d5 100644
+--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
++++ b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
+@@ -4,54 +4,25 @@ import { useParams } from "react-router-dom";
+ 
+ import { AdEditForm } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form";
+ import { AdEditFormSkeleton } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form-skeleton";
++import { useAdAiGeneration } from "@/features/ads/hooks/use-ad-ai-generation";
+ import { useGetAd } from "@/features/ads/hooks";
+-import {
+-  DESCRIPTION_GENERATION_INSTRUCTION,
+-  DESCRIPTION_IMPROVEMENT_INSTRUCTION,
+-  PRICE_INSTRUCTION
+-} from "@/features/ai/constants";
+-import { useGenerateResponse, useGetModels } from "@/features/ai/hooks";
+ 
+ import { Container } from "@/shared/ui";
+ 
+-const { Title } = Typography;
+-
+ export const AdEditPage = (): JSX.Element => {
+   const { id } = useParams<{ id: string }>();
+   const adId = Number(id);
+ 
+-  const { ad, isLoading: isAdLoading, isError, errorMessage } = useGetAd(adId);
+-  const { weakestModel, middleModel, strongestModel, isLoading: isModelsLoading } = useGetModels();
+-  const { mutateAsync } = useGenerateResponse();
+-
+-  const activeModel = middleModel || weakestModel || strongestModel;
+-
+-  const handleGeneratePrice = async (): Promise<string> => {
+-    if (!activeModel || !ad) throw new Error("Модель недоступна");
+-    const prompt = `${PRICE_INSTRUCTION}\n\nТовар: ${ad.title}, Категория: ${ad.category}, Параметры: ${JSON.stringify(ad.params)}\n\nОТВЕТЬ НА РУССКОМ.`;
+-    return mutateAsync({ model: activeModel, prompt });
+-  };
+-
+-  const handleGenerateDescription = async (currentDescription: string): Promise<string> => {
+-    if (!activeModel || !ad) throw new Error("Модель недоступна");
+-    const hasText = currentDescription.trim().length > 0;
+-    const instruction = hasText
+-      ? DESCRIPTION_IMPROVEMENT_INSTRUCTION
+-      : DESCRIPTION_GENERATION_INSTRUCTION;
+-
+-    const context = `Товар: ${ad.title}, Характеристики: ${JSON.stringify(ad.params)}${hasText ? `\nТекущий текст для улучшения: ${currentDescription}` : ""}`;
+-    const prompt = `${instruction}\n\n${context}\n\nВАЖНО: Пиши на русском языке.`;
+-
+-    return mutateAsync({ model: activeModel, prompt });
+-  };
++  const { ad, isLoading, isError, errorMessage } = useGetAd(adId);
++  const { isAiReady, generatePrice, generateDescription } = useAdAiGeneration(ad);
+ 
+   return (
+     <Container maxWidth={942}>
+-      <Title level={3} style={{ marginBottom: 24 }}>
++      <Typography.Title level={3} style={{ marginBottom: 24 }}>
+         Редактирование объявления
+-      </Title>
++      </Typography.Title>
+ 
+-      {isAdLoading ? (
++      {isLoading ? (
+         <AdEditFormSkeleton />
+       ) : isError || !ad || Number.isNaN(adId) ? (
+         <Alert
+@@ -64,8 +35,8 @@ export const AdEditPage = (): JSX.Element => {
+         <AdEditForm
+           adId={adId}
+           initialValues={ad}
+-          onGeneratePrice={isModelsLoading ? undefined : handleGeneratePrice}
+-          onGenerateDescription={isModelsLoading ? undefined : handleGenerateDescription}
++          onGeneratePrice={isAiReady ? generatePrice : undefined}
++          onGenerateDescription={isAiReady ? generateDescription : undefined}
+         />
+       )}
+     </Container>
+diff --git a/client/src/features/ads/pages/ad-edit-page/index.ts b/client/src/features/ads/pages/ad-edit-page/index.ts
+index c98140b..22d2266 100644
+--- a/client/src/features/ads/pages/ad-edit-page/index.ts
++++ b/client/src/features/ads/pages/ad-edit-page/index.ts
+@@ -1,2 +1 @@
+ export * from "./ad-edit-page";
+-export * from "./ad-edit-page.constants";
+diff --git a/client/src/features/ads/pages/ads-list-page/ads-list-page.module.css b/client/src/features/ads/pages/ads-list-page/ads-list-page.module.css
+index fcce374..8150cce 100644
+--- a/client/src/features/ads/pages/ads-list-page/ads-list-page.module.css
++++ b/client/src/features/ads/pages/ads-list-page/ads-list-page.module.css
+@@ -20,3 +20,16 @@
+ .pagination {
+   margin-top: auto;
+ }
++
++@media (max-width: 1024px) {
++  .filterWrapper {
++    flex: 0 0 220px;
++  }
++}
++
++@media (max-width: 768px) {
++  .filterWrapper {
++    flex: 0 0 100%;
++    width: 100%;
++  }
++}
+diff --git a/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx b/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
+index a1272b1..8736049 100644
+--- a/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
++++ b/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
+@@ -3,33 +3,28 @@ import { Flex, Space, Typography } from "antd";
+ 
+ import { Filter, PaginationComponent, Toolbar } from "@/features/ads/components";
+ import { AdCardList } from "@/features/ads/components/list/ad-card-list";
+-import { ADS_PLURAL_VARIANTS } from "@/features/ads/constants";
+-import { useAdsFilters } from "@/features/ads/hooks";
++import { ADS_PLURAL_VARIANTS, PAGE_SIZE } from "@/features/ads/constants";
++import { FilterProvider, useFilterContext } from "@/features/ads/contexts";
+ import { formatAdsCount } from "@/features/ads/utils";
+ 
+ import { Container } from "@/shared/ui";
+ 
+ import css from "./ads-list-page.module.css";
+ 
+-const pageSize = 10;
+-
+-export const AdsListPage = (): JSX.Element => {
++const AdsListContent = (): JSX.Element => {
+   const {
+     items,
+     total,
+     isLoading,
+     page,
+     searchValue,
+-    categories,
+-    needsRevision,
+     sortValue,
++    viewMode,
+     setPage,
+     search,
+-    changeCategories,
+-    changeNeedsRevision,
+-    resetSearch,
+-    sort
+-  } = useAdsFilters(pageSize);
++    sort,
++    setViewMode
++  } = useFilterContext();
+ 
+   return (
+     <Container>
+@@ -45,15 +40,9 @@ export const AdsListPage = (): JSX.Element => {
+           )}
+         </Space>
+ 
+-        <Flex align="stretch" gap={48} className={css.contentWrapper}>
++        <Flex align="stretch" gap={32} wrap="wrap" className={css.contentWrapper}>
+           <aside className={css.filterWrapper}>
+-            <Filter
+-              selectedCategories={categories}
+-              needsRevision={needsRevision}
+-              onCategoriesChange={changeCategories}
+-              onNeedsRevisionChange={changeNeedsRevision}
+-              onReset={resetSearch}
+-            />
++            <Filter />
+           </aside>
+ 
+           <Flex vertical gap={16} className={css.sectionWrapper}>
+@@ -62,14 +51,16 @@ export const AdsListPage = (): JSX.Element => {
+               onSearch={search}
+               sortValue={sortValue}
+               onSortChange={sort}
++              viewMode={viewMode}
++              onViewModeChange={setViewMode}
+             />
+ 
+-            <AdCardList ads={items} isLoading={isLoading} />
++            <AdCardList ads={items} isLoading={isLoading} viewMode={viewMode} />
+ 
+             <div className={css.pagination}>
+               <PaginationComponent
+                 current={page}
+-                pageSize={pageSize}
++                pageSize={PAGE_SIZE}
+                 total={total}
+                 onChange={setPage}
+               />
+@@ -80,3 +71,11 @@ export const AdsListPage = (): JSX.Element => {
+     </Container>
+   );
+ };
++
++export const AdsListPage = (): JSX.Element => {
++  return (
++    <FilterProvider>
++      <AdsListContent />
++    </FilterProvider>
++  );
++};
+diff --git a/client/src/features/ai/api/generate-response.ts b/client/src/features/ai/api/generate-response.ts
+index 7e86e4c..21ffdb9 100644
+--- a/client/src/features/ai/api/generate-response.ts
++++ b/client/src/features/ai/api/generate-response.ts
+@@ -6,6 +6,9 @@ import type {
+ 
+ import { apiOllama } from "@/shared/api";
+ 
++const AI_TEMPERATURE = 0.1;
++const AI_MAX_TOKENS = 300;
++
+ export const generateResponse = async (
+   payload: GenerateRequestType
+ ): Promise<SimplifiedGenerateResponseType> => {
+@@ -14,8 +17,8 @@ export const generateResponse = async (
+     prompt: payload.prompt,
+     stream: false,
+     options: {
+-      temperature: 0.1,
+-      num_predict: 300
++      temperature: AI_TEMPERATURE,
++      num_predict: AI_MAX_TOKENS
+     }
+   });
+ 
+diff --git a/client/src/features/ai/index.ts b/client/src/features/ai/index.ts
+new file mode 100644
+index 0000000..48aff0b
+--- /dev/null
++++ b/client/src/features/ai/index.ts
+@@ -0,0 +1,4 @@
++export * from "./api";
++export * from "./constants";
++export * from "./hooks";
++export * from "./types";
+diff --git a/client/src/features/ai/types/gerenate.types.ts b/client/src/features/ai/types/generate.types.ts
+similarity index 100%
+rename from client/src/features/ai/types/gerenate.types.ts
+rename to client/src/features/ai/types/generate.types.ts
+diff --git a/client/src/features/ai/types/index.ts b/client/src/features/ai/types/index.ts
+index 9678987..c51eb1d 100644
+--- a/client/src/features/ai/types/index.ts
++++ b/client/src/features/ai/types/index.ts
+@@ -1,2 +1,2 @@
+ export * from "./models.types";
+-export * from "./gerenate.types";
++export * from "./generate.types";
+diff --git a/client/src/shared/api/api-error.ts b/client/src/shared/api/api-error.ts
+new file mode 100644
+index 0000000..bd1fbef
+--- /dev/null
++++ b/client/src/shared/api/api-error.ts
+@@ -0,0 +1,28 @@
++import type { AxiosError } from "axios";
++
++export type ApiErrorResponse = {
++  error?: string | Record<string, string[]>;
++  message?: string;
++};
++
++export const getApiErrorMessage = (
++  error: AxiosError<ApiErrorResponse>,
++  fallback = "Произошла сетевая ошибка"
++): string => {
++  const serverError = error.response?.data?.error;
++
++  if (typeof serverError === "string") {
++    return serverError;
++  }
++
++  if (serverError && typeof serverError === "object") {
++    return "Ошибка валидации полей";
++  }
++
++  const message = error.response?.data?.message;
++  if (typeof message === "string") {
++    return message;
++  }
++
++  return fallback;
++};
+diff --git a/client/src/shared/api/index.ts b/client/src/shared/api/index.ts
+index b1120b9..073cfe6 100644
+--- a/client/src/shared/api/index.ts
++++ b/client/src/shared/api/index.ts
+@@ -1 +1,2 @@
+ export * from "./instance";
++export * from "./api-error";
+diff --git a/client/src/shared/api/instance.ts b/client/src/shared/api/instance.ts
+index 744070a..3d5d14e 100644
+--- a/client/src/shared/api/instance.ts
++++ b/client/src/shared/api/instance.ts
+@@ -1,21 +1,48 @@
+-import axios from "axios";
++import axios, { type AxiosError } from "axios";
+ 
+ import { API_URL, OLLAMA_API_URL } from "@/shared/config";
+ 
++const API_TIMEOUT = 15_000;
++const OLLAMA_TIMEOUT = 60_000;
++const MAX_RETRIES = 2;
++const RETRY_DELAY = 1_000;
++
++const isRetryable = (error: AxiosError): boolean => {
++  if (!error.response) return true;
++  return error.response.status >= 500;
++};
++
++const delay = (ms: number): Promise<void> => new Promise((resolve) => setTimeout(resolve, ms));
++
++const createRetryInterceptor = (maxRetries: number) => {
++  return async (error: AxiosError): Promise<unknown> => {
++    const config = error.config;
++    if (!config) return Promise.reject(error);
++
++    const retryCount = (config as Record<string, unknown>).__retryCount as number ?? 0;
++
++    if (retryCount >= maxRetries || !isRetryable(error)) {
++      return Promise.reject(error);
++    }
++
++    (config as Record<string, unknown>).__retryCount = retryCount + 1;
++    await delay(RETRY_DELAY * (retryCount + 1));
++
++    return axios(config);
++  };
++};
++
+ export const api = axios.create({
+   baseURL: API_URL,
+-  headers: { "Content-Type": "application/json" }
++  headers: { "Content-Type": "application/json" },
++  timeout: API_TIMEOUT
+ });
+ 
+ export const apiOllama = axios.create({
+   baseURL: OLLAMA_API_URL,
+-  headers: { "Content-Type": "application/json" }
++  headers: { "Content-Type": "application/json" },
++  timeout: OLLAMA_TIMEOUT
+ });
+ 
+-const errorHandler = (error: unknown): Promise<unknown> => {
+-  console.error(`API Error: ${error || "Cannot get error message."}`);
+-  return Promise.reject(error);
+-};
+-
+-api.interceptors.response.use((r) => r, errorHandler);
+-apiOllama.interceptors.response.use((r) => r, errorHandler);
++api.interceptors.response.use((r) => r, createRetryInterceptor(MAX_RETRIES));
++apiOllama.interceptors.response.use((r) => r, createRetryInterceptor(0));
+diff --git a/client/src/shared/index.ts b/client/src/shared/index.ts
+new file mode 100644
+index 0000000..b857841
+--- /dev/null
++++ b/client/src/shared/index.ts
+@@ -0,0 +1,6 @@
++export * from "./api";
++export * from "./config";
++export * from "./contexts";
++export * from "./hooks";
++export * from "./lib";
++export * from "./ui";
+diff --git a/client/src/shared/pages/not-found-page/not-found-page.module.css b/client/src/shared/pages/not-found-page/not-found-page.module.css
+index d5797dd..0303c5b 100644
+--- a/client/src/shared/pages/not-found-page/not-found-page.module.css
++++ b/client/src/shared/pages/not-found-page/not-found-page.module.css
+@@ -6,3 +6,10 @@
+ .title {
+   margin: 0;
+ }
++
++@media (max-width: 768px) {
++  .wrapper {
++    gap: 24px;
++    padding: 24px 16px;
++  }
++}
+diff --git a/client/src/shared/ui/container/container.module.css b/client/src/shared/ui/container/container.module.css
+index 0d1928d..07bca26 100644
+--- a/client/src/shared/ui/container/container.module.css
++++ b/client/src/shared/ui/container/container.module.css
+@@ -2,4 +2,17 @@
+   width: 100%;
+   max-width: 1280px;
+   margin: 0 auto;
++  padding: 24px 24px;
++}
++
++@media (max-width: 768px) {
++  .container {
++    padding: 16px 16px;
++  }
++}
++
++@media (max-width: 480px) {
++  .container {
++    padding: 12px 12px;
++  }
+ }
+diff --git a/client/src/shared/ui/error-boundary/error-boundary.tsx b/client/src/shared/ui/error-boundary/error-boundary.tsx
+new file mode 100644
+index 0000000..b40ad94
+--- /dev/null
++++ b/client/src/shared/ui/error-boundary/error-boundary.tsx
+@@ -0,0 +1,60 @@
++import { Component, type ErrorInfo, type ReactNode } from "react";
++import { Alert, Button, Flex } from "antd";
++
++type ErrorBoundaryProps = {
++  children: ReactNode;
++  fallback?: ReactNode;
++};
++
++type ErrorBoundaryState = {
++  hasError: boolean;
++  error: Error | null;
++};
++
++export class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
++  constructor(props: ErrorBoundaryProps) {
++    super(props);
++    this.state = { hasError: false, error: null };
++  }
++
++  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
++    return { hasError: true, error };
++  }
++
++  componentDidCatch(error: Error, errorInfo: ErrorInfo): void {
++    console.error("ErrorBoundary caught:", error, errorInfo);
++  }
++
++  handleReset = (): void => {
++    this.setState({ hasError: false, error: null });
++  };
++
++  render(): ReactNode {
++    if (this.state.hasError) {
++      if (this.props.fallback) {
++        return this.props.fallback;
++      }
++
++      return (
++        <Flex
++          vertical
++          align="center"
++          justify="center"
++          style={{ padding: 48, minHeight: "50vh" }}>
++          <Alert
++            type="error"
++            showIcon
++            message="Что-то пошло не так"
++            description={this.state.error?.message ?? "Произошла непредвиденная ошибка"}
++            style={{ maxWidth: 600, width: "100%" }}
++          />
++          <Button type="primary" onClick={this.handleReset} style={{ marginTop: 16 }}>
++            Попробовать снова
++          </Button>
++        </Flex>
++      );
++    }
++
++    return this.props.children;
++  }
++}
+diff --git a/client/src/shared/ui/error-boundary/index.ts b/client/src/shared/ui/error-boundary/index.ts
+new file mode 100644
+index 0000000..899c04f
+--- /dev/null
++++ b/client/src/shared/ui/error-boundary/index.ts
+@@ -0,0 +1 @@
++export { ErrorBoundary } from "./error-boundary";
+diff --git a/client/src/shared/ui/index.ts b/client/src/shared/ui/index.ts
+index d28c14e..31259c8 100644
+--- a/client/src/shared/ui/index.ts
++++ b/client/src/shared/ui/index.ts
+@@ -1,2 +1,3 @@
+ export * from "./container";
+ export * from "./ai-generate-button";
++export * from "./error-boundary";
+diff --git a/file_name.patch b/file_name.patch
+new file mode 100644
+index 0000000..5e94756
+--- /dev/null
++++ b/file_name.patch
+@@ -0,0 +1,1561 @@
++diff --git a/client/src/app/providers/query-provider.tsx b/client/src/app/providers/query-provider.tsx
++index 61b879e..a655ce6 100644
++--- a/client/src/app/providers/query-provider.tsx
+++++ b/client/src/app/providers/query-provider.tsx
++@@ -4,10 +4,12 @@ import type { JSX } from "react";
++ import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
++ import { ReactQueryDevtools } from "@tanstack/react-query-devtools";
++ 
+++const STALE_TIME_MS = 5 * 60 * 1000;
+++
++ const queryClient = new QueryClient({
++   defaultOptions: {
++     queries: {
++-      staleTime: 1000 * 60 * 5
+++      staleTime: STALE_TIME_MS
++     }
++   }
++ });
++diff --git a/client/src/app/router/index.tsx b/client/src/app/router/index.tsx
++index f22d1e1..a383d85 100644
++--- a/client/src/app/router/index.tsx
+++++ b/client/src/app/router/index.tsx
++@@ -6,19 +6,22 @@ import { App } from "@/app/app";
++ import { AdDetailsPage, AdEditPage, AdsListPage } from "@/features/ads/pages";
++ 
++ import { NotFoundPage } from "@/shared/pages/not-found-page/not-found-page";
+++import { ErrorBoundary } from "@/shared/ui";
++ 
++ export const Router = (): JSX.Element => {
++   return (
++     <BrowserRouter>
++-      <Routes>
++-        <Route path="/" element={<App />}>
++-          <Route index element={<Navigate to="/ads" replace />} />
++-          <Route path="ads" element={<AdsListPage />} />
++-          <Route path="ads/:id" element={<AdDetailsPage />} />
++-          <Route path="ads/:id/edit" element={<AdEditPage />} />
++-          <Route path="*" element={<NotFoundPage />} />
++-        </Route>
++-      </Routes>
+++      <ErrorBoundary>
+++        <Routes>
+++          <Route path="/" element={<App />}>
+++            <Route index element={<Navigate to="/ads" replace />} />
+++            <Route path="ads" element={<AdsListPage />} />
+++            <Route path="ads/:id" element={<AdDetailsPage />} />
+++            <Route path="ads/:id/edit" element={<AdEditPage />} />
+++            <Route path="*" element={<NotFoundPage />} />
+++          </Route>
+++        </Routes>
+++      </ErrorBoundary>
++     </BrowserRouter>
++   );
++ };
++diff --git a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
++index d592519..ae576f8 100644
++--- a/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
+++++ b/client/src/features/ads/components/details/ad-details-gallery/ad-details-gallery.tsx
++@@ -5,14 +5,10 @@ import { PlaceholderGallery } from "@/features/ads/components/details/placeholde
++ 
++ import css from "./ad-details-gallery.module.css";
++ 
++-type AdDetailsGalleryProps = {
++-  photos?: string[];
++-};
++-
++-export const AdDetailsGallery = ({ photos }: AdDetailsGalleryProps): JSX.Element => {
+++export const AdDetailsGallery = (): JSX.Element => {
++   return (
++     <Flex vertical gap={12} className={css.gallery}>
++-      <PlaceholderGallery photos={photos} />
+++      <PlaceholderGallery />
++     </Flex>
++   );
++ };
++diff --git a/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx b/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
++index ab0a4f7..cea8162 100644
++--- a/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
+++++ b/client/src/features/ads/components/details/ad-specifications/ad-specifications.tsx
++@@ -1,5 +1,5 @@
++ import { Descriptions, Typography } from "antd";
++-import type { JSX } from "react";
+++import type { JSX, ReactNode } from "react";
++ 
++ import type { AdType } from "@/features/ads/types/ad.types";
++ 
++@@ -9,29 +9,53 @@ type AdSpecificationsProps = {
++   params: AdType["params"];
++ };
++ 
++-type SpecificationFieldConfig = {
++-  key: string;
++-  label: string;
++-  format?: (value: unknown) => string;
++-};
+++type SpecEntry = { label: string; value: ReactNode };
+++
+++const getSpecEntries = (params: AdType["params"]): SpecEntry[] => {
+++  const entries: SpecEntry[] = [];
++ 
++-const SPECIFICATION_FIELDS: SpecificationFieldConfig[] = [
++-  { key: "type", label: "Тип" },
++-  { key: "brand", label: "Бренд" },
++-  { key: "model", label: "Модель" },
++-  { key: "color", label: "Цвет" },
++-  { key: "condition", label: "Состояние" },
++-  { key: "address", label: "Адрес" },
++-  { key: "area", label: "Площадь", format: (value) => `${value} м²` },
++-  { key: "floor", label: "Этаж" },
++-  { key: "transmission", label: "Коробка" },
++-  { key: "mileage", label: "Пробег", format: (value) => `${value} км` },
++-  { key: "enginePower", label: "Мощность", format: (value) => `${value} л.с.` },
++-  { key: "yearOfManufacture", label: "Год выпуска" }
++-];
+++  if ("type" in params && params.type) {
+++    entries.push({ label: "Тип", value: params.type });
+++  }
+++  if ("brand" in params && params.brand) {
+++    entries.push({ label: "Бренд", value: params.brand });
+++  }
+++  if ("model" in params && params.model) {
+++    entries.push({ label: "Модель", value: params.model });
+++  }
+++  if ("color" in params && params.color) {
+++    entries.push({ label: "Цвет", value: params.color });
+++  }
+++  if ("condition" in params && params.condition) {
+++    entries.push({ label: "Состояние", value: params.condition });
+++  }
+++  if ("address" in params && params.address) {
+++    entries.push({ label: "Адрес", value: params.address });
+++  }
+++  if ("area" in params && params.area) {
+++    entries.push({ label: "Площадь", value: `${params.area} м²` });
+++  }
+++  if ("floor" in params && params.floor) {
+++    entries.push({ label: "Этаж", value: params.floor });
+++  }
+++  if ("transmission" in params && params.transmission) {
+++    entries.push({ label: "Коробка", value: params.transmission });
+++  }
+++  if ("mileage" in params && params.mileage) {
+++    entries.push({ label: "Пробег", value: `${params.mileage} км` });
+++  }
+++  if ("enginePower" in params && params.enginePower) {
+++    entries.push({ label: "Мощность", value: `${params.enginePower} л.с.` });
+++  }
+++  if ("yearOfManufacture" in params && params.yearOfManufacture) {
+++    entries.push({ label: "Год выпуска", value: params.yearOfManufacture });
+++  }
+++
+++  return entries;
+++};
++ 
++ export const AdSpecifications = ({ params }: AdSpecificationsProps): JSX.Element => {
++-  const paramsRecord = params as Record<string, unknown>;
+++  const entries = getSpecEntries(params);
++ 
++   return (
++     <div>
++@@ -39,22 +63,11 @@ export const AdSpecifications = ({ params }: AdSpecificationsProps): JSX.Element
++         Характеристики
++       </Typography.Title>
++       <Descriptions column={1} size="small" className={css.descriptions}>
++-        {SPECIFICATION_FIELDS.map((field) => {
++-          if (!(field.key in paramsRecord)) {
++-            return null;
++-          }
++-
++-          const value = paramsRecord[field.key];
++-          if (!value) {
++-            return null;
++-          }
++-
++-          return (
++-            <Descriptions.Item key={field.key} label={field.label}>
++-              {field.format ? field.format(value) : String(value)}
++-            </Descriptions.Item>
++-          );
++-        })}
+++        {entries.map((entry) => (
+++          <Descriptions.Item key={entry.label} label={entry.label}>
+++            {entry.value}
+++          </Descriptions.Item>
+++        ))}
++       </Descriptions>
++     </div>
++   );
++diff --git a/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx b/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx
++new file mode 100644
++index 0000000..0485c46
++--- /dev/null
+++++ b/client/src/features/ads/components/edit/ad-edit-description-field/ad-edit-description-field.tsx
++@@ -0,0 +1,50 @@
+++import { type JSX } from "react";
+++import { Flex, Form, Input, Typography } from "antd";
+++
+++import {
+++  DESCRIPTION_GEN_LABELS,
+++  DESCRIPTION_IMPROVE_LABELS,
+++  MAX_DESCRIPTION_LENGTH
+++} from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+++
+++import { AiGenerateButton } from "@/shared/ui";
+++
+++type AdEditDescriptionFieldProps = {
+++  description: string;
+++  isPending: boolean;
+++  onGenerateDescription?: (currentText: string) => Promise<string>;
+++  onApplyDescription: (text: string) => void;
+++};
+++
+++export const AdEditDescriptionField = ({
+++  description,
+++  isPending,
+++  onGenerateDescription,
+++  onApplyDescription
+++}: AdEditDescriptionFieldProps): JSX.Element => {
+++  const isDescriptionEmpty = description.trim().length === 0;
+++
+++  return (
+++    <Form.Item
+++      name="description"
+++      label={
+++        <Flex align="center" gap={12}>
+++          <span>Описание</span>
+++          {onGenerateDescription && (
+++            <AiGenerateButton
+++              onGenerate={() => onGenerateDescription(description)}
+++              onApply={onApplyDescription}
+++              labels={isDescriptionEmpty ? DESCRIPTION_GEN_LABELS : DESCRIPTION_IMPROVE_LABELS}
+++            />
+++          )}
+++        </Flex>
+++      }
+++      extra={
+++        <Typography.Text type="secondary" style={{ fontSize: 12 }}>
+++          {description.length} / {MAX_DESCRIPTION_LENGTH}
+++        </Typography.Text>
+++      }>
+++      <Input.TextArea rows={6} maxLength={MAX_DESCRIPTION_LENGTH} disabled={isPending} />
+++    </Form.Item>
+++  );
+++};
++diff --git a/client/src/features/ads/components/edit/ad-edit-description-field/index.ts b/client/src/features/ads/components/edit/ad-edit-description-field/index.ts
++new file mode 100644
++index 0000000..0f33294
++--- /dev/null
+++++ b/client/src/features/ads/components/edit/ad-edit-description-field/index.ts
++@@ -0,0 +1 @@
+++export { AdEditDescriptionField } from "./ad-edit-description-field";
++diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
++similarity index 56%
++rename from client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts
++rename to client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
++index f5f4d7a..037a16a 100644
++--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.constants.ts
+++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.constants.ts
++@@ -29,3 +29,28 @@ export const REAL_ESTATE_TYPE_OPTIONS = [
++   { value: "house", label: "Дом" },
++   { value: "room", label: "Комната" }
++ ];
+++
+++const DEFAULT_AI_LABELS = {
+++  buttonLoading: "Выполняется запрос",
+++  buttonRetry: "Повторить запрос",
+++  popoverTitle: "Ответ AI:",
+++  popoverErrorTitle: "Ошибка запроса",
+++  errorMessage: "Не удалось получить оценку. Попробуйте позже.",
+++  applyButton: "Применить",
+++  closeButton: "Закрыть"
+++};
+++
+++export const PRICE_AI_LABELS = {
+++  buttonDefault: "Узнать рыночную цену",
+++  ...DEFAULT_AI_LABELS
+++};
+++
+++export const DESCRIPTION_GEN_LABELS = {
+++  buttonDefault: "Придумать описание",
+++  ...DEFAULT_AI_LABELS
+++};
+++
+++export const DESCRIPTION_IMPROVE_LABELS = {
+++  buttonDefault: "Улучшить описание",
+++  ...DEFAULT_AI_LABELS
+++};
++diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
++index d34f000..5f3832b 100644
++--- a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
+++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.tsx
++@@ -1,45 +1,18 @@
++ import { type JSX, useCallback, useEffect } from "react";
++-import { Button, Divider, Flex, Form, Input, Select, Typography } from "antd";
+++import { Button, Divider, Flex, Form, Input, Select } from "antd";
++ import { useNavigate } from "react-router-dom";
++ 
++ import { useFormDraft } from "@/features/ads/hooks";
++-import { useUpdateAd } from "@/features/ads/hooks/use-replace-ad";
++-import { CATEGORY_OPTIONS, MAX_DESCRIPTION_LENGTH } from "@/features/ads/pages/ad-edit-page";
+++import { useUpdateAd } from "@/features/ads/hooks/use-update-ad";
++ import { type AdCategory, type AdType, type AdUpdateType } from "@/features/ads/types";
++ 
++-import { AiGenerateButton } from "@/shared/ui";
++-
+++import { AdEditDescriptionField } from "../ad-edit-description-field";
++ import { AdEditParamsFields } from "../ad-edit-params-fields";
+++import { AdEditPriceField } from "../ad-edit-price-field";
++ 
++-import css from "./ad-edit-form.module.css";
++-
++-const { Text } = Typography;
++-const { TextArea } = Input;
++-
++-const DEFAULT_AI_LABELS = {
++-  buttonLoading: "Выполняется запрос",
++-  buttonRetry: "Повторить запрос",
++-  popoverTitle: "Ответ AI:",
++-  popoverErrorTitle: "Ошибка запроса",
++-  errorMessage: "Не удалось получить оценку. Попробуйте позже.",
++-  applyButton: "Применить",
++-  closeButton: "Закрыть"
++-};
++-
++-const PRICE_AI_LABELS = {
++-  buttonDefault: "Узнать рыночную цену",
++-  ...DEFAULT_AI_LABELS
++-};
++-
++-const DESCRIPTION_GEN_LABELS = {
++-  buttonDefault: "Придумать описание",
++-  ...DEFAULT_AI_LABELS
++-};
+++import { CATEGORY_OPTIONS } from "./ad-edit-form.constants";
++ 
++-const DESCRIPTION_IMPROVE_LABELS = {
++-  buttonDefault: "Улучшить описание",
++-  ...DEFAULT_AI_LABELS
++-};
+++import css from "./ad-edit-form.module.css";
++ 
++ type AdEditFormProps = {
++   adId: number;
++@@ -48,12 +21,6 @@ type AdEditFormProps = {
++   onGenerateDescription?: (currentText: string) => Promise<string>;
++ };
++ 
++-const extractPrice = (value: string): string | null => {
++-  const cleaned = value.replace(/\s/g, "");
++-  const match = cleaned.match(/\d+([.,]\d+)?/);
++-  return match ? match[0].replace(",", ".") : null;
++-};
++-
++ export const AdEditForm = ({
++   adId,
++   initialValues,
++@@ -68,7 +35,6 @@ export const AdEditForm = ({
++ 
++   const category = Form.useWatch("category", form);
++   const description = Form.useWatch("description", form) ?? "";
++-  const isDescriptionEmpty = description.trim().length === 0;
++ 
++   useEffect(() => {
++     if (initialValues && !form.isFieldsTouched()) {
++@@ -149,64 +115,30 @@ export const AdEditForm = ({
++ 
++       <Divider className={css.divider} />
++ 
++-      <div className={css.fieldFixedWidth}>
++-        <Text style={{ display: "block", marginBottom: 8 }}>Цена</Text>
++-        <Flex gap={8} align="flex-start">
++-          <Form.Item name="price" noStyle rules={[{ required: true }]}>
++-            <Input
++-              type="number"
++-              allowClear
++-              disabled={isPending}
++-              className={css.priceInput}
++-              placeholder="0"
++-            />
++-          </Form.Item>
++-          {onGeneratePrice && (
++-            <AiGenerateButton
++-              onGenerate={onGeneratePrice}
++-              onTransform={(raw) => extractPrice(raw) ?? ""}
++-              onApply={handleApplyPrice}
++-              labels={PRICE_AI_LABELS}
++-            />
++-          )}
++-        </Flex>
++-      </div>
+++      <AdEditPriceField
+++        isPending={isPending}
+++        onGeneratePrice={onGeneratePrice}
+++        onApplyPrice={handleApplyPrice}
+++      />
++ 
++       <Divider className={css.divider} />
++ 
++       {category && (
++         <>
++           <div className={css.section}>
++-            <Text strong className={css.sectionTitle}>
++-              Характеристики
++-            </Text>
+++            <span className={css.sectionTitle}>Характеристики</span>
++             <AdEditParamsFields category={category as AdCategory} />
++           </div>
++           <Divider className={css.divider} />
++         </>
++       )}
++ 
++-      <Form.Item
++-        name="description"
++-        label={
++-          <Flex align="center" gap={12}>
++-            <span>Описание</span>
++-            {onGenerateDescription && (
++-              <AiGenerateButton
++-                onGenerate={() => onGenerateDescription(description)}
++-                onApply={handleApplyDescription}
++-                labels={isDescriptionEmpty ? DESCRIPTION_GEN_LABELS : DESCRIPTION_IMPROVE_LABELS}
++-              />
++-            )}
++-          </Flex>
++-        }
++-        extra={
++-          <Text type="secondary" style={{ fontSize: 12 }}>
++-            {description.length} / {MAX_DESCRIPTION_LENGTH}
++-          </Text>
++-        }>
++-        <TextArea rows={6} maxLength={MAX_DESCRIPTION_LENGTH} disabled={isPending} />
++-      </Form.Item>
+++      <AdEditDescriptionField
+++        description={description}
+++        isPending={isPending}
+++        onGenerateDescription={onGenerateDescription}
+++        onApplyDescription={handleApplyDescription}
+++      />
++ 
++       <Flex gap={8}>
++         <Button type="primary" htmlType="submit" loading={isPending}>
++diff --git a/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts
++new file mode 100644
++index 0000000..3fd8ef6
++--- /dev/null
+++++ b/client/src/features/ads/components/edit/ad-edit-form/ad-edit-form.utils.ts
++@@ -0,0 +1,5 @@
+++export const extractPrice = (value: string): string | null => {
+++  const cleaned = value.replace(/\s/g, "");
+++  const match = cleaned.match(/\d+([.,]\d+)?/);
+++  return match ? match[0].replace(",", ".") : null;
+++};
++diff --git a/client/src/features/ads/components/edit/ad-edit-form/index.ts b/client/src/features/ads/components/edit/ad-edit-form/index.ts
++index 3b1a965..270f1aa 100644
++--- a/client/src/features/ads/components/edit/ad-edit-form/index.ts
+++++ b/client/src/features/ads/components/edit/ad-edit-form/index.ts
++@@ -1 +1,3 @@
++ export * from "./ad-edit-form";
+++export * from "./ad-edit-form.constants";
+++export * from "./ad-edit-form.utils";
++diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
++index fb07ee6..1c72c21 100644
++--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
+++++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/auto-fields.tsx
++@@ -1,7 +1,7 @@
++ import { type JSX } from "react";
++ 
++ import { ParamsFieldsList } from "@/features/ads/components";
++-import { AUTO_TRANSMISSION_OPTIONS } from "@/features/ads/pages/ad-edit-page";
+++import { AUTO_TRANSMISSION_OPTIONS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
++ import type { FieldConfig } from "@/features/ads/types";
++ 
++ const FIELDS: FieldConfig[] = [
++diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
++index 5b09fb9..54c7e65 100644
++--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
+++++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/electronics-fields.tsx
++@@ -4,7 +4,7 @@ import { ParamsFieldsList } from "@/features/ads/components";
++ import {
++   ELECTRONICS_CONDITION_OPTIONS,
++   ELECTRONICS_TYPE_OPTIONS
++-} from "@/features/ads/pages/ad-edit-page";
+++} from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
++ import type { FieldConfig } from "@/features/ads/types";
++ 
++ const FIELDS: FieldConfig[] = [
++diff --git a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
++index 0ebba6c..a2c300c 100644
++--- a/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
+++++ b/client/src/features/ads/components/edit/ad-edit-params-fields/fields/real-estate-fields.tsx
++@@ -1,7 +1,7 @@
++ import { type JSX } from "react";
++ 
++ import { ParamsFieldsList } from "@/features/ads/components";
++-import { REAL_ESTATE_TYPE_OPTIONS } from "@/features/ads/pages/ad-edit-page";
+++import { REAL_ESTATE_TYPE_OPTIONS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
++ import type { FieldConfig } from "@/features/ads/types";
++ 
++ const FIELDS: FieldConfig[] = [
++diff --git a/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx b/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx
++new file mode 100644
++index 0000000..05da6ff
++--- /dev/null
+++++ b/client/src/features/ads/components/edit/ad-edit-price-field/ad-edit-price-field.tsx
++@@ -0,0 +1,46 @@
+++import { type JSX } from "react";
+++import { Flex, Form, Input, Typography } from "antd";
+++
+++import { PRICE_AI_LABELS } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.constants";
+++import { extractPrice } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form.utils";
+++
+++import { AiGenerateButton } from "@/shared/ui";
+++
+++import css from "../ad-edit-form/ad-edit-form.module.css";
+++
+++type AdEditPriceFieldProps = {
+++  isPending: boolean;
+++  onGeneratePrice?: () => Promise<string>;
+++  onApplyPrice: (value: string) => void;
+++};
+++
+++export const AdEditPriceField = ({
+++  isPending,
+++  onGeneratePrice,
+++  onApplyPrice
+++}: AdEditPriceFieldProps): JSX.Element => {
+++  return (
+++    <div className={css.fieldFixedWidth}>
+++      <Typography.Text style={{ display: "block", marginBottom: 8 }}>Цена</Typography.Text>
+++      <Flex gap={8} align="flex-start">
+++        <Form.Item name="price" noStyle rules={[{ required: true }]}>
+++          <Input
+++            type="number"
+++            allowClear
+++            disabled={isPending}
+++            className={css.priceInput}
+++            placeholder="0"
+++          />
+++        </Form.Item>
+++        {onGeneratePrice && (
+++          <AiGenerateButton
+++            onGenerate={onGeneratePrice}
+++            onTransform={(raw) => extractPrice(raw) ?? ""}
+++            onApply={onApplyPrice}
+++            labels={PRICE_AI_LABELS}
+++          />
+++        )}
+++      </Flex>
+++    </div>
+++  );
+++};
++diff --git a/client/src/features/ads/components/edit/ad-edit-price-field/index.ts b/client/src/features/ads/components/edit/ad-edit-price-field/index.ts
++new file mode 100644
++index 0000000..4043f76
++--- /dev/null
+++++ b/client/src/features/ads/components/edit/ad-edit-price-field/index.ts
++@@ -0,0 +1 @@
+++export { AdEditPriceField } from "./ad-edit-price-field";
++diff --git a/client/src/features/ads/components/edit/index.ts b/client/src/features/ads/components/edit/index.ts
++index 4916e7c..85979ef 100644
++--- a/client/src/features/ads/components/edit/index.ts
+++++ b/client/src/features/ads/components/edit/index.ts
++@@ -1,3 +1,5 @@
++ export * from "./ad-edit-form";
+++export * from "./ad-edit-description-field";
++ export * from "./ad-edit-params-fields";
+++export * from "./ad-edit-price-field";
++ export * from "./params-field-list";
++diff --git a/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css b/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css
++new file mode 100644
++index 0000000..c3df19f
++--- /dev/null
+++++ b/client/src/features/ads/components/edit/params-field-list/params-field-list.module.css
++@@ -0,0 +1,9 @@
+++.fieldFixedWidth {
+++  width: 100%;
+++  max-width: 456px;
+++}
+++
+++.fieldFixedWidth :global(.ant-input):not(.ant-input-textarea),
+++.fieldFixedWidth :global(.ant-select-selector) {
+++  width: 100% !important;
+++}
++diff --git a/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx b/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
++index a795cfd..ba42782 100644
++--- a/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
+++++ b/client/src/features/ads/components/edit/params-field-list/params-field-list.tsx
++@@ -3,12 +3,12 @@ import { Form, Input, Select } from "antd";
++ 
++ import type { FieldConfig } from "@/features/ads/types";
++ 
+++import css from "./params-field-list.module.css";
+++
++ type ParamsFieldsListProps = {
++   fields: FieldConfig[];
++ };
++ 
++-import css from "../ad-edit-form/ad-edit-form.module.css";
++-
++ export const ParamsFieldsList = ({ fields }: ParamsFieldsListProps): JSX.Element => (
++   <>
++     {fields.map((field) => (
++diff --git a/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css b/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
++index c53cc14..6d1c589 100644
++--- a/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
+++++ b/client/src/features/ads/components/list/ad-card-list/ad-card-list.module.css
++@@ -12,6 +12,20 @@
++   display: flex;
++ }
++ 
+++.listView {
+++  display: grid;
+++  grid-template-columns: 1fr;
+++  gap: 12px;
+++
+++  padding-left: 0;
+++
+++  list-style: none;
+++}
+++
+++.listView > li {
+++  display: flex;
+++}
+++
++ .card {
++   width: 100%;
++   border-radius: 12px;
++diff --git a/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx b/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
++index cc51c67..9ed21bc 100644
++--- a/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
+++++ b/client/src/features/ads/components/list/ad-card-list/ad-card-list.tsx
++@@ -2,6 +2,8 @@ import { Card, Skeleton } from "antd";
++ import type { JSX } from "react";
++ 
++ import { AdCard } from "@/features/ads/components";
+++import type { ViewMode } from "@/features/ads/components/list/toolbar/toolbar";
+++import { SKELETON_COUNT } from "@/features/ads/constants";
++ import type { AdCardType } from "@/features/ads/types";
++ 
++ import css from "./ad-card-list.module.css";
++@@ -9,13 +11,16 @@ import css from "./ad-card-list.module.css";
++ type AdCardListProps = {
++   ads: AdCardType[];
++   isLoading: boolean;
+++  viewMode: ViewMode;
++ };
++ 
++-export const AdCardList = ({ ads, isLoading }: AdCardListProps): JSX.Element => {
+++export const AdCardList = ({ ads, isLoading, viewMode }: AdCardListProps): JSX.Element => {
+++  const listClass = viewMode === "list" ? css.listView : css.list;
+++
++   if (isLoading) {
++     return (
++-      <ul className={css.list}>
++-        {Array.from({ length: 10 }).map((_, index) => (
+++      <ul className={listClass}>
+++        {Array.from({ length: SKELETON_COUNT }).map((_, index) => (
++           <Card key={index} className={css.card}>
++             <Skeleton active />
++           </Card>
++@@ -25,7 +30,7 @@ export const AdCardList = ({ ads, isLoading }: AdCardListProps): JSX.Element =>
++   }
++ 
++   return (
++-    <ul className={css.list}>
+++    <ul className={listClass}>
++       {ads.map((ad) => (
++         <AdCard key={ad.id} ad={ad} />
++       ))}
++diff --git a/client/src/features/ads/components/list/filter/filter.tsx b/client/src/features/ads/components/list/filter/filter.tsx
++index f4fc911..6b94920 100644
++--- a/client/src/features/ads/components/list/filter/filter.tsx
+++++ b/client/src/features/ads/components/list/filter/filter.tsx
++@@ -2,25 +2,20 @@ import { type JSX } from "react";
++ import { Button, Collapse, Typography } from "antd";
++ 
++ import { FilterItems } from "@/features/ads/components";
++-import type { AdCategory } from "@/features/ads/types";
+++import { useFilterContext } from "@/features/ads/contexts";
++ 
++ import css from "./filter.module.css";
++ 
++-type FilterProps = {
++-  selectedCategories: AdCategory[];
++-  needsRevision: boolean;
++-  onCategoriesChange: (categories: AdCategory[]) => void;
++-  onNeedsRevisionChange: () => void;
++-  onReset: () => void;
++-};
++-export const Filter = ({
++-  selectedCategories,
++-  needsRevision,
++-  onCategoriesChange,
++-  onNeedsRevisionChange,
++-  onReset
++-}: FilterProps): JSX.Element => {
++-  const hasActiveFilters = selectedCategories.length > 0 || needsRevision;
+++export const Filter = (): JSX.Element => {
+++  const {
+++    categories,
+++    needsRevision,
+++    changeCategories,
+++    changeNeedsRevision,
+++    resetSearch
+++  } = useFilterContext();
+++
+++  const hasActiveFilters = categories.length > 0 || needsRevision;
++ 
++   return (
++     <div className={css.container}>
++@@ -39,10 +34,10 @@ export const Filter = ({
++               label: <span style={{ fontSize: "16px" }}>Категория</span>,
++               children: (
++                 <FilterItems
++-                  selectedCategories={selectedCategories}
+++                  selectedCategories={categories}
++                   needsRevision={needsRevision}
++-                  onCategoriesChange={onCategoriesChange}
++-                  onNeedsRevisionChange={onNeedsRevisionChange}
+++                  onCategoriesChange={changeCategories}
+++                  onNeedsRevisionChange={changeNeedsRevision}
++                 />
++               )
++             }
++@@ -50,7 +45,7 @@ export const Filter = ({
++         />
++       </div>
++ 
++-      <Button onClick={onReset} disabled={!hasActiveFilters} className={css.resetButton}>
+++      <Button onClick={resetSearch} disabled={!hasActiveFilters} className={css.resetButton}>
++         Сбросить фильтры
++       </Button>
++     </div>
++diff --git a/client/src/features/ads/components/list/toolbar/toolbar.tsx b/client/src/features/ads/components/list/toolbar/toolbar.tsx
++index dbc9bc7..2641b6e 100644
++--- a/client/src/features/ads/components/list/toolbar/toolbar.tsx
+++++ b/client/src/features/ads/components/list/toolbar/toolbar.tsx
++@@ -7,17 +7,23 @@ import { ADS_SORT_OPTIONS, DEFAULT_ADS_SORT } from "./toolbar.constants";
++ 
++ import { AppstoreOutlined, UnorderedListOutlined } from "@ant-design/icons";
++ 
+++export type ViewMode = "grid" | "list";
+++
++ type ToolbarProps = {
++   defaultSearchValue: string;
++   sortValue: AdsSortValue;
+++  viewMode: ViewMode;
++   onSearch: (value: string) => void;
++   onSortChange: (value: AdsSortValue) => void;
+++  onViewModeChange: (mode: ViewMode) => void;
++ };
++ export const Toolbar = ({
++   defaultSearchValue,
++   onSearch,
++   sortValue,
++-  onSortChange
+++  onSortChange,
+++  viewMode,
+++  onViewModeChange
++ }: ToolbarProps): JSX.Element => {
++   return (
++     <Card styles={{ body: { padding: "12px 16px" } }}>
++@@ -31,8 +37,16 @@ export const Toolbar = ({
++         />
++ 
++         <Space>
++-          <Button type="primary" icon={<AppstoreOutlined />} />
++-          <Button type="default" icon={<UnorderedListOutlined />} />
+++          <Button
+++            type={viewMode === "grid" ? "primary" : "default"}
+++            icon={<AppstoreOutlined />}
+++            onClick={() => onViewModeChange("grid")}
+++          />
+++          <Button
+++            type={viewMode === "list" ? "primary" : "default"}
+++            icon={<UnorderedListOutlined />}
+++            onClick={() => onViewModeChange("list")}
+++          />
++         </Space>
++         <Select
++           options={ADS_SORT_OPTIONS}
++diff --git a/client/src/features/ads/constants/index.ts b/client/src/features/ads/constants/index.ts
++index c1dadd8..4b16749 100644
++--- a/client/src/features/ads/constants/index.ts
+++++ b/client/src/features/ads/constants/index.ts
++@@ -8,3 +8,6 @@ export const ADS_PLURAL_VARIANTS: PluralizeVariants = {
++   many: "объявлений",
++   other: "объявления"
++ };
+++
+++export const PAGE_SIZE = 10;
+++export const SKELETON_COUNT = 10;
++diff --git a/client/src/features/ads/contexts/filter-context.tsx b/client/src/features/ads/contexts/filter-context.tsx
++new file mode 100644
++index 0000000..061a3d3
++--- /dev/null
+++++ b/client/src/features/ads/contexts/filter-context.tsx
++@@ -0,0 +1,48 @@
+++import { createContext, type ReactNode, useContext, useState, type JSX } from "react";
+++
+++import { useAdsFilters } from "@/features/ads/hooks/use-ads-filters";
+++import type { AdCategory, AdsSortValue } from "@/features/ads/types";
+++import type { AdCardType } from "@/features/ads/types/ad.types";
+++
+++import type { ViewMode } from "@/features/ads/components/list/toolbar/toolbar";
+++import { PAGE_SIZE } from "@/features/ads/constants";
+++
+++type FilterContextType = {
+++  items: AdCardType[];
+++  total: number;
+++  isLoading: boolean;
+++  page: number;
+++  searchValue: string;
+++  categories: AdCategory[];
+++  needsRevision: boolean;
+++  sortValue: AdsSortValue;
+++  viewMode: ViewMode;
+++  setPage: (page: number) => void;
+++  search: (value: string) => void;
+++  changeCategories: (values: AdCategory[]) => void;
+++  changeNeedsRevision: () => void;
+++  resetSearch: () => void;
+++  sort: (value: AdsSortValue) => void;
+++  setViewMode: (mode: ViewMode) => void;
+++};
+++
+++const FilterContext = createContext<FilterContextType | null>(null);
+++
+++export const useFilterContext = (): FilterContextType => {
+++  const context = useContext(FilterContext);
+++  if (!context) {
+++    throw new Error("useFilterContext must be used within FilterProvider");
+++  }
+++  return context;
+++};
+++
+++export const FilterProvider = ({ children }: { children: ReactNode }): JSX.Element => {
+++  const filters = useAdsFilters(PAGE_SIZE);
+++  const [viewMode, setViewMode] = useState<ViewMode>("grid");
+++
+++  return (
+++    <FilterContext.Provider value={{ ...filters, viewMode, setViewMode }}>
+++      {children}
+++    </FilterContext.Provider>
+++  );
+++};
++diff --git a/client/src/features/ads/contexts/index.ts b/client/src/features/ads/contexts/index.ts
++new file mode 100644
++index 0000000..3a84555
++--- /dev/null
+++++ b/client/src/features/ads/contexts/index.ts
++@@ -0,0 +1 @@
+++export { FilterProvider, useFilterContext } from "./filter-context";
++diff --git a/client/src/features/ads/hooks/index.ts b/client/src/features/ads/hooks/index.ts
++index 5c68350..c522b85 100644
++--- a/client/src/features/ads/hooks/index.ts
+++++ b/client/src/features/ads/hooks/index.ts
++@@ -1,5 +1,6 @@
+++export * from "./use-ad-ai-generation";
++ export * from "./use-get-ad";
++ export * from "./use-get-ads";
++ export * from "./use-ads-filters";
++-export * from "./use-replace-ad";
+++export * from "./use-update-ad";
++ export * from "./use-form-draft";
++diff --git a/client/src/features/ads/hooks/use-ad-ai-generation.ts b/client/src/features/ads/hooks/use-ad-ai-generation.ts
++new file mode 100644
++index 0000000..7bf4e0e
++--- /dev/null
+++++ b/client/src/features/ads/hooks/use-ad-ai-generation.ts
++@@ -0,0 +1,44 @@
+++import type { AdType } from "@/features/ads/types";
+++import {
+++  DESCRIPTION_GENERATION_INSTRUCTION,
+++  DESCRIPTION_IMPROVEMENT_INSTRUCTION,
+++  PRICE_INSTRUCTION
+++} from "@/features/ai/constants";
+++import { useGenerateResponse, useGetModels } from "@/features/ai/hooks";
+++
+++type UseAdAiGenerationReturnType = {
+++  isAiReady: boolean;
+++  generatePrice: () => Promise<string>;
+++  generateDescription: (currentDescription: string) => Promise<string>;
+++};
+++
+++export const useAdAiGeneration = (ad?: AdType): UseAdAiGenerationReturnType => {
+++  const { weakestModel, middleModel, strongestModel, isLoading } = useGetModels();
+++  const { mutateAsync } = useGenerateResponse();
+++
+++  const activeModel = middleModel || weakestModel || strongestModel;
+++  const isAiReady = !isLoading && !!activeModel;
+++
+++  const generatePrice = async (): Promise<string> => {
+++    if (!activeModel || !ad) throw new Error("Модель недоступна");
+++
+++    const prompt = `${PRICE_INSTRUCTION}\n\nТовар: ${ad.title}, Категория: ${ad.category}, Параметры: ${JSON.stringify(ad.params)}\n\nОТВЕТЬ НА РУССКОМ.`;
+++    return mutateAsync({ model: activeModel, prompt });
+++  };
+++
+++  const generateDescription = async (currentDescription: string): Promise<string> => {
+++    if (!activeModel || !ad) throw new Error("Модель недоступна");
+++
+++    const hasText = currentDescription.trim().length > 0;
+++    const instruction = hasText
+++      ? DESCRIPTION_IMPROVEMENT_INSTRUCTION
+++      : DESCRIPTION_GENERATION_INSTRUCTION;
+++
+++    const context = `Товар: ${ad.title}, Характеристики: ${JSON.stringify(ad.params)}${hasText ? `\nТекущий текст для улучшения: ${currentDescription}` : ""}`;
+++    const prompt = `${instruction}\n\n${context}\n\nВАЖНО: Пиши на русском языке.`;
+++
+++    return mutateAsync({ model: activeModel, prompt });
+++  };
+++
+++  return { isAiReady, generatePrice, generateDescription };
+++};
++diff --git a/client/src/features/ads/hooks/use-form-draft.ts b/client/src/features/ads/hooks/use-form-draft.ts
++index bfacb5a..212cb27 100644
++--- a/client/src/features/ads/hooks/use-form-draft.ts
+++++ b/client/src/features/ads/hooks/use-form-draft.ts
++@@ -1,13 +1,45 @@
++ import { useEffect } from "react";
++ import type { FormInstance } from "antd";
++ 
++-import { type AdUpdateType } from "../types";
+++import { type AdCategory, type AdUpdateType } from "../types";
++ 
++ type UseFormDraftReturnType = {
++   saveDraft: () => void;
++   clearDraft: () => void;
++ };
++ 
+++const VALID_CATEGORIES: AdCategory[] = ["auto", "real_estate", "electronics"];
+++
+++const isValidDraft = (data: unknown): data is Partial<AdUpdateType> => {
+++  if (typeof data !== "object" || data === null || Array.isArray(data)) {
+++    return false;
+++  }
+++
+++  const draft = data as Record<string, unknown>;
+++
+++  if (draft.category !== undefined && !VALID_CATEGORIES.includes(draft.category as AdCategory)) {
+++    return false;
+++  }
+++
+++  if (draft.title !== undefined && typeof draft.title !== "string") {
+++    return false;
+++  }
+++
+++  if (draft.price !== undefined && typeof draft.price !== "number") {
+++    return false;
+++  }
+++
+++  if (draft.description !== undefined && typeof draft.description !== "string") {
+++    return false;
+++  }
+++
+++  if (draft.params !== undefined && (typeof draft.params !== "object" || draft.params === null)) {
+++    return false;
+++  }
+++
+++  return true;
+++};
+++
++ export const useFormDraft = (
++   id: string | undefined,
++   form: FormInstance<AdUpdateType>
++@@ -19,10 +51,16 @@ export const useFormDraft = (
++     const savedDraft = localStorage.getItem(storageKey);
++     if (savedDraft) {
++       try {
++-        const parsed = JSON.parse(savedDraft);
++-        setTimeout(() => form.setFieldsValue(parsed), 0);
+++        const parsed: unknown = JSON.parse(savedDraft);
+++        if (isValidDraft(parsed)) {
+++          setTimeout(() => form.setFieldsValue(parsed), 0);
+++        } else {
+++          console.warn("Черновик повреждён, удаляем:", storageKey);
+++          localStorage.removeItem(storageKey);
+++        }
++       } catch (e) {
++         console.error("Ошибка парсинга черновика", e);
+++        localStorage.removeItem(storageKey);
++       }
++     }
++   }, [id, form, storageKey]);
++diff --git a/client/src/features/ads/hooks/use-replace-ad.ts b/client/src/features/ads/hooks/use-update-ad.ts
++similarity index 58%
++rename from client/src/features/ads/hooks/use-replace-ad.ts
++rename to client/src/features/ads/hooks/use-update-ad.ts
++index d3b651a..547d8ee 100644
++--- a/client/src/features/ads/hooks/use-replace-ad.ts
+++++ b/client/src/features/ads/hooks/use-update-ad.ts
++@@ -7,14 +7,16 @@ import { replaceAd, type UpdateAdResponseType } from "../api/replace-ad";
++ 
++ import { useMutation, type UseMutationResult, useQueryClient } from "@tanstack/react-query";
++ 
+++import { type ApiErrorResponse, getApiErrorMessage } from "@/shared/api";
+++
++ export const useUpdateAd = (
++   id: number
++-): UseMutationResult<UpdateAdResponseType, AxiosError<UpdateAdResponseType>, AdUpdateType> => {
+++): UseMutationResult<UpdateAdResponseType, AxiosError<ApiErrorResponse>, AdUpdateType> => {
++   const queryClient = useQueryClient();
++ 
++   const { message } = App.useApp();
++ 
++-  return useMutation<UpdateAdResponseType, AxiosError<UpdateAdResponseType>, AdUpdateType>({
+++  return useMutation<UpdateAdResponseType, AxiosError<ApiErrorResponse>, AdUpdateType>({
++     mutationFn: (data: AdUpdateType) => replaceAd(id, data),
++ 
++     onSuccess: (data) => {
++@@ -26,15 +28,7 @@ export const useUpdateAd = (
++     },
++ 
++     onError: (error) => {
++-      const serverError = error.response?.data?.error;
++-
++-      if (typeof serverError === "string") {
++-        void message.error(serverError);
++-      } else if (serverError && typeof serverError === "object") {
++-        void message.error("Ошибка валидации полей");
++-      } else {
++-        void message.error("Произошла сетевая ошибка");
++-      }
+++      void message.error(getApiErrorMessage(error));
++     }
++   });
++ };
++diff --git a/client/src/features/ads/index.ts b/client/src/features/ads/index.ts
++new file mode 100644
++index 0000000..2331d4f
++--- /dev/null
+++++ b/client/src/features/ads/index.ts
++@@ -0,0 +1,8 @@
+++export * from "./api";
+++export * from "./components";
+++export * from "./constants";
+++export * from "./contexts";
+++export * from "./hooks";
+++export * from "./lib";
+++export * from "./types";
+++export * from "./utils";
++diff --git a/client/src/features/ads/lib/get-missing-fields.ts b/client/src/features/ads/lib/get-missing-fields.ts
++index d53cbdb..df45aa6 100644
++--- a/client/src/features/ads/lib/get-missing-fields.ts
+++++ b/client/src/features/ads/lib/get-missing-fields.ts
++@@ -1,18 +1,26 @@
++ import type { AdResponse } from "@/features/ads/api/get-ad";
++ import { FIELD_LABELS, REQUIRED_FIELDS_BY_CATEGORY } from "@/features/ads/lib/labels";
++ 
+++const getParamValue = (params: AdResponse["params"], key: string): unknown => {
+++  if (key in params) {
+++    return (params as Record<string, unknown>)[key];
+++  }
+++  return undefined;
+++};
+++
++ export const getMissingFields = (ad: AdResponse): string[] => {
++   const missing: string[] = [];
++   const missingSet = new Set<string>();
++-  const paramsRecord = ad.params as Record<string, unknown>;
++ 
++   if (!ad.description) {
++     missing.push("Описание");
++     missingSet.add("Описание");
++   }
++ 
++-  REQUIRED_FIELDS_BY_CATEGORY[ad.category].forEach((key) => {
++-    const value = paramsRecord[key];
+++  const requiredFields = REQUIRED_FIELDS_BY_CATEGORY[ad.category];
+++
+++  requiredFields.forEach((key) => {
+++    const value = getParamValue(ad.params, key);
++ 
++     if (!value) {
++       const fieldName = FIELD_LABELS[key] ?? key;
++@@ -24,15 +32,18 @@ export const getMissingFields = (ad: AdResponse): string[] => {
++     }
++   });
++ 
++-  Object.entries(paramsRecord).forEach(([key, value]) => {
++-    const isKnownCategoryField = REQUIRED_FIELDS_BY_CATEGORY[ad.category].includes(key);
++-
++-    if (!isKnownCategoryField && !value) {
++-      const fieldName = FIELD_LABELS[key] ?? key;
++-
++-      if (!missingSet.has(fieldName)) {
++-        missing.push(fieldName);
++-        missingSet.add(fieldName);
+++  const paramKeys = Object.keys(ad.params);
+++  paramKeys.forEach((key) => {
+++    const isRequired = requiredFields.includes(key);
+++    if (!isRequired) {
+++      const value = getParamValue(ad.params, key);
+++      if (!value) {
+++        const fieldName = FIELD_LABELS[key] ?? key;
+++
+++        if (!missingSet.has(fieldName)) {
+++          missing.push(fieldName);
+++          missingSet.add(fieldName);
+++        }
++       }
++     }
++   });
++diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css
++deleted file mode 100644
++index 34f14c9..0000000
++--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.module.css
+++++ /dev/null
++@@ -1,33 +0,0 @@
++-.aiPopover :global(.ant-popover-inner) {
++-  min-width: 320px;
++-  padding: 16px;
++-  border-radius: 12px;
++-  background: white;
++-}
++-
++-.aiContent {
++-  display: flex;
++-  flex-direction: column;
++-  gap: 12px;
++-}
++-
++-.aiTitle {
++-  font-size: 16px;
++-  font-weight: 600;
++-}
++-
++-.aiText {
++-  font-size: 14px;
++-  line-height: 1.5;
++-  white-space: pre-line;
++-}
++-
++-.aiActions {
++-  display: flex;
++-  gap: 8px;
++-}
++-
++-.highlight {
++-  border-color: #52c41a !important;
++-  box-shadow: 0 0 0 2px rgb(82 196 26 / 20%);
++-}
++diff --git a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
++index 43db414..7b5c3d5 100644
++--- a/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
+++++ b/client/src/features/ads/pages/ad-edit-page/ad-edit-page.tsx
++@@ -4,54 +4,25 @@ import { useParams } from "react-router-dom";
++ 
++ import { AdEditForm } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form";
++ import { AdEditFormSkeleton } from "@/features/ads/components/edit/ad-edit-form/ad-edit-form-skeleton";
+++import { useAdAiGeneration } from "@/features/ads/hooks/use-ad-ai-generation";
++ import { useGetAd } from "@/features/ads/hooks";
++-import {
++-  DESCRIPTION_GENERATION_INSTRUCTION,
++-  DESCRIPTION_IMPROVEMENT_INSTRUCTION,
++-  PRICE_INSTRUCTION
++-} from "@/features/ai/constants";
++-import { useGenerateResponse, useGetModels } from "@/features/ai/hooks";
++ 
++ import { Container } from "@/shared/ui";
++ 
++-const { Title } = Typography;
++-
++ export const AdEditPage = (): JSX.Element => {
++   const { id } = useParams<{ id: string }>();
++   const adId = Number(id);
++ 
++-  const { ad, isLoading: isAdLoading, isError, errorMessage } = useGetAd(adId);
++-  const { weakestModel, middleModel, strongestModel, isLoading: isModelsLoading } = useGetModels();
++-  const { mutateAsync } = useGenerateResponse();
++-
++-  const activeModel = middleModel || weakestModel || strongestModel;
++-
++-  const handleGeneratePrice = async (): Promise<string> => {
++-    if (!activeModel || !ad) throw new Error("Модель недоступна");
++-    const prompt = `${PRICE_INSTRUCTION}\n\nТовар: ${ad.title}, Категория: ${ad.category}, Параметры: ${JSON.stringify(ad.params)}\n\nОТВЕТЬ НА РУССКОМ.`;
++-    return mutateAsync({ model: activeModel, prompt });
++-  };
++-
++-  const handleGenerateDescription = async (currentDescription: string): Promise<string> => {
++-    if (!activeModel || !ad) throw new Error("Модель недоступна");
++-    const hasText = currentDescription.trim().length > 0;
++-    const instruction = hasText
++-      ? DESCRIPTION_IMPROVEMENT_INSTRUCTION
++-      : DESCRIPTION_GENERATION_INSTRUCTION;
++-
++-    const context = `Товар: ${ad.title}, Характеристики: ${JSON.stringify(ad.params)}${hasText ? `\nТекущий текст для улучшения: ${currentDescription}` : ""}`;
++-    const prompt = `${instruction}\n\n${context}\n\nВАЖНО: Пиши на русском языке.`;
++-
++-    return mutateAsync({ model: activeModel, prompt });
++-  };
+++  const { ad, isLoading, isError, errorMessage } = useGetAd(adId);
+++  const { isAiReady, generatePrice, generateDescription } = useAdAiGeneration(ad);
++ 
++   return (
++     <Container maxWidth={942}>
++-      <Title level={3} style={{ marginBottom: 24 }}>
+++      <Typography.Title level={3} style={{ marginBottom: 24 }}>
++         Редактирование объявления
++-      </Title>
+++      </Typography.Title>
++ 
++-      {isAdLoading ? (
+++      {isLoading ? (
++         <AdEditFormSkeleton />
++       ) : isError || !ad || Number.isNaN(adId) ? (
++         <Alert
++@@ -64,8 +35,8 @@ export const AdEditPage = (): JSX.Element => {
++         <AdEditForm
++           adId={adId}
++           initialValues={ad}
++-          onGeneratePrice={isModelsLoading ? undefined : handleGeneratePrice}
++-          onGenerateDescription={isModelsLoading ? undefined : handleGenerateDescription}
+++          onGeneratePrice={isAiReady ? generatePrice : undefined}
+++          onGenerateDescription={isAiReady ? generateDescription : undefined}
++         />
++       )}
++     </Container>
++diff --git a/client/src/features/ads/pages/ad-edit-page/index.ts b/client/src/features/ads/pages/ad-edit-page/index.ts
++index c98140b..22d2266 100644
++--- a/client/src/features/ads/pages/ad-edit-page/index.ts
+++++ b/client/src/features/ads/pages/ad-edit-page/index.ts
++@@ -1,2 +1 @@
++ export * from "./ad-edit-page";
++-export * from "./ad-edit-page.constants";
++diff --git a/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx b/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
++index a1272b1..b749908 100644
++--- a/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
+++++ b/client/src/features/ads/pages/ads-list-page/ads-list-page.tsx
++@@ -3,33 +3,28 @@ import { Flex, Space, Typography } from "antd";
++ 
++ import { Filter, PaginationComponent, Toolbar } from "@/features/ads/components";
++ import { AdCardList } from "@/features/ads/components/list/ad-card-list";
++-import { ADS_PLURAL_VARIANTS } from "@/features/ads/constants";
++-import { useAdsFilters } from "@/features/ads/hooks";
+++import { ADS_PLURAL_VARIANTS, PAGE_SIZE } from "@/features/ads/constants";
+++import { FilterProvider, useFilterContext } from "@/features/ads/contexts";
++ import { formatAdsCount } from "@/features/ads/utils";
++ 
++ import { Container } from "@/shared/ui";
++ 
++ import css from "./ads-list-page.module.css";
++ 
++-const pageSize = 10;
++-
++-export const AdsListPage = (): JSX.Element => {
+++const AdsListContent = (): JSX.Element => {
++   const {
++     items,
++     total,
++     isLoading,
++     page,
++     searchValue,
++-    categories,
++-    needsRevision,
++     sortValue,
+++    viewMode,
++     setPage,
++     search,
++-    changeCategories,
++-    changeNeedsRevision,
++-    resetSearch,
++-    sort
++-  } = useAdsFilters(pageSize);
+++    sort,
+++    setViewMode
+++  } = useFilterContext();
++ 
++   return (
++     <Container>
++@@ -47,13 +42,7 @@ export const AdsListPage = (): JSX.Element => {
++ 
++         <Flex align="stretch" gap={48} className={css.contentWrapper}>
++           <aside className={css.filterWrapper}>
++-            <Filter
++-              selectedCategories={categories}
++-              needsRevision={needsRevision}
++-              onCategoriesChange={changeCategories}
++-              onNeedsRevisionChange={changeNeedsRevision}
++-              onReset={resetSearch}
++-            />
+++            <Filter />
++           </aside>
++ 
++           <Flex vertical gap={16} className={css.sectionWrapper}>
++@@ -62,14 +51,16 @@ export const AdsListPage = (): JSX.Element => {
++               onSearch={search}
++               sortValue={sortValue}
++               onSortChange={sort}
+++              viewMode={viewMode}
+++              onViewModeChange={setViewMode}
++             />
++ 
++-            <AdCardList ads={items} isLoading={isLoading} />
+++            <AdCardList ads={items} isLoading={isLoading} viewMode={viewMode} />
++ 
++             <div className={css.pagination}>
++               <PaginationComponent
++                 current={page}
++-                pageSize={pageSize}
+++                pageSize={PAGE_SIZE}
++                 total={total}
++                 onChange={setPage}
++               />
++@@ -80,3 +71,11 @@ export const AdsListPage = (): JSX.Element => {
++     </Container>
++   );
++ };
+++
+++export const AdsListPage = (): JSX.Element => {
+++  return (
+++    <FilterProvider>
+++      <AdsListContent />
+++    </FilterProvider>
+++  );
+++};
++diff --git a/client/src/features/ai/api/generate-response.ts b/client/src/features/ai/api/generate-response.ts
++index 7e86e4c..21ffdb9 100644
++--- a/client/src/features/ai/api/generate-response.ts
+++++ b/client/src/features/ai/api/generate-response.ts
++@@ -6,6 +6,9 @@ import type {
++ 
++ import { apiOllama } from "@/shared/api";
++ 
+++const AI_TEMPERATURE = 0.1;
+++const AI_MAX_TOKENS = 300;
+++
++ export const generateResponse = async (
++   payload: GenerateRequestType
++ ): Promise<SimplifiedGenerateResponseType> => {
++@@ -14,8 +17,8 @@ export const generateResponse = async (
++     prompt: payload.prompt,
++     stream: false,
++     options: {
++-      temperature: 0.1,
++-      num_predict: 300
+++      temperature: AI_TEMPERATURE,
+++      num_predict: AI_MAX_TOKENS
++     }
++   });
++ 
++diff --git a/client/src/features/ai/index.ts b/client/src/features/ai/index.ts
++new file mode 100644
++index 0000000..48aff0b
++--- /dev/null
+++++ b/client/src/features/ai/index.ts
++@@ -0,0 +1,4 @@
+++export * from "./api";
+++export * from "./constants";
+++export * from "./hooks";
+++export * from "./types";
++diff --git a/client/src/features/ai/types/gerenate.types.ts b/client/src/features/ai/types/generate.types.ts
++similarity index 100%
++rename from client/src/features/ai/types/gerenate.types.ts
++rename to client/src/features/ai/types/generate.types.ts
++diff --git a/client/src/features/ai/types/index.ts b/client/src/features/ai/types/index.ts
++index 9678987..c51eb1d 100644
++--- a/client/src/features/ai/types/index.ts
+++++ b/client/src/features/ai/types/index.ts
++@@ -1,2 +1,2 @@
++ export * from "./models.types";
++-export * from "./gerenate.types";
+++export * from "./generate.types";
++diff --git a/client/src/shared/api/api-error.ts b/client/src/shared/api/api-error.ts
++new file mode 100644
++index 0000000..bd1fbef
++--- /dev/null
+++++ b/client/src/shared/api/api-error.ts
++@@ -0,0 +1,28 @@
+++import type { AxiosError } from "axios";
+++
+++export type ApiErrorResponse = {
+++  error?: string | Record<string, string[]>;
+++  message?: string;
+++};
+++
+++export const getApiErrorMessage = (
+++  error: AxiosError<ApiErrorResponse>,
+++  fallback = "Произошла сетевая ошибка"
+++): string => {
+++  const serverError = error.response?.data?.error;
+++
+++  if (typeof serverError === "string") {
+++    return serverError;
+++  }
+++
+++  if (serverError && typeof serverError === "object") {
+++    return "Ошибка валидации полей";
+++  }
+++
+++  const message = error.response?.data?.message;
+++  if (typeof message === "string") {
+++    return message;
+++  }
+++
+++  return fallback;
+++};
++diff --git a/client/src/shared/api/index.ts b/client/src/shared/api/index.ts
++index b1120b9..073cfe6 100644
++--- a/client/src/shared/api/index.ts
+++++ b/client/src/shared/api/index.ts
++@@ -1 +1,2 @@
++ export * from "./instance";
+++export * from "./api-error";
++diff --git a/client/src/shared/api/instance.ts b/client/src/shared/api/instance.ts
++index 744070a..3d5d14e 100644
++--- a/client/src/shared/api/instance.ts
+++++ b/client/src/shared/api/instance.ts
++@@ -1,21 +1,48 @@
++-import axios from "axios";
+++import axios, { type AxiosError } from "axios";
++ 
++ import { API_URL, OLLAMA_API_URL } from "@/shared/config";
++ 
+++const API_TIMEOUT = 15_000;
+++const OLLAMA_TIMEOUT = 60_000;
+++const MAX_RETRIES = 2;
+++const RETRY_DELAY = 1_000;
+++
+++const isRetryable = (error: AxiosError): boolean => {
+++  if (!error.response) return true;
+++  return error.response.status >= 500;
+++};
+++
+++const delay = (ms: number): Promise<void> => new Promise((resolve) => setTimeout(resolve, ms));
+++
+++const createRetryInterceptor = (maxRetries: number) => {
+++  return async (error: AxiosError): Promise<unknown> => {
+++    const config = error.config;
+++    if (!config) return Promise.reject(error);
+++
+++    const retryCount = (config as Record<string, unknown>).__retryCount as number ?? 0;
+++
+++    if (retryCount >= maxRetries || !isRetryable(error)) {
+++      return Promise.reject(error);
+++    }
+++
+++    (config as Record<string, unknown>).__retryCount = retryCount + 1;
+++    await delay(RETRY_DELAY * (retryCount + 1));
+++
+++    return axios(config);
+++  };
+++};
+++
++ export const api = axios.create({
++   baseURL: API_URL,
++-  headers: { "Content-Type": "application/json" }
+++  headers: { "Content-Type": "application/json" },
+++  timeout: API_TIMEOUT
++ });
++ 
++ export const apiOllama = axios.create({
++   baseURL: OLLAMA_API_URL,
++-  headers: { "Content-Type": "application/json" }
+++  headers: { "Content-Type": "application/json" },
+++  timeout: OLLAMA_TIMEOUT
++ });
++ 
++-const errorHandler = (error: unknown): Promise<unknown> => {
++-  console.error(`API Error: ${error || "Cannot get error message."}`);
++-  return Promise.reject(error);
++-};
++-
++-api.interceptors.response.use((r) => r, errorHandler);
++-apiOllama.interceptors.response.use((r) => r, errorHandler);
+++api.interceptors.response.use((r) => r, createRetryInterceptor(MAX_RETRIES));
+++apiOllama.interceptors.response.use((r) => r, createRetryInterceptor(0));
++diff --git a/client/src/shared/index.ts b/client/src/shared/index.ts
++new file mode 100644
++index 0000000..b857841
++--- /dev/null
+++++ b/client/src/shared/index.ts
++@@ -0,0 +1,6 @@
+++export * from "./api";
+++export * from "./config";
+++export * from "./contexts";
+++export * from "./hooks";
+++export * from "./lib";
+++export * from "./ui";
++diff --git a/client/src/shared/ui/error-boundary/error-boundary.tsx b/client/src/shared/ui/error-boundary/error-boundary.tsx
++new file mode 100644
++index 0000000..b40ad94
++--- /dev/null
+++++ b/client/src/shared/ui/error-boundary/error-boundary.tsx
++@@ -0,0 +1,60 @@
+++import { Component, type ErrorInfo, type ReactNode } from "react";
+++import { Alert, Button, Flex } from "antd";
+++
+++type ErrorBoundaryProps = {
+++  children: ReactNode;
+++  fallback?: ReactNode;
+++};
+++
+++type ErrorBoundaryState = {
+++  hasError: boolean;
+++  error: Error | null;
+++};
+++
+++export class ErrorBoundary extends Component<ErrorBoundaryProps, ErrorBoundaryState> {
+++  constructor(props: ErrorBoundaryProps) {
+++    super(props);
+++    this.state = { hasError: false, error: null };
+++  }
+++
+++  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
+++    return { hasError: true, error };
+++  }
+++
+++  componentDidCatch(error: Error, errorInfo: ErrorInfo): void {
+++    console.error("ErrorBoundary caught:", error, errorInfo);
+++  }
+++
+++  handleReset = (): void => {
+++    this.setState({ hasError: false, error: null });
+++  };
+++
+++  render(): ReactNode {
+++    if (this.state.hasError) {
+++      if (this.props.fallback) {
+++        return this.props.fallback;
+++      }
+++
+++      return (
+++        <Flex
+++          vertical
+++          align="center"
+++          justify="center"
+++          style={{ padding: 48, minHeight: "50vh" }}>
+++          <Alert
+++            type="error"
+++            showIcon
+++            message="Что-то пошло не так"
+++            description={this.state.error?.message ?? "Произошла непредвиденная ошибка"}
+++            style={{ maxWidth: 600, width: "100%" }}
+++          />
+++          <Button type="primary" onClick={this.handleReset} style={{ marginTop: 16 }}>
+++            Попробовать снова
+++          </Button>
+++        </Flex>
+++      );
+++    }
+++
+++    return this.props.children;
+++  }
+++}
++diff --git a/client/src/shared/ui/error-boundary/index.ts b/client/src/shared/ui/error-boundary/index.ts
++new file mode 100644
++index 0000000..899c04f
++--- /dev/null
+++++ b/client/src/shared/ui/error-boundary/index.ts
++@@ -0,0 +1 @@
+++export { ErrorBoundary } from "./error-boundary";
++diff --git a/client/src/shared/ui/index.ts b/client/src/shared/ui/index.ts
++index d28c14e..31259c8 100644
++--- a/client/src/shared/ui/index.ts
+++++ b/client/src/shared/ui/index.ts
++@@ -1,2 +1,3 @@
++ export * from "./container";
++ export * from "./ai-generate-button";
+++export * from "./error-boundary";
